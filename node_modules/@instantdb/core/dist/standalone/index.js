var ir = Object.defineProperty;
var or = (n, e, t) => e in n ? ir(n, e, { enumerable: !0, configurable: !0, writable: !0, value: t }) : n[e] = t;
var h = (n, e, t) => or(n, typeof e != "symbol" ? e + "" : e, t);
function R(n) {
  if (typeof n == "number")
    return (Math.abs(n * 2654435761) >>> 0).toString(16);
  if (typeof n == "boolean") return n ? "1" : "0";
  if (n === null) return "null";
  if (n === void 0) return "undefined";
  if (typeof n == "string") {
    let e = 2166136261;
    for (let t = 0; t < n.length; t++)
      e ^= n.charCodeAt(t), e += (e << 1) + (e << 4) + (e << 7) + (e << 8) + (e << 24), e = e >>> 0;
    return e.toString(16);
  }
  if (Array.isArray(n)) {
    let e = 2166136261;
    for (let t = 0; t < n.length; t++) {
      e ^= (t + 1) * 2654435761;
      const r = R(n[t]);
      for (let s = 0; s < r.length; s++)
        e ^= r.charCodeAt(s), e *= 16777619, e = e >>> 0;
    }
    return e.toString(16);
  }
  if (typeof n == "object") {
    let e = 2166136261;
    const t = Object.keys(n).sort();
    for (let r = 0; r < t.length; r++) {
      const s = t[r], i = R(s);
      e ^= parseInt(i, 16), e *= 16777619, e = e >>> 0;
      const o = R(n[s]);
      e ^= parseInt(o, 16), e *= 16777619, e = e >>> 0;
    }
    return e.toString(16);
  }
  return R(String(n));
}
const j = {
  Remove: "remove",
  Replace: "replace",
  Add: "add"
}, bn = Symbol.for("__MUTATIVE_PROXY_DRAFT__"), ar = Symbol("__MUTATIVE_RAW_RETURN_SYMBOL__"), Le = Symbol.iterator, B = {
  mutable: "mutable",
  immutable: "immutable"
}, vt = {};
function ke(n, e) {
  return n instanceof Map ? n.has(e) : Object.prototype.hasOwnProperty.call(n, e);
}
function Ut(n, e) {
  if (e in n) {
    let t = Reflect.getPrototypeOf(n);
    for (; t; ) {
      const r = Reflect.getOwnPropertyDescriptor(t, e);
      if (r)
        return r;
      t = Reflect.getPrototypeOf(t);
    }
  }
}
function kt(n) {
  return Object.getPrototypeOf(n) === Set.prototype;
}
function Ot(n) {
  return Object.getPrototypeOf(n) === Map.prototype;
}
function Q(n) {
  var e;
  return (e = n.copy) !== null && e !== void 0 ? e : n.original;
}
function fe(n) {
  return !!O(n);
}
function O(n) {
  return typeof n != "object" ? null : n == null ? void 0 : n[bn];
}
function It(n) {
  var e;
  const t = O(n);
  return t ? (e = t.copy) !== null && e !== void 0 ? e : t.original : n;
}
function Z(n, e) {
  if (!n || typeof n != "object")
    return !1;
  let t;
  return Object.getPrototypeOf(n) === Object.prototype || Array.isArray(n) || n instanceof Map || n instanceof Set || !!(e != null && e.mark) && ((t = e.mark(n, B)) === B.immutable || typeof t == "function");
}
function mn(n, e = []) {
  if (Object.hasOwnProperty.call(n, "key")) {
    const t = n.parent.copy, r = O(oe(t, n.key));
    if (r !== null && (r == null ? void 0 : r.original) !== n.original)
      return null;
    const s = n.parent.type === 3, i = s ? Array.from(n.parent.setMap.keys()).indexOf(n.key) : n.key;
    if (!(s && t.size > i || ke(t, i)))
      return null;
    e.push(i);
  }
  if (n.parent)
    return mn(n.parent, e);
  e.reverse();
  try {
    cr(n.copy, e);
  } catch {
    return null;
  }
  return e;
}
function de(n) {
  return Array.isArray(n) ? 1 : n instanceof Map ? 2 : n instanceof Set ? 3 : 0;
}
function oe(n, e) {
  return de(n) === 2 ? n.get(e) : n[e];
}
function Ee(n, e, t) {
  de(n) === 2 ? n.set(e, t) : n[e] = t;
}
function Xe(n, e) {
  const t = O(n);
  return (t ? Q(t) : n)[e];
}
function ie(n, e) {
  return n === e ? n !== 0 || 1 / n === 1 / e : n !== n && e !== e;
}
function ot(n) {
  if (n)
    for (; n.finalities.revoke.length > 0; )
      n.finalities.revoke.pop()();
}
function le(n, e) {
  return e ? n : [""].concat(n).map((t) => {
    const r = `${t}`;
    return r.indexOf("/") === -1 && r.indexOf("~") === -1 ? r : r.replace(/~/g, "~0").replace(/\//g, "~1");
  }).join("/");
}
function cr(n, e) {
  for (let t = 0; t < e.length - 1; t += 1) {
    const r = e[t];
    if (n = oe(de(n) === 3 ? Array.from(n) : n, r), typeof n != "object")
      throw new Error(`Cannot resolve patch at '${e.join("/")}'.`);
  }
  return n;
}
function ur(n) {
  const e = Object.create(Object.getPrototypeOf(n));
  return Reflect.ownKeys(n).forEach((t) => {
    let r = Reflect.getOwnPropertyDescriptor(n, t);
    if (r.enumerable && r.configurable && r.writable) {
      e[t] = n[t];
      return;
    }
    r.writable || (r.writable = !0, r.configurable = !0), (r.get || r.set) && (r = {
      configurable: !0,
      writable: !0,
      enumerable: r.enumerable,
      value: n[t]
    }), Reflect.defineProperty(e, t, r);
  }), e;
}
const lr = Object.prototype.propertyIsEnumerable;
function gn(n, e) {
  let t;
  if (Array.isArray(n))
    return Array.prototype.concat.call(n);
  if (n instanceof Set) {
    if (!kt(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(n.values());
    }
    return Set.prototype.difference ? Set.prototype.difference.call(n, /* @__PURE__ */ new Set()) : new Set(n.values());
  } else if (n instanceof Map) {
    if (!Ot(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(n);
    }
    return new Map(n);
  } else if (e != null && e.mark && (t = e.mark(n, B), t !== void 0) && t !== B.mutable) {
    if (t === B.immutable)
      return ur(n);
    if (typeof t == "function") {
      if (e.enablePatches || e.enableAutoFreeze)
        throw new Error("You can't use mark and patches or auto freeze together.");
      return t();
    }
    throw new Error(`Unsupported mark result: ${t}`);
  } else if (typeof n == "object" && Object.getPrototypeOf(n) === Object.prototype) {
    const r = {};
    return Object.keys(n).forEach((s) => {
      r[s] = n[s];
    }), Object.getOwnPropertySymbols(n).forEach((s) => {
      lr.call(n, s) && (r[s] = n[s]);
    }), r;
  } else
    throw new Error("Please check mark() to ensure that it is a stable marker draftable function.");
}
function F(n) {
  n.copy || (n.copy = gn(n.original, n.options));
}
function Se(n) {
  if (!Z(n))
    return It(n);
  if (Array.isArray(n))
    return n.map(Se);
  if (n instanceof Map) {
    const t = Array.from(n.entries()).map(([r, s]) => [
      r,
      Se(s)
    ]);
    if (!Ot(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(t);
    }
    return new Map(t);
  }
  if (n instanceof Set) {
    const t = Array.from(n).map(Se);
    if (!kt(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(t);
    }
    return new Set(t);
  }
  const e = Object.create(Object.getPrototypeOf(n));
  for (const t in n)
    e[t] = Se(n[t]);
  return e;
}
function Fe(n) {
  return fe(n) ? Se(n) : n;
}
function te(n) {
  var e;
  n.assignedMap = (e = n.assignedMap) !== null && e !== void 0 ? e : /* @__PURE__ */ new Map(), n.operated || (n.operated = !0, n.parent && te(n.parent));
}
function Lt() {
  throw new Error("Cannot modify frozen object");
}
function pe(n, e, t, r, s) {
  {
    t = t ?? /* @__PURE__ */ new WeakMap(), r = r ?? [], s = s ?? [];
    const o = t.has(n) ? t.get(n) : n;
    if (r.length > 0) {
      const a = r.indexOf(o);
      if (o && typeof o == "object" && a !== -1)
        throw r[0] === o ? new Error("Forbids circular reference") : new Error(`Forbids circular reference: ~/${s.slice(0, a).map((c, u) => {
          if (typeof c == "symbol")
            return `[${c.toString()}]`;
          const l = r[u];
          return typeof c == "object" && (l instanceof Map || l instanceof Set) ? Array.from(l.keys()).indexOf(c) : c;
        }).join("/")}`);
      r.push(o), s.push(e);
    } else
      r.push(o);
  }
  if (Object.isFrozen(n) || fe(n)) {
    r.pop(), s.pop();
    return;
  }
  switch (de(n)) {
    case 2:
      for (const [a, c] of n)
        pe(a, a, t, r, s), pe(c, a, t, r, s);
      n.set = n.clear = n.delete = Lt;
      break;
    case 3:
      for (const a of n)
        pe(a, a, t, r, s);
      n.add = n.clear = n.delete = Lt;
      break;
    case 1:
      Object.freeze(n);
      let o = 0;
      for (const a of n)
        pe(a, o, t, r, s), o += 1;
      break;
    default:
      Object.freeze(n), Object.keys(n).forEach((a) => {
        const c = n[a];
        pe(c, a, t, r, s);
      });
  }
  r.pop(), s.pop();
}
function Ct(n, e) {
  const t = de(n);
  if (t === 0)
    Reflect.ownKeys(n).forEach((r) => {
      e(r, n[r], n);
    });
  else if (t === 1) {
    let r = 0;
    for (const s of n)
      e(r, s, n), r += 1;
  } else
    n.forEach((r, s) => e(s, r, n));
}
function _n(n, e, t) {
  if (fe(n) || !Z(n, t) || e.has(n) || Object.isFrozen(n))
    return;
  const r = n instanceof Set, s = r ? /* @__PURE__ */ new Map() : void 0;
  if (e.add(n), Ct(n, (i, o) => {
    var a;
    if (fe(o)) {
      const c = O(o);
      F(c);
      const u = !((a = c.assignedMap) === null || a === void 0) && a.size || c.operated ? c.copy : c.original;
      Ee(r ? s : n, i, u);
    } else
      _n(o, e, t);
  }), s) {
    const i = n, o = Array.from(i);
    i.clear(), o.forEach((a) => {
      i.add(s.has(a) ? s.get(a) : a);
    });
  }
}
function fr(n, e) {
  const t = n.type === 3 ? n.setMap : n.copy;
  n.finalities.revoke.length > 1 && n.assignedMap.get(e) && t && _n(oe(t, e), n.finalities.handledSet, n.options);
}
function at(n) {
  n.type === 3 && n.copy && (n.copy.clear(), n.setMap.forEach((e) => {
    n.copy.add(It(e));
  }));
}
function ct(n, e, t, r) {
  if (n.operated && n.assignedMap && n.assignedMap.size > 0 && !n.finalized) {
    if (t && r) {
      const i = mn(n);
      i && e(n, i, t, r);
    }
    n.finalized = !0;
  }
}
function Et(n, e, t, r) {
  const s = O(t);
  s && (s.callbacks || (s.callbacks = []), s.callbacks.push((i, o) => {
    var a;
    const c = n.type === 3 ? n.setMap : n.copy;
    if (ie(oe(c, e), t)) {
      let u = s.original;
      s.copy && (u = s.copy), at(n), ct(n, r, i, o), n.options.enableAutoFreeze && (n.options.updatedValues = (a = n.options.updatedValues) !== null && a !== void 0 ? a : /* @__PURE__ */ new WeakMap(), n.options.updatedValues.set(u, s.original)), Ee(c, e, u);
    }
  }), n.options.enableAutoFreeze && s.finalities !== n.finalities && (n.options.enableAutoFreeze = !1)), Z(t, n.options) && n.finalities.draft.push(() => {
    const i = n.type === 3 ? n.setMap : n.copy;
    ie(oe(i, e), t) && fr(n, e);
  });
}
function dr(n, e, t, r, s) {
  let { original: i, assignedMap: o, options: a } = n, c = n.copy;
  c.length < i.length && ([i, c] = [c, i], [t, r] = [r, t]);
  for (let u = 0; u < i.length; u += 1)
    if (o.get(u.toString()) && c[u] !== i[u]) {
      const l = e.concat([u]), f = le(l, s);
      t.push({
        op: j.Replace,
        path: f,
        // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
        value: Fe(c[u])
      }), r.push({
        op: j.Replace,
        path: f,
        // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
        value: Fe(i[u])
      });
    }
  for (let u = i.length; u < c.length; u += 1) {
    const l = e.concat([u]), f = le(l, s);
    t.push({
      op: j.Add,
      path: f,
      // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
      value: Fe(c[u])
    });
  }
  if (i.length < c.length) {
    const { arrayLengthAssignment: u = !0 } = a.enablePatches;
    if (u) {
      const l = e.concat(["length"]), f = le(l, s);
      r.push({
        op: j.Replace,
        path: f,
        value: i.length
      });
    } else
      for (let l = c.length; i.length < l; l -= 1) {
        const f = e.concat([l - 1]), d = le(f, s);
        r.push({
          op: j.Remove,
          path: d
        });
      }
  }
}
function hr({ original: n, copy: e, assignedMap: t }, r, s, i, o) {
  t.forEach((a, c) => {
    const u = oe(n, c), l = Fe(oe(e, c)), f = a ? ke(n, c) ? j.Replace : j.Add : j.Remove;
    if (ie(u, l) && f === j.Replace)
      return;
    const d = r.concat(c), p = le(d, o);
    s.push(f === j.Remove ? { op: f, path: p } : { op: f, path: p, value: l }), i.push(f === j.Add ? { op: j.Remove, path: p } : f === j.Remove ? { op: j.Add, path: p, value: u } : { op: j.Replace, path: p, value: u });
  });
}
function pr({ original: n, copy: e }, t, r, s, i) {
  let o = 0;
  n.forEach((a) => {
    if (!e.has(a)) {
      const c = t.concat([o]), u = le(c, i);
      r.push({
        op: j.Remove,
        path: u,
        value: a
      }), s.unshift({
        op: j.Add,
        path: u,
        value: a
      });
    }
    o += 1;
  }), o = 0, e.forEach((a) => {
    if (!n.has(a)) {
      const c = t.concat([o]), u = le(c, i);
      r.push({
        op: j.Add,
        path: u,
        value: a
      }), s.unshift({
        op: j.Remove,
        path: u,
        value: a
      });
    }
    o += 1;
  });
}
function Ce(n, e, t, r) {
  const { pathAsArray: s = !0 } = n.options.enablePatches;
  switch (n.type) {
    case 0:
    case 2:
      return hr(n, e, t, r, s);
    case 1:
      return dr(n, e, t, r, s);
    case 3:
      return pr(n, e, t, r, s);
  }
}
const Ke = (n, e, t = !1) => {
  if (typeof n == "object" && n !== null && (!Z(n, e) || t))
    throw new Error("Strict mode: Mutable data cannot be accessed directly, please use 'unsafe(callback)' wrap.");
}, ut = {
  get size() {
    return Q(O(this)).size;
  },
  has(n) {
    return Q(O(this)).has(n);
  },
  set(n, e) {
    const t = O(this), r = Q(t);
    return (!r.has(n) || !ie(r.get(n), e)) && (F(t), te(t), t.assignedMap.set(n, !0), t.copy.set(n, e), Et(t, n, e, Ce)), this;
  },
  delete(n) {
    if (!this.has(n))
      return !1;
    const e = O(this);
    return F(e), te(e), e.original.has(n) ? e.assignedMap.set(n, !1) : e.assignedMap.delete(n), e.copy.delete(n), !0;
  },
  clear() {
    const n = O(this);
    if (this.size) {
      F(n), te(n), n.assignedMap = /* @__PURE__ */ new Map();
      for (const [e] of n.original)
        n.assignedMap.set(e, !1);
      n.copy.clear();
    }
  },
  forEach(n, e) {
    const t = O(this);
    Q(t).forEach((r, s) => {
      n.call(e, this.get(s), s, this);
    });
  },
  get(n) {
    var e, t;
    const r = O(this), s = Q(r).get(n), i = ((t = (e = r.options).mark) === null || t === void 0 ? void 0 : t.call(e, s, B)) === B.mutable;
    if (r.options.strict && Ke(s, r.options, i), i || r.finalized || !Z(s, r.options) || s !== r.original.get(n))
      return s;
    const o = vt.createDraft({
      original: s,
      parentDraft: r,
      key: n,
      finalities: r.finalities,
      options: r.options
    });
    return F(r), r.copy.set(n, o), o;
  },
  keys() {
    return Q(O(this)).keys();
  },
  values() {
    const n = this.keys();
    return {
      [Le]: () => this.values(),
      next: () => {
        const e = n.next();
        return e.done ? e : {
          done: !1,
          value: this.get(e.value)
        };
      }
    };
  },
  entries() {
    const n = this.keys();
    return {
      [Le]: () => this.entries(),
      next: () => {
        const e = n.next();
        if (e.done)
          return e;
        const t = this.get(e.value);
        return {
          done: !1,
          value: [e.value, t]
        };
      }
    };
  },
  [Le]() {
    return this.entries();
  }
}, yr = Reflect.ownKeys(ut), Ft = (n, e, { isValuesIterator: t }) => () => {
  var r, s;
  const i = e.next();
  if (i.done)
    return i;
  const o = i.value;
  let a = n.setMap.get(o);
  const c = O(a), u = ((s = (r = n.options).mark) === null || s === void 0 ? void 0 : s.call(r, a, B)) === B.mutable;
  if (n.options.strict && Ke(o, n.options, u), !u && !c && Z(o, n.options) && !n.finalized && n.original.has(o)) {
    const l = vt.createDraft({
      original: o,
      parentDraft: n,
      key: o,
      finalities: n.finalities,
      options: n.options
    });
    n.setMap.set(o, l), a = l;
  } else c && (a = c.proxy);
  return {
    done: !1,
    value: t ? a : [a, a]
  };
}, ze = {
  get size() {
    return O(this).setMap.size;
  },
  has(n) {
    const e = O(this);
    if (e.setMap.has(n))
      return !0;
    F(e);
    const t = O(n);
    return !!(t && e.setMap.has(t.original));
  },
  add(n) {
    const e = O(this);
    return this.has(n) || (F(e), te(e), e.assignedMap.set(n, !0), e.setMap.set(n, n), Et(e, n, n, Ce)), this;
  },
  delete(n) {
    if (!this.has(n))
      return !1;
    const e = O(this);
    F(e), te(e);
    const t = O(n);
    return t && e.setMap.has(t.original) ? (e.assignedMap.set(t.original, !1), e.setMap.delete(t.original)) : (!t && e.setMap.has(n) ? e.assignedMap.set(n, !1) : e.assignedMap.delete(n), e.setMap.delete(n));
  },
  clear() {
    if (!this.size)
      return;
    const n = O(this);
    F(n), te(n);
    for (const e of n.original)
      n.assignedMap.set(e, !1);
    n.setMap.clear();
  },
  values() {
    const n = O(this);
    F(n);
    const e = n.setMap.keys();
    return {
      [Symbol.iterator]: () => this.values(),
      next: Ft(n, e, { isValuesIterator: !0 })
    };
  },
  entries() {
    const n = O(this);
    F(n);
    const e = n.setMap.keys();
    return {
      [Symbol.iterator]: () => this.entries(),
      next: Ft(n, e, {
        isValuesIterator: !1
      })
    };
  },
  keys() {
    return this.values();
  },
  [Le]() {
    return this.values();
  },
  forEach(n, e) {
    const t = this.values();
    let r = t.next();
    for (; !r.done; )
      n.call(e, r.value, r.value, this), r = t.next();
  }
};
Set.prototype.difference && Object.assign(ze, {
  intersection(n) {
    return Set.prototype.intersection.call(new Set(this.values()), n);
  },
  union(n) {
    return Set.prototype.union.call(new Set(this.values()), n);
  },
  difference(n) {
    return Set.prototype.difference.call(new Set(this.values()), n);
  },
  symmetricDifference(n) {
    return Set.prototype.symmetricDifference.call(new Set(this.values()), n);
  },
  isSubsetOf(n) {
    return Set.prototype.isSubsetOf.call(new Set(this.values()), n);
  },
  isSupersetOf(n) {
    return Set.prototype.isSupersetOf.call(new Set(this.values()), n);
  },
  isDisjointFrom(n) {
    return Set.prototype.isDisjointFrom.call(new Set(this.values()), n);
  }
});
const br = Reflect.ownKeys(ze), wn = /* @__PURE__ */ new WeakSet(), Tn = {
  get(n, e, t) {
    var r, s;
    const i = (r = n.copy) === null || r === void 0 ? void 0 : r[e];
    if (i && wn.has(i))
      return i;
    if (e === bn)
      return n;
    let o;
    if (n.options.mark) {
      const u = e === "size" && (n.original instanceof Map || n.original instanceof Set) ? Reflect.get(n.original, e) : Reflect.get(n.original, e, t);
      if (o = n.options.mark(u, B), o === B.mutable)
        return n.options.strict && Ke(u, n.options, !0), u;
    }
    const a = Q(n);
    if (a instanceof Map && yr.includes(e)) {
      if (e === "size")
        return Object.getOwnPropertyDescriptor(ut, "size").get.call(n.proxy);
      const u = ut[e];
      if (u)
        return u.bind(n.proxy);
    }
    if (a instanceof Set && br.includes(e)) {
      if (e === "size")
        return Object.getOwnPropertyDescriptor(ze, "size").get.call(n.proxy);
      const u = ze[e];
      if (u)
        return u.bind(n.proxy);
    }
    if (!ke(a, e)) {
      const u = Ut(a, e);
      return u ? "value" in u ? u.value : (
        // !case: support for getter
        (s = u.get) === null || s === void 0 ? void 0 : s.call(n.proxy)
      ) : void 0;
    }
    const c = a[e];
    if (n.options.strict && Ke(c, n.options), n.finalized || !Z(c, n.options))
      return c;
    if (c === Xe(n.original, e)) {
      if (F(n), n.copy[e] = Mt({
        original: n.original[e],
        parentDraft: n,
        key: n.type === 1 ? Number(e) : e,
        finalities: n.finalities,
        options: n.options
      }), typeof o == "function") {
        const u = O(n.copy[e]);
        return F(u), te(u), u.copy;
      }
      return n.copy[e];
    }
    return c;
  },
  set(n, e, t) {
    var r;
    if (n.type === 3 || n.type === 2)
      throw new Error("Map/Set draft does not support any property assignment.");
    let s;
    if (n.type === 1 && e !== "length" && !(Number.isInteger(s = Number(e)) && s >= 0 && (e === 0 || s === 0 || String(s) === String(e))))
      throw new Error("Only supports setting array indices and the 'length' property.");
    const i = Ut(Q(n), e);
    if (i != null && i.set)
      return i.set.call(n.proxy, t), !0;
    const o = Xe(Q(n), e), a = O(o);
    return a && ie(a.original, t) ? (n.copy[e] = t, n.assignedMap = (r = n.assignedMap) !== null && r !== void 0 ? r : /* @__PURE__ */ new Map(), n.assignedMap.set(e, !1), !0) : (ie(t, o) && (t !== void 0 || ke(n.original, e)) || (F(n), te(n), ke(n.original, e) && ie(t, n.original[e]) ? n.assignedMap.delete(e) : n.assignedMap.set(e, !0), n.copy[e] = t, Et(n, e, t, Ce)), !0);
  },
  has(n, e) {
    return e in Q(n);
  },
  ownKeys(n) {
    return Reflect.ownKeys(Q(n));
  },
  getOwnPropertyDescriptor(n, e) {
    const t = Q(n), r = Reflect.getOwnPropertyDescriptor(t, e);
    return r && {
      writable: !0,
      configurable: n.type !== 1 || e !== "length",
      enumerable: r.enumerable,
      value: t[e]
    };
  },
  getPrototypeOf(n) {
    return Reflect.getPrototypeOf(n.original);
  },
  setPrototypeOf() {
    throw new Error("Cannot call 'setPrototypeOf()' on drafts");
  },
  defineProperty() {
    throw new Error("Cannot call 'defineProperty()' on drafts");
  },
  deleteProperty(n, e) {
    var t;
    return n.type === 1 ? Tn.set.call(this, n, e, void 0, n.proxy) : (Xe(n.original, e) !== void 0 || e in n.original ? (F(n), te(n), n.assignedMap.set(e, !1)) : (n.assignedMap = (t = n.assignedMap) !== null && t !== void 0 ? t : /* @__PURE__ */ new Map(), n.assignedMap.delete(e)), n.copy && delete n.copy[e], !0);
  }
};
function Mt(n) {
  const { original: e, parentDraft: t, key: r, finalities: s, options: i } = n, o = de(e), a = {
    type: o,
    finalized: !1,
    parent: t,
    original: e,
    copy: null,
    proxy: null,
    finalities: s,
    options: i,
    // Mapping of draft Set items to their corresponding draft values.
    setMap: o === 3 ? new Map(e.entries()) : void 0
  };
  (r || "key" in n) && (a.key = r);
  const { proxy: c, revoke: u } = Proxy.revocable(o === 1 ? Object.assign([], a) : a, Tn);
  if (s.revoke.push(u), wn.add(c), a.proxy = c, t) {
    const l = t;
    l.finalities.draft.push((f, d) => {
      var p, y;
      const m = O(c);
      let _ = l.type === 3 ? l.setMap : l.copy;
      const w = oe(_, r), b = O(w);
      if (b) {
        let g = b.original;
        b.operated && (g = It(w)), at(b), ct(b, Ce, f, d), l.options.enableAutoFreeze && (l.options.updatedValues = (p = l.options.updatedValues) !== null && p !== void 0 ? p : /* @__PURE__ */ new WeakMap(), l.options.updatedValues.set(g, b.original)), Ee(_, r, g);
      }
      (y = m.callbacks) === null || y === void 0 || y.forEach((g) => {
        g(f, d);
      });
    });
  } else {
    const l = O(c);
    l.finalities.draft.push((f, d) => {
      at(l), ct(l, Ce, f, d);
    });
  }
  return c;
}
vt.createDraft = Mt;
function mr(n, e, t, r, s) {
  var i;
  const o = O(n), a = (i = o == null ? void 0 : o.original) !== null && i !== void 0 ? i : n, c = !!e.length;
  if (o != null && o.operated)
    for (; o.finalities.draft.length > 0; )
      o.finalities.draft.pop()(t, r);
  const u = c ? e[0] : o ? o.operated ? o.copy : o.original : n;
  return o && ot(o), s && pe(u, u, o == null ? void 0 : o.options.updatedValues), [
    u,
    t && c ? [{ op: j.Replace, path: [], value: e[0] }] : t,
    r && c ? [{ op: j.Replace, path: [], value: a }] : r
  ];
}
function gr(n, e) {
  var t;
  const r = {
    draft: [],
    revoke: [],
    handledSet: /* @__PURE__ */ new WeakSet()
  };
  let s, i;
  e.enablePatches && (s = [], i = []);
  const a = ((t = e.mark) === null || t === void 0 ? void 0 : t.call(e, n, B)) === B.mutable || !Z(n, e) ? n : Mt({
    original: n,
    parentDraft: null,
    finalities: r,
    options: e
  });
  return [
    a,
    (c = []) => {
      const [u, l, f] = mr(a, c, s, i, e.enableAutoFreeze);
      return e.enablePatches ? [u, l, f] : u;
    }
  ];
}
function lt(n) {
  const { rootDraft: e, value: t, useRawReturn: r = !1, isRoot: s = !0 } = n;
  Ct(t, (i, o, a) => {
    const c = O(o);
    if (c && e && c.finalities === e.finalities) {
      n.isContainDraft = !0;
      const u = c.original;
      if (a instanceof Set) {
        const l = Array.from(a);
        a.clear(), l.forEach((f) => a.add(i === f ? u : f));
      } else
        Ee(a, i, u);
    } else typeof o == "object" && o !== null && (n.value = o, n.isRoot = !1, lt(n));
  }), s && (n.isContainDraft || console.warn("The return value does not contain any draft, please use 'rawReturn()' to wrap the return value to improve performance."), r && console.warn("The return value contains drafts, please don't use 'rawReturn()' to wrap the return value."));
}
function Sn(n) {
  var e;
  const t = O(n);
  if (!Z(n, t == null ? void 0 : t.options))
    return n;
  const r = de(n);
  if (t && !t.operated)
    return t.original;
  let s;
  function i() {
    s = r === 2 ? Ot(n) ? new Map(n) : new (Object.getPrototypeOf(n)).constructor(n) : r === 3 ? Array.from(t.setMap.values()) : gn(n, t == null ? void 0 : t.options);
  }
  if (t) {
    t.finalized = !0;
    try {
      i();
    } finally {
      t.finalized = !1;
    }
  } else
    s = n;
  if (Ct(s, (o, a) => {
    if (t && ie(oe(t.original, o), a))
      return;
    const c = Sn(a);
    c !== a && (s === n && i(), Ee(s, o, c));
  }), r === 3) {
    const o = (e = t == null ? void 0 : t.original) !== null && e !== void 0 ? e : s;
    return kt(o) ? new Set(s) : new (Object.getPrototypeOf(o)).constructor(s);
  }
  return s;
}
function Nt(n) {
  if (!fe(n))
    throw new Error(`current() is only used for Draft, parameter: ${n}`);
  return Sn(n);
}
const _r = (n) => function e(t, r, s) {
  var i, o, a;
  if (typeof t == "function" && typeof r != "function")
    return function(T, ...S) {
      return e(T, (v) => t.call(this, v, ...S), r);
    };
  const c = t, u = r;
  let l = s;
  if (typeof r != "function" && (l = r), l !== void 0 && Object.prototype.toString.call(l) !== "[object Object]")
    throw new Error(`Invalid options: ${l}, 'options' should be an object.`);
  l = Object.assign(Object.assign({}, n), l);
  const f = fe(c) ? Nt(c) : c, d = Array.isArray(l.mark) ? (T, S) => {
    for (const v of l.mark) {
      if (typeof v != "function")
        throw new Error(`Invalid mark: ${v}, 'mark' should be a function.`);
      const E = v(T, S);
      if (E)
        return E;
    }
  } : l.mark, p = (i = l.enablePatches) !== null && i !== void 0 ? i : !1, y = (o = l.strict) !== null && o !== void 0 ? o : !1, _ = {
    enableAutoFreeze: (a = l.enableAutoFreeze) !== null && a !== void 0 ? a : !1,
    mark: d,
    strict: y,
    enablePatches: p
  };
  if (!Z(f, _) && typeof f == "object" && f !== null)
    throw new Error("Invalid base state: create() only supports plain objects, arrays, Set, Map or using mark() to mark the state as immutable.");
  const [w, b] = gr(f, _);
  if (typeof r != "function") {
    if (!Z(f, _))
      throw new Error("Invalid base state: create() only supports plain objects, arrays, Set, Map or using mark() to mark the state as immutable.");
    return [w, b];
  }
  let g;
  try {
    g = u(w);
  } catch (T) {
    throw ot(O(w)), T;
  }
  const k = (T) => {
    const S = O(w);
    if (!fe(T)) {
      if (T !== void 0 && !ie(T, w) && (S != null && S.operated))
        throw new Error("Either the value is returned as a new non-draft value, or only the draft is modified without returning any value.");
      const E = T == null ? void 0 : T[ar];
      if (E) {
        const D = E[0];
        return _.strict && typeof T == "object" && T !== null && lt({
          rootDraft: S,
          value: T,
          useRawReturn: !0
        }), b([D]);
      }
      if (T !== void 0)
        return typeof T == "object" && T !== null && lt({ rootDraft: S, value: T }), b([T]);
    }
    if (T === w || T === void 0)
      return b([]);
    const v = O(T);
    if (_ === v.options) {
      if (v.operated)
        throw new Error("Cannot return a modified child draft.");
      return b([Nt(T)]);
    }
    return b([T]);
  };
  return g instanceof Promise ? g.then(k, (T) => {
    throw ot(O(w)), T;
  }) : k(g);
}, We = _r();
Object.prototype.constructor.toString();
function An(n, e) {
  const t = Object.keys(n), r = Object.keys(e);
  return t.length === r.length && Object.keys(n).every((s) => e.hasOwnProperty(s));
}
function qt(n, e) {
  return Object.keys(n).length === Object.keys(e).length && Object.keys(n).every(
    (t) => e.hasOwnProperty(t) && n[t] === e[t]
  );
}
function Ne(n, e) {
  return typeof n != "object" || typeof e != "object" || n === null || e === null ? n === e : An(n, e) ? Object.keys(n).every(
    (t) => Ne(n[t], e[t])
  ) : !1;
}
function Pt(n) {
  if (!Ae(n))
    return n;
  const e = {};
  for (const [t, r] of Object.entries(n))
    r !== void 0 && (e[t] = r);
  return e;
}
function vn(n, e) {
  if (!Ae(n) || !Ae(e))
    return e;
  const t = { ...n };
  for (const r of Object.keys(e)) {
    if (e[r] === void 0) continue;
    if (e[r] === null) {
      delete t[r];
      continue;
    }
    const s = Ae(n[r]) && Ae(e[r]);
    t[r] = s ? vn(n[r], e[r]) : e[r];
  }
  return t;
}
function Ae(n) {
  return typeof n == "object" && n !== null && !Array.isArray(n);
}
function wr(n, e, t) {
  if (!n || e.length === 0)
    return;
  let r = n || {};
  for (let i = 0; i < e.length - 1; i++) {
    const o = e[i];
    (!(o in r) || typeof r[o] != "object") && (r[o] = typeof e[i + 1] == "number" ? [] : {}), r = r[o];
  }
  const s = e[e.length - 1];
  Array.isArray(r) && typeof s == "number" ? r.splice(s, 0, t) : r[s] = t;
}
function Kt(n, e, t) {
  if (!n || e.length === 0)
    return;
  let r = n || {};
  for (let s = 0; s < e.length - 1; s++) {
    const i = e[s];
    (!(i in r) || typeof r[i] != "object") && (r[i] = typeof e[s + 1] == "number" ? [] : {}), r = r[i];
  }
  r[e[e.length - 1]] = t;
}
function kn(n, e) {
  if (!n || e.length === 0)
    return;
  const [t, ...r] = e;
  if (t in n) {
    if (r.length === 0) {
      Array.isArray(n) ? n.splice(t, 1) : delete n[t];
      return;
    }
    kn(n[t], r), Tr(n[t]) && delete n[t];
  }
}
function Tr(n) {
  return n && Object.keys(n).length === 0;
}
const zt = /ZULU|YEKT|YEKST|YAPT|YAKT|YAKST|XJT|WGT|WGST|WFT|WETDST|WET|WDT|WAT|WAST|WAKT|WADT|VUT|VOLT|VLAT|VLAST|VET|UZT|UZST|UYT|UYST|UTC|UT|ULAT|ULAST|UCT|TVT|TRUT|TOT|TMT|TKT|TJT|TFT|TAHT|SGT|SCT|SAST|SADT|RET|PYT|PYST|PWT|PST|PONT|PMST|PMDT|PKT|PKST|PHT|PGT|PETT|PETST|PET|PDT|OMST|OMSST|NZT|NZST|NZDT|NUT|NST|NPT|NOVT|NOVST|NFT|NDT|MYT|MVT|MUT|MUST|MST|MSK|MSD|MPT|MMT|MHT|MEZ|METDST|MET|MESZ|MEST|MDT|MAWT|MART|MAGT|MAGST|LKT|LINT|LIGT|LHST|LHDT|KST|KRAT|KRAST|KOST|KGT|KGST|KDT|JST|JAYT|IST|IRT|IRKT|IRKST|IOT|IDT|ICT|HST|HKT|GYT|GMT|GILT|GFT|GET|GEST|GAMT|GALT|FNT|FNST|FKT|FKST|FJT|FJST|FET|EST|EGT|EGST|EETDST|EET|EEST|EDT|EAT|EAST|EASST|DDUT|DAVT|CXT|CST|COT|CLT|CLST|CKT|CHUT|CHAST|CHADT|CETDST|CET|CEST|CDT|CCT|CAST|CADT|BTT|BST|BRT|BRST|BRA|BOT|BORT|BNT|BDT|BDST|AZT|AZST|AZOT|AZOST|AWST|AWSST|AST|ART|ARST|ANAT|ANAST|AMT|AMST|ALMT|ALMST|AKST|AKDT|AFT|AEST|AESST|AEDT|ADT|ACWST|ACT|ACST|ACSST|ACDT$/, Sr = {
  ZULU: 0,
  YEKT: 18e3,
  YEKST: 21600,
  YAPT: 36e3,
  YAKT: 32400,
  YAKST: 32400,
  XJT: 21600,
  WGT: -10800,
  WGST: -7200,
  WFT: 43200,
  WETDST: 3600,
  WET: 0,
  WDT: 32400,
  WAT: 3600,
  WAST: 25200,
  WAKT: 43200,
  WADT: 28800,
  VUT: 39600,
  VOLT: 10800,
  VLAT: 36e3,
  VLAST: 36e3,
  VET: -14400,
  UZT: 18e3,
  UZST: 21600,
  UYT: -10800,
  UYST: -7200,
  UTC: 0,
  UT: 0,
  ULAT: 28800,
  ULAST: 32400,
  UCT: 0,
  TVT: 43200,
  TRUT: 36e3,
  TOT: 46800,
  TMT: 18e3,
  TKT: 46800,
  TJT: 18e3,
  TFT: 18e3,
  TAHT: -36e3,
  SGT: 28800,
  SCT: 14400,
  SAST: 7200,
  SADT: 37800,
  RET: 14400,
  PYT: -14400,
  PYST: -10800,
  PWT: 32400,
  PST: -28800,
  PONT: 39600,
  PMST: -10800,
  PMDT: -7200,
  PKT: 18e3,
  PKST: 21600,
  PHT: 28800,
  PGT: 36e3,
  PETT: 43200,
  PETST: 43200,
  PET: -18e3,
  PDT: -25200,
  OMST: 21600,
  OMSST: 21600,
  NZT: 43200,
  NZST: 43200,
  NZDT: 46800,
  NUT: -39600,
  NST: -12600,
  NPT: 20700,
  NOVT: 25200,
  NOVST: 25200,
  NFT: -12600,
  NDT: -9e3,
  MYT: 28800,
  MVT: 18e3,
  MUT: 14400,
  MUST: 18e3,
  MST: -25200,
  MSK: 10800,
  MSD: 14400,
  MPT: 36e3,
  MMT: 23400,
  MHT: 43200,
  MEZ: 3600,
  METDST: 7200,
  MET: 3600,
  MESZ: 7200,
  MEST: 7200,
  MDT: -21600,
  MAWT: 18e3,
  MART: -34200,
  MAGT: 39600,
  MAGST: 39600,
  LKT: 19800,
  LINT: 50400,
  LIGT: 36e3,
  LHST: 37800,
  LHDT: 37800,
  KST: 32400,
  KRAT: 25200,
  KRAST: 25200,
  KOST: 39600,
  KGT: 21600,
  KGST: 21600,
  KDT: 36e3,
  JST: 32400,
  JAYT: 32400,
  IST: 7200,
  IRT: 12600,
  IRKT: 28800,
  IRKST: 28800,
  IOT: 21600,
  IDT: 10800,
  ICT: 25200,
  HST: -36e3,
  HKT: 28800,
  GYT: -14400,
  GMT: 0,
  GILT: 43200,
  GFT: -10800,
  GET: 14400,
  GEST: 14400,
  GAMT: -32400,
  GALT: -21600,
  FNT: -7200,
  FNST: -3600,
  FKT: -10800,
  FKST: -10800,
  FJT: 43200,
  FJST: 46800,
  FET: 10800,
  EST: -18e3,
  EGT: -3600,
  EGST: 0,
  EETDST: 10800,
  EET: 7200,
  EEST: 10800,
  EDT: -14400,
  EAT: 10800,
  EAST: -21600,
  EASST: -21600,
  DDUT: 36e3,
  DAVT: 25200,
  CXT: 25200,
  CST: -21600,
  COT: -18e3,
  CLT: -14400,
  CLST: -10800,
  CKT: -36e3,
  CHUT: 36e3,
  CHAST: 45900,
  CHADT: 49500,
  CETDST: 7200,
  CET: 3600,
  CEST: 7200,
  CDT: -18e3,
  CCT: 28800,
  CAST: 34200,
  CADT: 37800,
  BTT: 21600,
  BST: 3600,
  BRT: -10800,
  BRST: -7200,
  BRA: -10800,
  BOT: -14400,
  BORT: 28800,
  BNT: 28800,
  BDT: 21600,
  BDST: 7200,
  AZT: 14400,
  AZST: 14400,
  AZOT: -3600,
  AZOST: 0,
  AWST: 28800,
  AWSST: 32400,
  AST: -14400,
  ART: -10800,
  ARST: -10800,
  ANAT: 43200,
  ANAST: 43200,
  AMT: -14400,
  AMST: 14400,
  ALMT: 21600,
  ALMST: 25200,
  AKST: -32400,
  AKDT: -28800,
  AFT: 16200,
  AEST: 36e3,
  AESST: 39600,
  AEDT: 39600,
  ADT: -10800,
  ACWST: 31500,
  ACT: -18e3,
  ACST: 34200,
  ACSST: 37800,
  ACDT: 37800
};
function Ar(n) {
  return new Date(n);
}
function vr(n) {
  return /* @__PURE__ */ new Date(n + "Z");
}
const kr = /^(\d+)[\./-](\d+)[\./-](\d+)$/;
function Or(n) {
  const e = n.match(kr);
  if (!e)
    return null;
  const [t, r, s, i] = e;
  return r <= 0 || s <= 0 || i <= 0 ? null : r > 999 ? new Date(Date.UTC(r, s - 1, i, 0, 0, 0, 0)) : new Date(Date.UTC(i, r - 1, s, 0, 0, 0, 0));
}
function Ir(n) {
  const [e, t] = n.split(" ");
  return /* @__PURE__ */ new Date(e + "T" + t + "Z");
}
function Cr(n) {
  const [e, t] = n.split(" ");
  return /* @__PURE__ */ new Date(e + "T" + t + "Z");
}
function Er(n) {
  return new Date(n);
}
function Mr(n) {
  const e = /^(\w{3}) (\w{3}) (\d{2}) (\d{4})$/;
  if (!n.match(e))
    throw new Error(`Unable to parse \`${n}\` as a date.`);
  const r = /* @__PURE__ */ new Date(n + " UTC");
  return new Date(
    Date.UTC(
      r.getUTCFullYear(),
      r.getUTCMonth(),
      r.getUTCDate(),
      0,
      0,
      0,
      0
    )
  );
}
function Pr(n) {
  const e = /^(.+T.+)([+-])(\d{2})$/, t = n.match(e);
  if (t) {
    const [, r, s, i] = t, o = `${r}${s}${i}:00`;
    return new Date(o);
  }
  return null;
}
function jr(n) {
  const e = /^(\d+)-(\d{1,2})-(\d{1,2})([ T])(.+)$/, t = n.match(e);
  if (t) {
    const [, r, s, i, o, a] = t, c = s.padStart(2, "0"), u = i.padStart(2, "0"), l = `${r}-${c}-${u}T${a}`;
    return new Date(l);
  }
  return null;
}
function Dr(n) {
  const [e, t] = n.split(", "), [r, s, i] = e.split("/").map(Number), o = t.match(/(\d{1,2}):(\d{2}):(\d{2}) (AM|PM)/);
  if (!o)
    throw new Error(`Unable to parse time from: ${n}`);
  let [, a, c, u, l] = o;
  return a = Number(a), c = Number(c), u = Number(u), l === "PM" && a !== 12 ? a += 12 : l === "AM" && a === 12 && (a = 0), new Date(Date.UTC(i, r - 1, s, a, c, u));
}
function $r(n) {
  switch (n) {
    case "epoch":
      return /* @__PURE__ */ new Date(0);
    case "infinity":
    case "-infinity":
    case "today":
    case "tomorrow":
    case "yesterday":
      return null;
  }
}
function Rr(n) {
  const e = n.match(zt);
  if (!e)
    return null;
  const [t] = e, r = Sr[t], s = new Date(n.replace(zt, "Z"));
  return new Date(s.getTime() - r * 1e3);
}
const xr = [
  Or,
  Cr,
  Mr,
  Dr,
  Er,
  vr,
  Pr,
  Ir,
  Ar,
  $r,
  Rr,
  jr
];
function Ur(n, e) {
  try {
    const t = n(e);
    return t instanceof Date && !isNaN(t.getTime()) ? t : null;
  } catch {
    return null;
  }
}
function ft(n) {
  for (const e of xr) {
    const t = Ur(e, n);
    if (t)
      return t;
  }
  return null;
}
function Lr(n) {
  try {
    const e = JSON.parse(n);
    return typeof e == "string" ? ft(e) : null;
  } catch {
    return null;
  }
}
function Be(n) {
  if (n !== void 0) {
    if (n === null)
      return null;
    if (n instanceof Date)
      return n;
    if (typeof n == "string") {
      const e = ft(n) || Lr(n) || ft(n.trim());
      if (!e)
        throw new Error(`Unable to parse \`${n}\` as a date.`);
      return e;
    } else if (typeof n == "number")
      return new Date(n);
    throw new Error(
      `Invalid date value \`${n}\`. Expected a date, number, or string, got type ${typeof n}.`
    );
  }
}
class ce {
  constructor(e, t) {
    h(this, "attrs");
    h(this, "linkIndex");
    h(this, "_blobAttrs", null);
    h(this, "_primaryKeys", null);
    h(this, "_forwardIdents", null);
    h(this, "_revIdents", null);
    this.attrs = e, this.linkIndex = t;
  }
  resetAttrIndexes() {
    this._blobAttrs = null, this._primaryKeys = null, this._forwardIdents = null, this._revIdents = null;
  }
  addAttr(e) {
    this.attrs[e.id] = e, this.resetAttrIndexes();
  }
  deleteAttr(e) {
    delete this.attrs[e], this.resetAttrIndexes();
  }
  updateAttr(e) {
    const t = this.attrs[e.id];
    t && (this.attrs[e.id] = { ...t, ...e }, this.resetAttrIndexes());
  }
  getAttr(e) {
    return this.attrs[e];
  }
  get blobAttrs() {
    if (this._blobAttrs)
      return this._blobAttrs;
    this._blobAttrs = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs))
      if (Dt(e)) {
        const [t, r, s] = e["forward-identity"];
        K(this.blobAttrs, [r, s], e);
      }
    return this._blobAttrs;
  }
  get primaryKeys() {
    if (this._primaryKeys)
      return this._primaryKeys;
    this._primaryKeys = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs))
      if (e["primary?"]) {
        const [t, r] = e["forward-identity"];
        K(this._primaryKeys, [r], e);
      }
    return this._primaryKeys;
  }
  get forwardIdents() {
    if (this._forwardIdents)
      return this._forwardIdents;
    this._forwardIdents = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs)) {
      const t = e["forward-identity"], [r, s, i] = t;
      K(this._forwardIdents, [s, i], e);
    }
    return this._forwardIdents;
  }
  get revIdents() {
    if (this._revIdents)
      return this._revIdents;
    this._revIdents = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs)) {
      const t = e["reverse-identity"];
      if (t) {
        const [r, s, i] = t;
        K(this._revIdents, [s, i], e);
      }
    }
    return this._revIdents;
  }
  toJSON() {
    return { attrs: this.attrs, linkIndex: this.linkIndex };
  }
}
function Fr(n) {
  return n.cardinality === "one";
}
function jt(n) {
  return n["value-type"] === "ref";
}
function Dt(n) {
  return n["value-type"] === "blob";
}
function _e(n, e) {
  return e.reduce((t, r) => t && t.get(r), n);
}
function Y(n, e) {
  if (e.length === 0) throw new Error("path must have at least one element");
  if (e.length === 1) {
    n.delete(e[0]);
    return;
  }
  const [t, ...r] = e;
  n.has(t) && Y(n.get(t), r);
}
function K(n, e, t) {
  let r = n;
  const s = e.length - 1;
  for (let i = 0; i < s; i++) {
    const o = e[i];
    let a = r.get(o);
    a === void 0 && (a = /* @__PURE__ */ new Map(), r.set(o, a)), r = a;
  }
  s > -1 && r.set(e[s], t);
}
function On(n, e, t) {
  const r = /* @__PURE__ */ new Map(), s = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
  for (const o of e) {
    let [a, c, u] = o;
    const l = n.getAttr(c);
    if (!l) {
      console.warn("no such attr", c, a);
      continue;
    }
    l["checked-data-type"] === "date" && t && (u = Be(u), o[2] = u), jt(l) && K(i, [u, c, a], o), K(r, [a, c, u], o), K(s, [c, a, u], o);
  }
  return { eav: r, aev: s, vae: i };
}
function In(n) {
  return {
    triples: z(n.eav, 3),
    cardinalityInference: n.cardinalityInference,
    useDateObjects: n.useDateObjects,
    version: 1
  };
}
function Cn(n, e) {
  return ye(
    n,
    e.triples,
    e.cardinalityInference,
    e.useDateObjects
  );
}
function En(n, e) {
  if (n)
    return new ce(n.attrs, n.linkIndex);
  if (e && "__type" in e)
    return new ce(e.attrs, e.linkIndex);
}
function Nr(n, e) {
  return _e(n.eav, [e]) !== void 0;
}
function ye(n, e, t, r) {
  const s = On(
    n,
    e,
    r
  );
  return s.cardinalityInference = t, s.useDateObjects = r, s;
}
function Me(n, e) {
  var s, i;
  let t;
  if (Array.isArray(e[0])) {
    const [o, a] = e[0], c = n.aev.get(o);
    if (!c)
      return null;
    t = (s = z(c, 2).find((l) => l[2] === a)) == null ? void 0 : s[0];
  } else
    t = e[0];
  if (!t)
    return null;
  const r = e[2];
  if (Array.isArray(r) && r.length === 2 && n.aev.get(r[0])) {
    const [o, a] = r, c = n.aev.get(o);
    if (!c)
      return null;
    const l = (i = z(c, 2).find((m) => m[2] === a)) == null ? void 0 : i[0];
    if (!l)
      return null;
    const [f, d, p, ...y] = e;
    return [t, d, l, ...y];
  } else {
    const [o, ...a] = e;
    return [t, ...a];
  }
}
function Mn(n, e, t) {
  const r = Me(n, t);
  if (!r)
    return;
  const [s, i, o] = r, a = e.getAttr(i);
  a && (Y(n.eav, [s, i, o]), Y(n.aev, [i, s, o]), jt(a) && Y(n.vae, [o, i, s]));
}
let qr = 0;
function Pn(n, e, t) {
  const [r, s, i] = t;
  let o;
  const a = _e(n.eav, [r, s, i]);
  return a && (o = a[3]), o || Date.now() * 10 + qr++;
}
function jn(n, e, t) {
  const r = Me(n, t);
  if (!r)
    return;
  let [s, i, o] = r;
  const a = e.getAttr(i);
  if (!a)
    return;
  a["checked-data-type"] === "date" && n.useDateObjects && (o = Be(o));
  const c = _e(n.eav, [s, i, o]), u = (c == null ? void 0 : c[3]) ?? Pn(n, a, r), l = [s, i, o, u];
  Fr(a) ? (K(n.eav, [s, i], /* @__PURE__ */ new Map([[o, l]])), K(n.aev, [i, s], /* @__PURE__ */ new Map([[o, l]]))) : (K(n.eav, [s, i, o], l), K(n.aev, [i, s, o], l)), jt(a) && K(n.vae, [o, i, s], l);
}
function Kr(n, e, t) {
  var p;
  const r = Me(n, t);
  if (!r)
    return;
  const [s, i, o] = r, a = e.getAttr(i);
  if (!a) return;
  if (!Dt(a))
    throw new Error("merge operation is not supported for links");
  const c = _e(n.eav, [s, i]);
  if (!c) return;
  const u = (p = c.values().next()) == null ? void 0 : p.value;
  if (!u) return;
  const l = u[2], f = vn(l, o), d = [
    s,
    i,
    f,
    Pn(n, a, u)
  ];
  K(n.eav, [s, i], /* @__PURE__ */ new Map([[f, d]])), K(n.aev, [i, s], /* @__PURE__ */ new Map([[f, d]]));
}
function dt(n, e, t) {
  var u, l;
  const [r, s] = t, i = Me(n, [r]);
  if (!i)
    return;
  const [o] = i, a = n.eav.get(o);
  if (a) {
    for (const f of a.keys()) {
      const d = e.getAttr(f);
      d && d["on-delete-reverse"] === "cascade" && z(a.get(f), 1).forEach(
        ([p, y, m]) => {
          var _;
          return dt(n, e, [m, (_ = d["reverse-identity"]) == null ? void 0 : _[1]]);
        }
      ), // Fall back to deleting everything if we've rehydrated tx-steps from
      // the store that didn't set `etype` in deleteEntity
      (!s || // If we don't know about the attr, let's just get rid of it
      !d || // Make sure it matches the etype
      ((u = d["forward-identity"]) == null ? void 0 : u[1]) === s) && (Y(n.aev, [f, o]), Y(n.eav, [o, f]));
    }
    a.size === 0 && Y(n.eav, [o]);
  }
  const c = n.vae.get(o) && z(n.vae.get(o), 2);
  c && c.forEach((f) => {
    var _, w, b;
    const [d, p, y] = f, m = e.getAttr(p);
    (!s || !m || ((_ = m["reverse-identity"]) == null ? void 0 : _[1]) === s) && (Y(n.eav, [d, p, y]), Y(n.aev, [p, d, y]), Y(n.vae, [y, p, d])), m && m["on-delete"] === "cascade" && ((w = m["reverse-identity"]) == null ? void 0 : w[1]) === s && dt(n, e, [d, (b = m["forward-identity"]) == null ? void 0 : b[1]]);
  }), ((l = n.vae.get(o)) == null ? void 0 : l.size) === 0 && Y(n.vae, [o]);
}
function Dn(n, e, t) {
  const r = On(
    e,
    t,
    n.useDateObjects
  );
  Object.keys(r).forEach((s) => {
    n[s] = r[s];
  });
}
function zr(n, [e]) {
  n.addAttr(e);
}
function $n(n) {
  return z(n.eav, 3);
}
function Wr(n, e, [t]) {
  if (!e.getAttr(t)) return;
  const r = $n(n).filter(([s, i]) => i !== t);
  e.deleteAttr(t), Dn(n, e, r);
}
function Vr(n, e, [t]) {
  e.getAttr(t.id) && (e.updateAttr(t), Dn(n, e, $n(n)));
}
function Qr(n, e, t) {
  const [r, ...s] = t;
  switch (r) {
    case "add-triple":
      jn(n, e, s);
      break;
    case "deep-merge-triple":
      Kr(n, e, s);
      break;
    case "retract-triple":
      Mn(n, e, s);
      break;
    case "delete-entity":
      dt(n, e, s);
      break;
    case "add-attr":
      zr(e, s);
      break;
    case "delete-attr":
      Wr(n, e, s);
      break;
    case "update-attr":
      Vr(n, e, s);
      break;
    case "restore-attr":
      break;
    case "rule-params":
      break;
    default:
      throw new Error(`unhandled transaction action: ${r}`);
  }
}
function z(n, e, t = []) {
  if (!n || e === 0)
    return t;
  if (e === 1) {
    for (const r of n.values())
      t.push(r);
    return t;
  }
  for (const r of n.values())
    z(r, e - 1, t);
  return t;
}
function De(n, e, t) {
  var i;
  const r = [];
  if (t != null && t.hasOwnProperty("$not")) {
    for (const o of e.keys())
      t.$not !== o && r.push(e.get(o));
    return r;
  }
  if (t != null && t.hasOwnProperty("$isNull")) {
    const { attrId: o, isNull: a, reverse: c } = t.$isNull;
    if (c)
      for (const u of e.keys()) {
        const l = n.vae.get(u), f = !l || !l.get(o);
        (a ? f : !f) && r.push(e.get(u));
      }
    else {
      const u = n.aev.get(o);
      for (const l of e.keys()) {
        const f = !u || ((i = u.get(l)) == null ? void 0 : i.get(null)) || !u.get(l);
        (a ? f : !f) && r.push(e.get(l));
      }
    }
    return r;
  }
  if (t != null && t.$comparator)
    return z(e, 1).filter(t.$op);
  const s = t.in || t.$in || [t];
  for (const o of s) {
    const a = e.get(o);
    a && r.push(a);
  }
  return r;
}
function Br(n, e, t) {
  let r = "";
  return n !== void 0 && (r += "e"), e !== void 0 && (r += "a"), t !== void 0 && (r += "v"), r;
}
function Gr(n, [e, t, r]) {
  var i, o;
  switch (Br(e, t, r)) {
    case "e": {
      const a = n.eav.get(e);
      return z(a, 2);
    }
    case "ea": {
      const a = (i = n.eav.get(e)) == null ? void 0 : i.get(t);
      return z(a, 1);
    }
    case "eav": {
      const a = (o = n.eav.get(e)) == null ? void 0 : o.get(t);
      return a ? De(n, a, r) : [];
    }
    case "ev": {
      const a = n.eav.get(e);
      if (!a)
        return [];
      const c = [];
      for (const u of a.values())
        c.push(...De(n, u, r));
      return c;
    }
    case "a": {
      const a = n.aev.get(t);
      return z(a, 2);
    }
    case "av": {
      const a = n.aev.get(t);
      if (!a)
        return [];
      const c = [];
      for (const u of a.values())
        c.push(...De(n, u, r));
      return c;
    }
    case "v": {
      const a = [];
      for (const c of n.eav.values())
        for (const u of c.values())
          a.push(...De(n, u, r));
      return a;
    }
    default:
      return z(n.eav, 3);
  }
}
function Hr(n, e, t) {
  var s;
  const r = {};
  if (!e)
    return r;
  for (const [i, o] of e.entries()) {
    const a = (s = n.eav.get(t)) == null ? void 0 : s.get(o.id), c = z(a, 1);
    for (const u of c)
      r[i] = u[2];
  }
  return r;
}
function $(n, e, t) {
  var r;
  return (r = n.forwardIdents.get(e)) == null ? void 0 : r.get(t);
}
function he(n, e, t) {
  var r;
  return (r = n.revIdents.get(e)) == null ? void 0 : r.get(t);
}
function Jr(n, e) {
  return n.blobAttrs.get(e);
}
function Rn(n, e) {
  var r;
  const t = n.primaryKeys.get(e);
  return t || ((r = n.forwardIdents.get(e)) == null ? void 0 : r.get("id"));
}
function Yr(n, e, t) {
  const r = Me(n, t);
  if (!r)
    return;
  const [s, i, o] = r;
  if (e.getAttr(i))
    return _e(n.eav, [s, i]);
}
function Zr(n, e, t) {
  const r = t.filter(
    ([s, i, o, a, c]) => {
      if (s !== "add-triple" && s !== "deep-merge-triple")
        return !0;
      const u = c == null ? void 0 : c.mode;
      if (u !== "create" && u !== "update")
        return !0;
      let l = !1;
      const f = e.getAttr(o);
      if (f) {
        const d = Rn(
          e,
          f["forward-identity"][1]
        );
        l = !!Yr(n, e, [
          i,
          d == null ? void 0 : d.id,
          i
        ]);
      }
      return !(u === "create" && l || u === "update" && !l);
    }
  );
  return We(
    { store: n, attrsStore: e },
    (s) => {
      r.forEach((i) => {
        Qr(s.store, s.attrsStore, i);
      });
    },
    {
      mark: (s) => {
        if (s instanceof ce)
          return "immutable";
      }
    }
  );
}
function Xr(n) {
  return typeof n == "string" && n.startsWith("?");
}
function es(n, e, t) {
  if (t.hasOwnProperty(n)) {
    const r = t[n];
    return xn(r, e, t);
  }
  return { ...t, [n]: e };
}
function Wt(n, e, t) {
  return n === e ? t : null;
}
function ts(n) {
  switch (typeof n) {
    case "string":
      return n.startsWith("?") ? es : Wt;
    default:
      return Wt;
  }
}
const ns = [
  "in",
  "$in",
  "$not",
  "$isNull",
  "$comparator"
  // covers all of $gt, $lt, etc.
];
function rs(n) {
  for (const e of ns)
    if (n.hasOwnProperty(e))
      return !0;
  return !1;
}
function xn(n, e, t) {
  return t ? typeof n == "object" ? rs(n) ? t : null : ts(n)(n, e, t) : null;
}
function ss(n, e, t) {
  return n.reduce((r, s, i) => {
    const o = e[i];
    return xn(s, o, r);
  }, t);
}
function is(n, e, t) {
  return cs(n, e, t).map((r) => ss(e, r, t)).filter((r) => r);
}
function os(n, e, t) {
  return e.or ? e.or.patterns.flatMap((r) => ht(n, r, t)) : e.and ? e.and.patterns.reduce((r, s) => ht(n, s, r), t) : t.flatMap((r) => is(n, e, r));
}
function ht(n, e, t = [{}]) {
  return e.reduce((r, s) => os(n, s, r), t);
}
function $t(n, e) {
  return Array.isArray(e) ? e.map((t) => $t(n, t)) : Xr(e) ? n[e] : e;
}
function as(n, { find: e, where: t }) {
  return ht(n, t).map((s) => $t(s, e));
}
function cs(n, e, t) {
  return Gr(n, $t(t, e));
}
const us = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/i;
function me(n) {
  return typeof n == "string" && us.test(n);
}
const x = [];
for (let n = 0; n < 256; ++n)
  x.push((n + 256).toString(16).slice(1));
function ls(n, e = 0) {
  return (x[n[e + 0]] + x[n[e + 1]] + x[n[e + 2]] + x[n[e + 3]] + "-" + x[n[e + 4]] + x[n[e + 5]] + "-" + x[n[e + 6]] + x[n[e + 7]] + "-" + x[n[e + 8]] + x[n[e + 9]] + "-" + x[n[e + 10]] + x[n[e + 11]] + x[n[e + 12]] + x[n[e + 13]] + x[n[e + 14]] + x[n[e + 15]]).toLowerCase();
}
let et;
const fs = new Uint8Array(16);
function ds() {
  if (!et) {
    if (typeof crypto > "u" || !crypto.getRandomValues)
      throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
    et = crypto.getRandomValues.bind(crypto);
  }
  return et(fs);
}
const hs = typeof crypto < "u" && crypto.randomUUID && crypto.randomUUID.bind(crypto), Vt = { randomUUID: hs };
function ps(n, e, t) {
  var s;
  if (Vt.randomUUID && !n)
    return Vt.randomUUID();
  n = n || {};
  const r = n.random ?? ((s = n.rng) == null ? void 0 : s.call(n)) ?? ds();
  if (r.length < 16)
    throw new Error("Random bytes length must be >= 16");
  return r[6] = r[6] & 15 | 64, r[8] = r[8] & 63 | 128, ls(r);
}
function Qt(n) {
  const e = n.replace(/-/g, ""), t = [];
  for (let r = 0; r < e.length; r += 2)
    t.push(parseInt(e.substring(r, r + 2), 16));
  return t;
}
function ys(n, e) {
  for (let t = 0; t < n.length; t++) {
    if (n[t] < e[t]) return -1;
    if (n[t] > e[t]) return 1;
  }
  return 0;
}
function bs(n, e) {
  return ys(Qt(n), Qt(e));
}
function M() {
  return ps();
}
function ms(n, e) {
  return n.localeCompare(e);
}
function gs() {
  let n = ms;
  if (typeof Intl == "object" && Intl.hasOwnProperty("Collator"))
    try {
      n = Intl.Collator("en-US").compare;
    } catch {
    }
  return n;
}
const _s = gs();
let Un = !0;
const ra = (n) => {
  Un = n;
};
let ws = 0;
function Oe(n) {
  return Ge(`_${n}`, ws++);
}
function Ge(n, e) {
  return `?${n}-${e}`;
}
class ge extends Error {
  constructor(e) {
    super(e), this.name = "AttrNotFoundError";
  }
}
function Ts(n, e) {
  const t = Rn(n, e);
  if (!t)
    throw new ge(`Could not find id attr for ${e}`);
  return t;
}
function Bt(n, e, t, r) {
  return [Ss(n, e, t, r)];
}
function Ss(n, e, t, r) {
  return [
    n(t, r),
    Ts(e, t).id,
    n(t, r),
    n("time", r)
  ];
}
function As(n, e, t) {
  return n.map((r) => r === e ? t : r);
}
function Ln(n, e, t, r, s) {
  const i = $(e, t, s), o = he(e, t, s), a = i || o;
  if (!a)
    throw new ge(`Could not find attr for ${[t, s]}`);
  if (a["value-type"] !== "ref")
    throw new Error(`Attr ${a.id} is not a ref`);
  const [c, u] = a["forward-identity"], [l, f] = a["reverse-identity"], d = r + 1, p = i ? [
    n(u, r),
    a.id,
    n(f, d),
    Oe("time")
  ] : [
    n(u, d),
    a.id,
    n(f, r),
    Oe("time")
  ];
  return [i ? f : u, d, p, a, !!i];
}
function Gt(n, e) {
  if (typeof e != "string")
    return function(o) {
      return !1;
    };
  const r = e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&").replace(/%/g, ".*").replace(/_/g, "."), s = new RegExp(
    `^${r}$`,
    n ? void 0 : "i"
  );
  return function(o) {
    return typeof o != "string" ? !1 : s.test(o);
  };
}
function vs(n, e) {
  if (typeof e != "object" || e.hasOwnProperty("$in") || e.hasOwnProperty("in"))
    return e;
  const t = n["checked-data-type"] === "date";
  if (e.hasOwnProperty("$gt"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) > new Date(e.$gt);
      } : function(s) {
        return s[2] > e.$gt;
      }
    };
  if (e.hasOwnProperty("$gte"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) >= new Date(e.$gte);
      } : function(s) {
        return s[2] >= e.$gte;
      }
    };
  if (e.hasOwnProperty("$lt"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) < new Date(e.$lt);
      } : function(s) {
        return s[2] < e.$lt;
      }
    };
  if (e.hasOwnProperty("$lte"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) <= new Date(e.$lte);
      } : function(s) {
        return s[2] <= e.$lte;
      }
    };
  if (e.hasOwnProperty("$like")) {
    const r = Gt(!0, e.$like);
    return {
      $comparator: !0,
      $op: function(i) {
        return r(i[2]);
      }
    };
  }
  if (e.hasOwnProperty("$ilike")) {
    const r = Gt(!1, e.$ilike);
    return {
      $comparator: !0,
      $op: function(i) {
        return r(i[2]);
      }
    };
  }
  return e;
}
function ks(n, e, t, r, s, i) {
  const o = $(e, t, s), a = he(
    e,
    t,
    s
  ), c = o || a;
  if (!c)
    throw new ge(
      `No attr for etype = ${t} label = ${s}`
    );
  if (i != null && i.hasOwnProperty("$isNull")) {
    const u = $(e, t, "id");
    if (!u)
      throw new ge(
        `No attr for etype = ${t} label = id`
      );
    return [
      n(t, r),
      u.id,
      { $isNull: { attrId: c.id, isNull: i.$isNull, reverse: !o } },
      Oe("time")
    ];
  }
  return o ? [
    n(t, r),
    c.id,
    vs(c, i),
    Oe("time")
  ] : [i, c.id, n(t, r), Oe("time")];
}
function Os(n, e, t, r, s) {
  const [i, o, a] = s.reduce(
    (c, u) => {
      const [l, f, d] = c, [p, y, m] = Ln(
        n,
        e,
        l,
        f,
        u
      );
      return [p, y, [...d, m]];
    },
    [t, r, []]
  );
  return [i, o, a];
}
function pt(n, e, t, r, s, i) {
  const o = s.slice(0, s.length - 1), a = s[s.length - 1], [c, u, l] = Os(
    n,
    e,
    t,
    r,
    o
  ), f = ks(
    n,
    e,
    c,
    u,
    a,
    i
  );
  return l.concat([f]);
}
function Is(n, e) {
  return e ? [e].concat(n) : n;
}
function Cs([n, e]) {
  return n === "or" && Array.isArray(e);
}
function Es([n, e]) {
  return n === "and" && Array.isArray(e);
}
function Ms(n, e, t) {
  return (r, s) => {
    const i = n(r, s);
    return e == i ? i : `${i}-${t}`;
  };
}
function Ht(n, e, t, r, s, i) {
  const o = n(r, s), a = i.map((c, u) => {
    const l = Ms(n, o, u);
    return Fn(l, t, r, s, c);
  });
  return { [e]: { patterns: a, joinSym: o } };
}
function Ps(n) {
  const e = [];
  for (let t = 1; t <= n.length; t++)
    e.push(n.slice(0, t));
  return e;
}
function Jt(n, e, t, r, s) {
  return Ps(s).map(
    (i) => pt(n, e, t, r, i, {
      $isNull: !0
    })
  );
}
function Fn(n, e, t, r, s) {
  return Object.entries(s).flatMap(([i, o]) => {
    if (Cs([i, o]))
      return Ht(n, "or", e, t, r, o);
    if (Es([i, o]))
      return Ht(n, "and", e, t, r, o);
    if (i === "$entityIdStartsWith")
      return [];
    const a = i.split(".");
    if (o != null && o.hasOwnProperty("$ne") && (o = { ...o, $not: o.$ne }, delete o.$ne), o != null && o.hasOwnProperty("$not")) {
      const c = pt(
        n,
        e,
        t,
        r,
        a,
        o
      ), u = Jt(
        n,
        e,
        t,
        r,
        a
      );
      return [
        {
          or: {
            patterns: [c, ...u],
            joinSym: n(t, r)
          }
        }
      ];
    }
    return o != null && o.hasOwnProperty("$isNull") && o.$isNull === !0 && a.length > 1 ? [
      {
        or: {
          patterns: Jt(
            n,
            e,
            t,
            r,
            a
          ),
          joinSym: n(t, r)
        }
      }
    ] : pt(n, e, t, r, a, o);
  });
}
function js(n, e, t, r) {
  const s = Ge;
  return r ? Fn(s, n, e, t, r).concat(Bt(s, n, e, t)) : Bt(s, n, e, t);
}
function Ds(n, e, t) {
  return [n(e, t), n("time", t)];
}
function $s(n, e, t, r, s, i) {
  const [o, a, c, u, l] = Ln(
    n,
    e,
    t,
    r,
    s
  ), f = As(c, n(t, r), i);
  return [o, a, f, u, l];
}
function Rs(n, e, t, { etype: r, level: s, form: i }, o) {
  const a = Object.keys(i).filter((c) => c !== "$");
  return a.length ? Object.entries(o).map(function([u, l]) {
    return a.map(function(p) {
      var m, _, w;
      const y = !!(e.cardinalityInference && ((w = (_ = (m = t.linkIndex) == null ? void 0 : m[r]) == null ? void 0 : _[p]) != null && w.isSingular));
      try {
        const [b, g, k] = $s(
          n,
          t,
          r,
          s,
          p,
          u
        ), T = qn(e, t, {
          etype: b,
          level: g,
          form: i[p],
          join: k
        }), S = y ? T[0] : T;
        return { [p]: S };
      } catch (b) {
        if (b instanceof ge)
          return { [p]: y ? void 0 : [] };
        throw b;
      }
    }).reduce(function(p, y) {
      return { ...p, ...y };
    }, l);
  }) : Object.values(o);
}
function xs(n, e, t) {
  return t === "string" ? _s(n, e) : n > e ? 1 : -1;
}
function ve(n, e, t, r, s) {
  return e === r || e == null && r == null ? bs(n, t) : r == null ? 1 : e == null ? -1 : xs(e, r, s);
}
function Ve([n, e], [t, r], s) {
  return ve(n, e, t, r, s);
}
function yt(n) {
  return n == null ? n : new Date(n).getTime();
}
function Us(n, e, t, r) {
  var y;
  const [s, i, o, a] = n, c = t === "desc" ? 1 : -1;
  if (((y = e["forward-identity"]) == null ? void 0 : y[2]) === "id")
    return Ve(r, [s, a], null) === c;
  const [u, l] = r, f = e["checked-data-type"], d = f === "date" ? yt(l) : l, p = f === "date" ? yt(o) : o;
  return Ve([u, d], [s, p], f) === c;
}
function Ls(n, e) {
  const t = e[1];
  return n.getAttr(t);
}
function Fs(n, e, t) {
  const r = Object.keys(t)[0];
  return $(n, e, r);
}
function Ns(n, e, t, r) {
  if (t)
    return Ls(n, t);
  if (r)
    return Fs(n, e, r);
}
function qs(n, e, t) {
  var s, i;
  if (!Array.isArray(t.fields))
    return Jr(n, e);
  const r = /* @__PURE__ */ new Map();
  for (const o of t.fields) {
    const a = $(n, e, o), c = (s = a == null ? void 0 : a["forward-identity"]) == null ? void 0 : s[2];
    c && Dt(a) && r.set(c, a);
  }
  if (!r.has("id")) {
    const o = $(n, e, "id"), a = (i = o == null ? void 0 : o["forward-identity"]) == null ? void 0 : i[2];
    a && r.set(a, o);
  }
  return r;
}
function Ks(n, e, { etype: t, pageInfo: r, dq: s, form: i }) {
  var y, m;
  const o = (y = i == null ? void 0 : i.$) == null ? void 0 : y.order, a = Nn(i), c = zs(i);
  let u = as(n, s);
  const l = r == null ? void 0 : r["start-cursor"], f = Ns(e, t, l, o);
  if (f && ((m = f == null ? void 0 : f["forward-identity"]) == null ? void 0 : m[2]) !== "id") {
    const _ = f["checked-data-type"] === "date", w = f.id;
    u = u.map(([b]) => {
      var k, T, S, v, E;
      let g = (E = (v = (S = (T = (k = n.eav.get(b)) == null ? void 0 : k.get(w)) == null ? void 0 : T.values()) == null ? void 0 : S.next()) == null ? void 0 : v.value) == null ? void 0 : E[2];
      return _ && (g = yt(g)), [b, g];
    });
  }
  u.sort(
    c === "asc" ? function(w, b) {
      return Ve(w, b, f == null ? void 0 : f["checked-data-type"]);
    } : function(w, b) {
      return Ve(b, w, f == null ? void 0 : f["checked-data-type"]);
    }
  );
  let d = {};
  const p = qs(e, t, s);
  for (const _ of u) {
    const [w] = _;
    if (d[w] || !a && l && f && Us(l, f, c, _))
      continue;
    const b = Hr(n, p, w);
    b && (d[w] = b);
  }
  return d;
}
function zs(n) {
  var t;
  const e = (t = n.$) == null ? void 0 : t.order;
  return e && e[Object.keys(e)[0]] || "asc";
}
function Nn(n) {
  var s, i, o;
  const e = (s = n.$) == null ? void 0 : s.offset, t = (i = n.$) == null ? void 0 : i.before, r = (o = n.$) == null ? void 0 : o.after;
  return !e && !t && !r;
}
function Ws(n, e, { etype: t, level: r, form: s, join: i, pageInfo: o }) {
  var d, p, y, m, _;
  if (!Nn(s) && (!o || !o["start-cursor"]))
    return [];
  const a = Is(
    js(e, t, r, (d = s.$) == null ? void 0 : d.where),
    i
  ), c = Ds(Ge, t, r), u = (p = s.$) == null ? void 0 : p.fields, l = Ks(n, e, {
    etype: t,
    pageInfo: o,
    form: s,
    dq: { where: a, find: c, fields: u }
  }), f = ((y = s.$) == null ? void 0 : y.limit) || ((m = s.$) == null ? void 0 : m.first) || ((_ = s.$) == null ? void 0 : _.last);
  if (f != null) {
    r > 0 && Un && console.warn(
      "WARNING: Limits in child queries are only run client-side. Data returned from the server will not have a limit."
    );
    const w = Object.entries(l);
    return w.length <= f ? l : Object.fromEntries(w.slice(0, f));
  }
  return l;
}
function Vs(n, e, t) {
  try {
    return Ws(n, e, t);
  } catch (r) {
    if (r instanceof ge)
      return {};
    throw r;
  }
}
function qn(n, e, t) {
  const r = Vs(n, e, t);
  return Rs(Ge, n, e, t, r);
}
function Qs(n) {
  const e = {};
  for (const [t, r] of Object.entries(n))
    e[t] = {
      startCursor: r["start-cursor"],
      endCursor: r["end-cursor"],
      hasNextPage: r["has-next-page?"],
      hasPreviousPage: r["has-previous-page?"]
    };
  return e;
}
function Rt({
  store: n,
  attrsStore: e,
  pageInfo: t,
  aggregate: r
}, s) {
  const o = { data: Object.keys(s).reduce(function(c, u) {
    return r != null && r[u] || u === "$$ruleParams" || (c[u] = qn(n, e, {
      etype: u,
      form: s[u],
      level: 0,
      pageInfo: t == null ? void 0 : t[u]
    })), c;
  }, {}) };
  return t && (o.pageInfo = Qs(t)), r && (o.aggregate = r), o;
}
function Bs() {
  const e = {
    __etype: 1,
    __ops: 1,
    create: 1,
    update: 1,
    link: 1,
    unlink: 1,
    delete: 1,
    merge: 1,
    ruleParams: 1
  };
  return new Set(Object.keys(e));
}
const Gs = Bs();
function qe(n, e, t) {
  const r = {
    __etype: n,
    __ops: t
  };
  return new Proxy(r, {
    get: (s, i) => {
      if (i === "__ops") return t;
      if (i === "__etype") return n;
      if (Gs.has(i))
        return (o, a) => qe(n, e, [
          ...t,
          a ? [i, n, e, o, a] : [i, n, e, o]
        ]);
    }
  });
}
function Hs(n, e) {
  return `lookup__${n}__${JSON.stringify(e)}`;
}
function Qe(n) {
  return n.startsWith("lookup__");
}
function bt(n) {
  const [e, t, ...r] = n.split("__");
  return [t, JSON.parse(r.join("__"))];
}
function Js(n) {
  return new Proxy(
    {
      __etype: n
    },
    {
      get(e, t) {
        if (t === "lookup")
          return (s, i) => qe(n, bt(Hs(s, i)), []);
        if (t === "__etype") return n;
        const r = t;
        return Qe(r) ? qe(n, bt(r), []) : qe(n, r, []);
      }
    }
  );
}
function Kn() {
  return new Proxy(
    {},
    {
      get(n, e) {
        return Js(e);
      }
    }
  );
}
const sa = Kn();
function Ys(n) {
  return n.__ops;
}
function Zs(n, e) {
  const { attrIdMap: t, refSwapAttrIds: r } = n, s = [];
  for (const o of e) {
    const a = t[o];
    if (a)
      s.push(a);
    else if (Array.isArray(o) && o.length == 2 && t[o[0]]) {
      const [c, u] = o;
      s.push([t[c], u]);
    } else
      s.push(o);
  }
  const [i] = e;
  if ((i === "add-triple" || i === "retract-triple") && r.has(e[2])) {
    const o = s[1];
    s[1] = s[3], s[3] = o;
  }
  return s;
}
function Xs(n) {
  if (Array.isArray(n))
    return n;
  const e = Object.entries(n);
  if (e.length !== 1)
    throw new Error(
      "lookup must be an object with a single unique attr and value."
    );
  return e[0];
}
function ei(n, e, t) {
  return t.indexOf(".") !== -1 && // attr names can have `.` in them, so use the attr we find with a `.`
  // before assuming it's a ref lookup.
  !$(n, e, t);
}
function mt(n) {
  const [e, t, ...r] = n.split(".");
  if (r.length > 0 || t !== "id")
    throw new Error(`${n} is not a valid lookup attribute.`);
  return e;
}
function ti(n, e, t) {
  if (!ei(n, e, t))
    return $(n, e, t);
  const r = mt(t), s = $(n, e, r) || he(n, e, r);
  if (s && s["value-type"] !== "ref")
    throw new Error(`${t} does not reference a valid link attribute.`);
  return s;
}
function gt(n) {
  return typeof n == "string" && !Qe(n) ? null : typeof n == "string" && Qe(n) ? bt(n) : Xs(n);
}
function W(n, e, t) {
  const r = gt(t);
  if (r === null)
    return t;
  const [s, i] = r, o = ti(n, e, s);
  if (!o || !o["unique?"])
    throw new Error(`${s} is not a unique attribute.`);
  return [o.id, i];
}
function zn(n, e, t, r) {
  var o;
  const s = W(n, e, t);
  return Array.isArray(s) ? [[
    "add-triple",
    s,
    (o = $(n, e, "id")) == null ? void 0 : o.id,
    s
  ]].concat(r) : r;
}
function ni({ attrsStore: n }, [e, t, r]) {
  const s = Object.entries(r).flatMap(([i, o]) => {
    const a = Array.isArray(o) ? o : [o], c = $(n, e, i), u = he(n, e, i);
    return a.map((l) => c ? [
      "add-triple",
      W(n, e, t),
      c.id,
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      W(n, c["reverse-identity"][1], l)
    ] : [
      "add-triple",
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      W(n, u["forward-identity"][1], l),
      u == null ? void 0 : u.id,
      W(n, e, t)
    ]);
  });
  return zn(n, e, t, s);
}
function ri({ attrsStore: n }, [e, t, r]) {
  const s = Object.entries(r).flatMap(([i, o]) => {
    const a = Array.isArray(o) ? o : [o], c = $(n, e, i), u = he(n, e, i);
    return a.map((l) => c ? [
      "retract-triple",
      W(n, e, t),
      c.id,
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      W(n, c["reverse-identity"][1], l)
    ] : [
      "retract-triple",
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      W(n, u["forward-identity"][1], l),
      u.id,
      W(n, e, t)
    ]);
  });
  return zn(n, e, t, s);
}
function si(n, e, t, r) {
  var s;
  if (Array.isArray(r)) {
    const [i, o] = r;
    for (const a of n || []) {
      const c = a == null ? void 0 : a.aev.get(i);
      if (c) {
        for (const [u, l, f] of z(c, 2))
          if (f === o)
            return !0;
      }
    }
  } else
    for (const i of n || []) {
      const o = i == null ? void 0 : i.eav.get(r);
      if (o) {
        for (const a of o.keys())
          if (((s = e.getAttr(a)) == null ? void 0 : s["forward-identity"][1]) == t)
            return !0;
      }
    }
  return !1;
}
function Wn({ stores: n, attrsStore: e }, [t, r, s, i]) {
  return (i == null ? void 0 : i.upsert) === !1 ? { mode: "update" } : (i == null ? void 0 : i.upsert) === !0 ? null : si(n, e, t, r) ? { mode: "update" } : null;
}
function ii(n, e) {
  const { attrsStore: t } = n, [r, s, i, o] = e, a = Pt(i), c = W(t, r, s);
  return [["id", c]].concat(Object.entries(a)).map(([l, f]) => {
    const d = $(t, r, l);
    return d["checked-data-type"] === "date" && n.useDateObjects && (f = Be(f)), ["add-triple", c, d.id, f, { mode: "create" }];
  });
}
function oi(n, e) {
  const { attrsStore: t } = n, [r, s, i, o] = e, a = Pt(i), c = W(t, r, s), u = Wn(n, [r, c, i, o]);
  return [["id", c]].concat(Object.entries(a)).map(([f, d]) => {
    const p = $(t, r, f);
    return p["checked-data-type"] === "date" && n.useDateObjects && (d = Be(d)), [
      "add-triple",
      c,
      p.id,
      d,
      ...u ? [u] : []
    ];
  });
}
function ai({ attrsStore: n }, [e, t]) {
  return [["delete-entity", W(n, e, t), e]];
}
function ci(n, e) {
  const { attrsStore: t } = n, [r, s, i, o] = e, a = Pt(i), c = W(t, r, s), u = Wn(n, [r, c, i, o]), l = Object.entries(a).map(([d, p]) => {
    const y = $(t, r, d);
    return [
      "deep-merge-triple",
      c,
      y.id,
      p,
      ...u ? [u] : []
    ];
  });
  return [[
    "add-triple",
    c,
    $(t, r, "id").id,
    c,
    ...u ? [u] : []
  ]].concat(l);
}
function ui({ attrsStore: n }, [e, t, r]) {
  return [["rule-params", W(n, e, t), e, r]];
}
function li(n) {
  const [e, t, r, s, i] = n;
  if (!s)
    return n;
  const o = { ...s };
  return delete o.id, [e, t, r, o, ...i ? [i] : []];
}
function fi(n, e) {
  const [t, ...r] = li(e);
  switch (t) {
    case "merge":
      return ci(n, r);
    case "create":
      return ii(n, r);
    case "update":
      return oi(n, r);
    case "link":
      return ni(n, r);
    case "unlink":
      return ri(n, r);
    case "delete":
      return ai(n, r);
    case "ruleParams":
      return ui(n, r);
    default:
      throw new Error(`unsupported action ${t}`);
  }
}
function di(n) {
  switch (n) {
    case "string":
    case "date":
    case "boolean":
    case "number":
      return n;
    default:
      return;
  }
}
function hi(n, e, t) {
  var a, c;
  const r = (c = (a = n.entities[e]) == null ? void 0 : a.attrs) == null ? void 0 : c[t];
  if (t === "id") return null;
  if (!r)
    throw new Error(`${e}.${t} does not exist in your schema`);
  const { unique: s, indexed: i } = r == null ? void 0 : r.config, o = di(r == null ? void 0 : r.valueType);
  return {
    "index?": i,
    "unique?": s,
    "checked-data-type": o
  };
}
function $e(n, e, t, r) {
  const s = n ? hi(n, e, t) : null, i = M(), a = [M(), e, t];
  return {
    id: i,
    "forward-identity": a,
    "value-type": "blob",
    cardinality: "one",
    "unique?": !1,
    "index?": !1,
    isUnsynced: !0,
    ...s || {},
    ...r || {}
  };
}
function pi(n, e, t) {
  return Object.values(n.links).find((i) => i.forward.on === e && i.forward.label === t || i.reverse.on === e && i.reverse.label === t);
}
function yi(n, e, t) {
  const r = pi(n, e, t);
  if (!r)
    throw new Error(`Couldn't find the link ${e}.${t} in your schema`);
  const { forward: s, reverse: i } = r;
  return {
    "forward-identity": [M(), s.on, s.label],
    "reverse-identity": [M(), i.on, i.label],
    cardinality: s.has === "one" ? "one" : "many",
    "unique?": i.has === "one",
    "on-delete": s.onDelete,
    "on-delete-reverse": i.onDelete
  };
}
function Yt(n, e, t, r) {
  const s = n ? yi(n, e, t) : null, i = M(), o = [M(), e, t], a = [M(), t, e];
  return {
    id: i,
    // @ts-ignore: ts thinks it's any[]
    "forward-identity": o,
    // @ts-ignore: ts thinks it's any[]
    "reverse-identity": a,
    "value-type": "ref",
    // @ts-ignore: ts thinks it's type string
    cardinality: "many",
    "unique?": !1,
    "index?": !1,
    isUnsynced: !0,
    ...s || {},
    ...r || {}
  };
}
const bi = /* @__PURE__ */ new Set(["create", "update", "merge", "link", "unlink"]), mi = /* @__PURE__ */ new Set(["link", "unlink"]), gi = /* @__PURE__ */ new Set(["create", "update", "merge"]), _i = /* @__PURE__ */ new Set([
  "link",
  "unlink",
  "create",
  "update",
  "merge",
  "delete",
  "ruleParams"
]), _t = { "unique?": !0, "index?": !0 }, wi = {
  ..._t,
  cardinality: "one"
};
function Ti(n) {
  const e = [], [t, r, s, i] = n;
  if (!_i.has(t))
    return e;
  const o = gt(s);
  if (o && e.push({ etype: r, lookupPair: o }), t === "link")
    for (const [a, c] of Object.entries(i)) {
      const u = Array.isArray(c) ? c : [c];
      for (const l of u) {
        const f = gt(l);
        f && e.push({
          etype: r,
          lookupPair: f,
          linkLabel: a
        });
      }
    }
  return e;
}
function Si({ attrsStore: n, schema: e }, t) {
  var d, p;
  const r = /* @__PURE__ */ new Set(), s = [], i = [];
  function o(y, m) {
    return $(n, y, m) || s.find(
      (_) => _["forward-identity"][1] === y && _["forward-identity"][2] === m
    );
  }
  function a(y, m) {
    return he(n, y, m) || s.find(
      (_) => {
        var w, b;
        return ((w = _["reverse-identity"]) == null ? void 0 : w[1]) === y && ((b = _["reverse-identity"]) == null ? void 0 : b[2]) === m;
      }
    );
  }
  function c(y) {
    s.push(y), i.push(["add-attr", y]), r.add(y.id);
  }
  function u(y) {
    y && "isUnsynced" in y && y.isUnsynced && !r.has(y.id) && (s.push(y), i.push(["add-attr", y]), r.add(y.id));
  }
  function l(y, m) {
    return m.indexOf(".") !== -1 && // attr names can have `.` in them, so use the attr we find with a `.`
    // before assuming it's a ref lookup.
    !o(y, m);
  }
  function f(y, m) {
    const _ = o(y, m), w = a(y, m);
    u(_), u(w), !_ && !w && c(Yt(e, y, m, wi));
  }
  for (const y of t)
    for (const { etype: m, lookupPair: _, linkLabel: w } of Ti(y)) {
      const b = _[0];
      if (w) {
        f(m, w);
        const g = o(m, w), k = a(m, w);
        u(g), u(k);
        const T = ((d = g == null ? void 0 : g["reverse-identity"]) == null ? void 0 : d[1]) || ((p = k == null ? void 0 : k["forward-identity"]) == null ? void 0 : p[1]) || w;
        if (l(T, b))
          f(T, mt(b));
        else {
          const S = o(T, b);
          S || c(
            $e(e, T, b, _t)
          ), u(S);
        }
      } else if (l(m, b))
        f(m, mt(b));
      else {
        const g = o(m, b);
        g || c($e(e, m, b, _t)), u(g);
      }
    }
  for (const y of t) {
    const [m, _, w, b] = y;
    if (bi.has(m)) {
      const g = o(_, "id");
      u(g), g || c($e(e, _, "id", { "unique?": !0 }));
      for (const k of Object.keys(b)) {
        const T = o(_, k);
        if (u(T), gi.has(m) && (T || c(
          $e(
            e,
            _,
            k,
            k === "id" ? { "unique?": !0 } : null
          )
        )), mi.has(m)) {
          const S = a(_, k);
          !T && !S && c(Yt(e, _, k)), u(S);
        }
      }
    }
  }
  if (s.length) {
    const y = { ...n.attrs };
    for (const m of s)
      y[m.id] = m;
    return [new ce(y, n.linkIndex), i];
  }
  return [n, i];
}
function Ai(n, e) {
  const r = (Array.isArray(e) ? e : [e]).flatMap((c) => Ys(c)), [s, i] = Si(n, r), o = { ...n, attrsStore: s }, a = r.flatMap((c) => fi(o, c));
  return [...i, ...a];
}
function Zt(n, e) {
  typeof requestIdleCallback > "u" ? n() : requestIdleCallback(n, { timeout: e });
}
const Ie = "__meta";
class Vn {
  constructor(e, t) {
  }
}
class wt {
  constructor(e) {
    h(this, "currentValue");
    h(this, "_subs", []);
    h(this, "_persister");
    h(this, "_merge");
    h(this, "serialize");
    h(this, "parse");
    h(this, "_saveThrottleMs");
    h(this, "_idleCallbackMaxWaitMs");
    h(this, "_nextSave", null);
    h(this, "_nextGc", null);
    h(this, "_pendingSaveKeys", /* @__PURE__ */ new Set());
    h(this, "_loadedKeys", /* @__PURE__ */ new Set());
    h(this, "_loadingKeys");
    h(this, "_objectSize");
    h(this, "_log");
    h(this, "onKeyLoaded");
    h(this, "_version", 0);
    h(this, "_meta", {
      isLoading: !0,
      onLoadCbs: [],
      value: null,
      error: null,
      attempts: 0
    });
    h(this, "_gcOpts");
    this._persister = e.persister, this._merge = e.merge, this.serialize = e.serialize, this.parse = e.parse, this._objectSize = e.objectSize, this._log = e.logger, this._saveThrottleMs = e.saveThrottleMs ?? 100, this._idleCallbackMaxWaitMs = e.idleCallbackMaxWaitMs ?? 1e3, this._gcOpts = e.gc, this.currentValue = {}, this._loadedKeys = /* @__PURE__ */ new Set(), this._loadingKeys = {}, this._initMeta(), e.preloadEntryCount && this._preloadEntries(e.preloadEntryCount);
  }
  async _initMeta() {
    var e;
    this._meta.loadingPromise && await this._meta.loadingPromise;
    try {
      const t = this._persister.getItem(Ie);
      this._meta.loadingPromise = t;
      const r = await t;
      this._meta.isLoading = !1, this._meta.error = null, this._meta.loadingPromise = null, this._meta.attempts = 0;
      const s = ((e = this._meta.value) == null ? void 0 : e.objects) ?? {}, i = r ?? {}, o = i.objects ?? {};
      this._meta.value = {
        ...i,
        objects: { ...s, ...o }
      };
    } catch (t) {
      this._meta.error = t, this._meta.attempts++, this._meta.loadingPromise = null;
    }
  }
  async _getMeta() {
    return this._meta.value ? this._meta.value : this._meta.loadingPromise ? (await this._meta.loadingPromise, this._meta.value) : (this._initMeta(), await this._meta.loadingPromise, this._meta.value);
  }
  async _refreshMeta() {
    return await this._initMeta(), this._meta.value;
  }
  async _preloadEntries(e) {
    const t = await this.waitForMetaToLoad();
    if (!t) return;
    const r = Object.entries(t.objects);
    r.sort(([s, i], [o, a]) => a.updatedAt - i.updatedAt);
    for (const [s] of r.slice(0, e))
      this._loadKey(s);
  }
  async _getFromStorage(e) {
    try {
      const t = await this._persister.getItem(e);
      return t && this.parse(e, t);
    } catch (t) {
      return console.error(`Unable to read from storage for key=${e}`, t), null;
    }
  }
  async waitForKeyToLoad(e) {
    return this._loadedKeys.has(e) ? this.currentValue[e] : (await (this._loadingKeys[e] || this._loadKey(e)), this.currentValue[e]);
  }
  // Used for tests
  async waitForMetaToLoad() {
    return this._getMeta();
  }
  // Unloads the key so that it can be garbage collected, but does not
  // delete it. Removes the key from currentValue.
  unloadKey(e) {
    this._loadedKeys.delete(e), delete this._loadingKeys[e], delete this.currentValue[e];
  }
  async _loadKey(e) {
    if (this._loadedKeys.has(e) || e in this._loadingKeys) return;
    const t = this._getFromStorage(e);
    this._loadingKeys[e] = t;
    const r = await t;
    if (delete this._loadingKeys[e], this._loadedKeys.add(e), r) {
      const s = this._merge(e, r, this.currentValue[e]);
      s && (this.currentValue[e] = s);
    }
    this.onKeyLoaded && this.onKeyLoaded(e);
  }
  // Returns a promise with a number so that we can wait for flush
  // to finish in the tests. The number is the number of operations
  // it performed, but it's mostly there so that typescript will warn
  // us if we forget to retun the promise from the function.
  _writeToStorage(e) {
    const t = [], r = e == null ? void 0 : e.skipGc;
    if (this._meta.isLoading) {
      const f = new Promise((d, p) => {
        setTimeout(
          () => this._enqueuePersist(
            e ? { ...e, attempts: (e.attempts || 0) + 1 } : { attempts: 1 }
          ).then(d).catch(p),
          10 + ((e == null ? void 0 : e.attempts) ?? 0) * 1e3
        );
      });
      return t.push(f), Promise.all(t).then(
        (d) => d.reduce((p, y) => p + y, 0)
      );
    }
    const s = this._meta.value;
    if (!s)
      return Promise.resolve(0);
    const i = [], o = [];
    for (const f of this._pendingSaveKeys)
      f in this.currentValue ? o.push(f) : (i.push(f), delete s.objects[f]);
    for (const f of i) {
      const d = this._persister.removeItem(f);
      t.push(d.then(() => 1)), this._loadedKeys.delete(f), this._pendingSaveKeys.delete(f);
    }
    const a = [], c = [[Ie, s]], u = s.objects ?? {};
    s.objects = u;
    for (const f of o)
      if (this._loadedKeys.has(f)) {
        const d = this.serialize(f, this.currentValue[f]);
        c.push([f, d]);
        const p = this._objectSize(d), y = u[f] ?? {
          createdAt: Date.now(),
          updatedAt: Date.now(),
          size: p
        };
        y.updatedAt = Date.now(), y.size = p, u[f] = y, this._pendingSaveKeys.delete(f);
      } else
        a.push(f);
    const l = this._persister.multiSet(c);
    t.push(l.then(() => 1));
    for (const f of a) {
      const d = this._loadKey(f).then(() => this._enqueuePersist(e));
      t.push(d);
    }
    return r || this.gc(), Promise.all(t).then((f) => f.reduce((d, p) => d + p, 0));
  }
  async flush() {
    return this._nextSave ? (clearTimeout(this._nextSave), this._nextSave = null, this._writeToStorage()) : void 0;
  }
  async _gc() {
    if (!this._gcOpts)
      return;
    const e = new Set(await this._persister.getAllKeys());
    e.delete(Ie);
    const t = new Set(Object.keys(this.currentValue));
    for (const d of Object.keys(this._loadingKeys))
      t.add(d);
    for (const d of this._loadedKeys)
      t.add(d);
    const r = await this._refreshMeta();
    if (!r) {
      this._log.info("Could not gc because we were not able to load meta");
      return;
    }
    const s = [], i = {
      gcOpts: this._gcOpts,
      keys: e,
      sacredKeys: t,
      removed: [],
      metaRemoved: [],
      removedMissingCount: 0,
      removedOldCount: 0,
      removedThresholdCount: 0,
      removedSizeCount: 0
    };
    for (const d of e)
      t.has(d) || d in r.objects || (this._log.info("Lost track of key in meta", d), s.push(this._persister.removeItem(d)), i.removed.push(d), i.removedMissingCount++);
    const o = Date.now();
    for (const [d, p] of Object.entries(r.objects))
      !t.has(d) && p.updatedAt < o - this._gcOpts.maxAgeMs && (s.push(this._persister.removeItem(d)), delete r.objects[d], i.removed.push(d), i.removedOldCount++);
    const a = Object.entries(r.objects);
    a.sort(([d, p], [y, m]) => p.updatedAt - m.updatedAt);
    const c = a.filter(([d]) => !t.has(d));
    if (a.length > this._gcOpts.maxEntries)
      for (const [d] of c.slice(
        0,
        a.length - this._gcOpts.maxEntries
      ))
        s.push(this._persister.removeItem(d)), delete r.objects[d], i.removed.push(d), i.removedThresholdCount++;
    const u = Object.entries(r.objects);
    u.sort(([d, p], [y, m]) => p.updatedAt - m.updatedAt);
    const l = u.filter(([d]) => !t.has(d));
    let f = u.reduce((d, [p, y]) => d + y.size, 0);
    for (; f > 0 && f > this._gcOpts.maxSize && l.length; ) {
      const [[d, p]] = l.splice(0, 1);
      f -= p.size, s.push(this._persister.removeItem(d)), delete r.objects[d], i.removed.push(d), i.removedSizeCount++;
    }
    for (const d of Object.keys(r.objects))
      !e.has(d) && !t.has(d) && delete r.objects[d];
    return (i.removed.length || i.metaRemoved.length) && s.push(this._enqueuePersist({ skipGc: !0 })), this._log.info("Completed GC", i), await Promise.all(s), i;
  }
  // Schedules a GC to run in one minute (unless it is already scheduled)
  gc() {
    this._nextGc || (this._nextGc = setTimeout(
      () => {
        Zt(() => {
          this._nextGc = null, this._gc();
        }, 30 * 1e3);
      },
      // 1 minute + some jitter to keep multiple tabs from running at same time
      1e3 * 60 + Math.random() * 500
    ));
  }
  _enqueuePersist(e) {
    return new Promise((t, r) => {
      if (this._nextSave) {
        t(0);
        return;
      }
      this._nextSave = setTimeout(() => {
        Zt(() => {
          this._nextSave = null, this._writeToStorage(e).then(t).catch(r);
        }, this._idleCallbackMaxWaitMs);
      }, this._saveThrottleMs);
    });
  }
  version() {
    return this._version;
  }
  // Takes a function that updates the store in place.
  // Uses `mutative` to get a list of keys that were changed
  // so that we know which entries we need to persist to the store.
  updateInPlace(e) {
    this._version++;
    const [t, r] = We(this.currentValue, e, {
      enablePatches: !0
    });
    for (const s of r) {
      const i = s.path[0];
      i && typeof i == "string" && (this._pendingSaveKeys.add(i), this._loadedKeys.has(i) || this._loadKey(i));
    }
    this.currentValue = t, this._enqueuePersist();
    for (const s of this._subs)
      s(this.currentValue);
    return t;
  }
  subscribe(e) {
    return this._subs.push(e), e(this.currentValue), () => {
      this._subs = this._subs.filter((t) => t !== e);
    };
  }
}
const vi = 6, ki = ["kv", "querySubs", "syncSubs"];
function Oi(n) {
  return function(t) {
    console.error("Error in IndexedDB event", { source: n, event: t });
  };
}
async function Ii(n) {
  return new Promise((e) => {
    const t = indexedDB.open(n);
    t.onerror = (r) => {
      e(null);
    }, t.onsuccess = (r) => {
      const i = r.target.result;
      e(i);
    }, t.onupgradeneeded = (r) => {
      var i;
      (i = r.target.transaction) == null || i.abort(), e(null);
    };
  });
}
async function Ci(n, e, t) {
  const r = (
    // Backwards compatibility for older versions where we JSON.stringified before storing
    typeof e == "string" ? JSON.parse(e) : e
  );
  if (!r)
    return;
  const s = /* @__PURE__ */ new Set();
  return new Promise((i, o) => {
    var l, f, d;
    const a = {};
    for (const [p, y] of Object.entries(r)) {
      const m = typeof y == "string" ? JSON.parse(y) : y;
      if (m.lastAccessed) {
        const w = {
          createdAt: m.lastAccessed,
          updatedAt: m.lastAccessed,
          size: ((d = (f = (l = m.result) == null ? void 0 : l.store) == null ? void 0 : f.triples) == null ? void 0 : d.length) ?? 0
        };
        a[p] = w;
      }
      const _ = t.put(m, p);
      s.add(_);
    }
    const c = { objects: a }, u = t.put(c, Ie);
    s.add(u);
    for (const p of s)
      p.onsuccess = () => {
        s.delete(p), s.size === 0 && i();
      }, p.onerror = (y) => {
        o(y);
      };
  });
}
async function Xt(n, e, t) {
  const r = t.put(e, n);
  return new Promise((s, i) => {
    r.onsuccess = () => s(), r.onerror = (o) => i(o);
  });
}
async function Ei(n, e) {
  const t = await Ii(`instant_${n}_5`);
  if (!t)
    return;
  const r = await new Promise((l, f) => {
    const y = t.transaction(["kv"], "readonly").objectStore("kv").openCursor();
    y.onerror = (_) => {
      f(_);
    };
    const m = [];
    y.onsuccess = () => {
      const _ = y.result;
      if (_) {
        const w = _.key, b = _.value;
        m.push([w, b]), _.continue();
      } else
        l(m);
    }, y.onerror = (_) => {
      f(_);
    };
  }), s = e.transaction(["kv", "querySubs"], "readwrite"), i = s.objectStore("kv"), o = s.objectStore("querySubs"), a = [], c = { objects: {} };
  for (const [l, f] of r)
    switch (l) {
      case "querySubs": {
        const d = Ci(l, f, o);
        a.push(d);
        break;
      }
      default: {
        const d = Xt(l, f, i);
        a.push(d);
        const p = {
          createdAt: Date.now(),
          updatedAt: Date.now(),
          size: 0
        };
        c.objects[l] = p;
        break;
      }
    }
  const u = Xt(Ie, c, i);
  a.push(u), await Promise.all(a), await new Promise((l, f) => {
    s.oncomplete = (d) => l(d), s.onerror = (d) => f(d), s.onabort = (d) => f(d);
  });
}
const en = /* @__PURE__ */ new Map();
class Qn extends Vn {
  constructor(t, r) {
    super(t, r);
    h(this, "dbName");
    h(this, "_storeName");
    h(this, "_appId");
    h(this, "_prefix");
    h(this, "_dbPromise");
    this.dbName = `instant_${t}_${vi}`, this._storeName = r, this._appId = t, this._dbPromise = this._init();
  }
  _init() {
    return new Promise((t, r) => {
      let s = !1;
      const i = indexedDB.open(this.dbName, 1);
      i.onerror = (o) => {
        r(o);
      }, i.onsuccess = (o) => {
        const c = o.target.result;
        if (s) {
          const u = Ei(this._appId, c).catch((l) => {
            Oi("Error upgrading store from version 5 to 6.")(l);
          });
          en.set(this.dbName, u), u.then(() => t(c)).catch(() => t(c));
        } else {
          const u = en.get(this.dbName);
          u ? u.then(() => t(c)).catch(() => t(c)) : t(c);
        }
      }, i.onupgradeneeded = (o) => {
        s = !0, this._upgradeStore(o);
      };
    });
  }
  _upgradeStore(t) {
    const s = t.target.result;
    for (const i of ki)
      s.objectStoreNames.contains(i) || s.createObjectStore(i);
  }
  async getItem(t) {
    const r = await this._dbPromise;
    return new Promise((s, i) => {
      const c = r.transaction([this._storeName], "readonly").objectStore(this._storeName).get(t);
      c.onerror = (u) => {
        i(u);
      }, c.onsuccess = (u) => {
        c.result ? s(c.result) : s(null);
      };
    });
  }
  async setItem(t, r) {
    const s = await this._dbPromise;
    return new Promise((i, o) => {
      const u = s.transaction([this._storeName], "readwrite").objectStore(this._storeName).put(r, t);
      u.onerror = (l) => {
        o(l);
      }, u.onsuccess = (l) => {
        i();
      };
    });
  }
  // Performs all writes in a transaction so that all succeed or none succeed.
  async multiSet(t) {
    const r = await this._dbPromise;
    return new Promise((s, i) => {
      const o = r.transaction([this._storeName], "readwrite"), a = o.objectStore(this._storeName), c = /* @__PURE__ */ new Set();
      for (const [u, l] of t) {
        const f = a.put(l, u);
        c.add(f);
      }
      for (const u of c)
        u.onerror = (l) => {
          o.abort(), i(l);
        }, u.onsuccess = (l) => {
          c.delete(u), c.size === 0 && s();
        };
    });
  }
  async removeItem(t) {
    const r = await this._dbPromise;
    return new Promise((s, i) => {
      const c = r.transaction([this._storeName], "readwrite").objectStore(this._storeName).delete(t);
      c.onerror = (u) => {
        i(u);
      }, c.onsuccess = (u) => {
        s();
      };
    });
  }
  async getAllKeys() {
    const t = await this._dbPromise;
    return new Promise((r, s) => {
      const a = t.transaction([this._storeName], "readonly").objectStore(this._storeName).getAllKeys();
      a.onerror = (c) => {
        s(c);
      }, a.onsuccess = (c) => {
        r(a.result.filter((u) => typeof u == "string"));
      };
    });
  }
}
class Bn {
  static async getIsOnline() {
    return navigator.onLine;
  }
  static listen(e) {
    const t = () => {
      e(!0);
    }, r = () => {
      e(!1);
    };
    return addEventListener("online", t), addEventListener("offline", r), () => {
      removeEventListener("online", t), removeEventListener("offline", r);
    };
  }
}
class V extends Error {
  constructor(t, r) {
    super(t);
    h(this, "hint");
    this.hint = r;
    const s = new.target.prototype;
    Object.setPrototypeOf && Object.setPrototypeOf(this, s), Error.captureStackTrace && Error.captureStackTrace(this, V), this.name = "InstantError";
  }
  get [Symbol.toStringTag]() {
    return "InstantError";
  }
}
class He extends V {
  constructor(t) {
    var i;
    const r = ((i = t.body) == null ? void 0 : i.message) || `API Error (${t.status})`;
    super(r, t.body.hint);
    h(this, "body");
    h(this, "status");
    const s = new.target.prototype;
    Object.setPrototypeOf && Object.setPrototypeOf(this, s), Error.captureStackTrace && Error.captureStackTrace(this, He), this.name = "InstantAPIError", this.status = t.status, this.body = t.body;
  }
  get [Symbol.toStringTag]() {
    return "InstantAPIError";
  }
}
async function X(n, e) {
  const t = await fetch(n, e), r = await t.json();
  return t.status === 200 ? Promise.resolve(r) : Promise.reject(new He({ status: t.status, body: r }));
}
function Mi({
  apiURI: n,
  appId: e,
  email: t
}) {
  return X(`${n}/runtime/auth/send_magic_code`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({ "app-id": e, email: t })
  });
}
async function Pi({
  apiURI: n,
  appId: e,
  email: t,
  code: r,
  refreshToken: s
}) {
  return await X(`${n}/runtime/auth/verify_magic_code`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      "app-id": e,
      email: t,
      code: r,
      ...s ? { "refresh-token": s } : {}
    })
  });
}
async function ji({
  apiURI: n,
  appId: e,
  refreshToken: t
}) {
  return await X(`${n}/runtime/auth/verify_refresh_token`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      "app-id": e,
      "refresh-token": t
    })
  });
}
async function Di({
  apiURI: n,
  appId: e
}) {
  return await X(`${n}/runtime/auth/sign_in_guest`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      "app-id": e
    })
  });
}
async function tn({
  apiURI: n,
  appId: e,
  code: t,
  codeVerifier: r,
  refreshToken: s
}) {
  return await X(`${n}/runtime/oauth/token`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      app_id: e,
      code: t,
      code_verifier: r,
      refresh_token: s
    })
  });
}
async function $i({
  apiURI: n,
  appId: e,
  nonce: t,
  idToken: r,
  clientName: s,
  refreshToken: i
}) {
  return await X(`${n}/runtime/oauth/id_token`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      app_id: e,
      nonce: t,
      id_token: r,
      client_name: s,
      refresh_token: i
    })
  });
}
async function Ri({
  apiURI: n,
  appId: e,
  refreshToken: t
}) {
  return await X(`${n}/runtime/signout`, {
    method: "POST",
    headers: {
      "content-type": "application/json"
    },
    body: JSON.stringify({
      app_id: e,
      refresh_token: t
    })
  });
}
async function xi({
  apiURI: n,
  appId: e,
  path: t,
  file: r,
  refreshToken: s,
  contentType: i,
  contentDisposition: o
}) {
  const a = {
    app_id: e,
    path: t,
    authorization: `Bearer ${s}`,
    "content-type": i || r.type
  };
  return o && (a["content-disposition"] = o), await X(`${n}/storage/upload`, {
    method: "PUT",
    headers: a,
    body: r
  });
}
async function Ui({
  apiURI: n,
  appId: e,
  path: t,
  refreshToken: r
}) {
  const { data: s } = await X(
    `${n}/storage/files?app_id=${e}&filename=${encodeURIComponent(t)}`,
    {
      method: "DELETE",
      headers: {
        "content-type": "application/json",
        authorization: `Bearer ${r}`
      }
    }
  );
  return s;
}
async function Li({
  apiURI: n,
  appId: e,
  fileName: t,
  refreshToken: r,
  metadata: s = {}
}) {
  const { data: i } = await X(`${n}/storage/signed-upload-url`, {
    method: "POST",
    headers: {
      "content-type": "application/json",
      authorization: `Bearer ${r}`
    },
    body: JSON.stringify({
      app_id: e,
      filename: t
    })
  });
  return i;
}
async function Fi(n, e) {
  return (await fetch(n, {
    method: "PUT",
    body: e,
    headers: {
      "Content-Type": e.type
    }
  })).ok;
}
async function Ni({
  apiURI: n,
  appId: e,
  path: t,
  refreshToken: r
}) {
  const { data: s } = await X(
    `${n}/storage/signed-download-url?app_id=${e}&filename=${encodeURIComponent(
      t
    )}`,
    {
      method: "GET",
      headers: {
        "content-type": "application/json",
        authorization: `Bearer ${r}`
      }
    }
  );
  return s;
}
let xt = !1, Gn = !1, Hn = !1;
typeof window < "u" && typeof window.localStorage < "u" && (xt = !!window.localStorage.getItem("devBackend"), Gn = !!window.localStorage.getItem("__instantLogging"), Hn = !!window.localStorage.getItem("__devtoolLocalDash"));
function nn(n, e) {
  if (!e) return n;
  const t = {};
  return e.forEach((r) => {
    t[r] = n[r];
  }), t;
}
function qi(n, e, t) {
  const r = {
    peers: {}
  };
  if (e && "user" in e ? e.user : !0) {
    const i = nn(n.user ?? {}, e == null ? void 0 : e.keys);
    r.user = { ...i, peerId: t };
  }
  for (const i of Object.keys(n.peers ?? {})) {
    const o = (e == null ? void 0 : e.peers) === void 0, a = Array.isArray(e == null ? void 0 : e.peers) && (e == null ? void 0 : e.peers.includes(i));
    if (o || a) {
      const c = nn(n.peers[i], e == null ? void 0 : e.keys);
      r.peers[i] = { ...c, peerId: i };
    }
  }
  return r;
}
function Ki(n, e) {
  if (n.isLoading !== e.isLoading || n.error !== e.error || (n.user || e.user) && (!n.user || !e.user || !qt(n.user, e.user)) || !An(n.peers, e.peers)) return !0;
  for (const r of Object.keys(n.peers))
    if (!qt(n.peers[r], e.peers[r])) return !0;
  return !1;
}
class rn {
  constructor() {
    h(this, "promise");
    h(this, "_resolve");
    h(this, "_reject");
    this.promise = new Promise((e, t) => {
      this._resolve = e, this._reject = t;
    });
  }
  resolve(e) {
    this._resolve(e);
  }
  reject(e) {
    this._reject(e);
  }
}
function Jn(n, e = []) {
  n.forEach((t) => {
    const { data: r } = t, { "datalog-result": s } = r, { "join-rows": i } = s;
    for (const o of i)
      for (const a of o)
        e.push(a);
    Jn(t["child-nodes"], e);
  });
}
function sn(n) {
  const e = [];
  return Jn(n, e), e;
}
function Tt(n) {
  return Object.values(n.links).reduce((e, t) => {
    var r, s;
    return e[r = t.forward.on] ?? (e[r] = {}), e[t.forward.on][t.forward.label] = {
      isForward: !0,
      isSingular: t.forward.has === "one",
      link: t
    }, e[s = t.reverse.on] ?? (e[s] = {}), e[t.reverse.on][t.reverse.label] = {
      isForward: !1,
      isSingular: t.reverse.has === "one",
      link: t
    }, e;
  }, {});
}
const Yn = "v0.22.142";
function zi(n, e) {
  return {
    info: n ? (...t) => console.info(...t, e()) : () => {
    },
    debug: n ? (...t) => console.debug(...t, e()) : () => {
    },
    error: n ? (...t) => console.error(...t, e()) : () => {
    }
  };
}
class N {
  constructor(e, t, r, s = { indexed: !1, unique: !1 }) {
    h(this, "metadata", {});
    this.valueType = e, this.required = t, this.isIndexed = r, this.config = s;
  }
  /**
   * @deprecated Only use this temporarily for attributes that you want
   * to treat as required in frontend code but can’t yet mark as required
   * and enforced for backend
   */
  clientRequired() {
    return new N(
      this.valueType,
      !1,
      this.isIndexed,
      this.config
    );
  }
  optional() {
    return new N(
      this.valueType,
      !1,
      this.isIndexed,
      this.config
    );
  }
  unique() {
    return new N(
      this.valueType,
      this.required,
      this.isIndexed,
      {
        ...this.config,
        unique: !0
      }
    );
  }
  indexed() {
    return new N(
      this.valueType,
      this.required,
      !0,
      {
        ...this.config,
        indexed: !0
      }
    );
  }
  // clientValidate(clientValidator: (value: ValueType) => boolean) {
  //   return new DataAttrDef(this.valueType, this.required, {
  //     ...this.config,
  //     clientValidator,
  //   });
  // }
}
class Je {
  constructor(e, t) {
    this.attrs = e, this.links = t;
  }
  asType() {
    return new Je(this.attrs, this.links);
  }
}
class Ye {
  constructor(e, t, r) {
    this.entities = e, this.links = t, this.rooms = r;
  }
  /**
   * @deprecated
   * `withRoomSchema` is deprecated. Define your schema in `rooms` directly:
   *
   * @example
   * // Before:
   * const schema = i.schema({
   *   // ...
   * }).withRoomSchema<RoomSchema>()
   *
   * // After
   * const schema = i.schema({
   *  rooms: {
   *    // ...
   *  }
   * })
   *
   * @see https://instantdb.com/docs/presence-and-topics#typesafety
   */
  withRoomSchema() {
    return new Ye(
      this.entities,
      this.links,
      {}
    );
  }
}
class C extends Error {
  constructor(e, t) {
    const r = t ? `At path '${t}': ${e}` : e;
    super(r), this.name = "QueryValidationError";
  }
}
const on = [
  "where",
  "order",
  "limit",
  "last",
  "first",
  "offset",
  "after",
  "before",
  "fields",
  "aggregate"
], Wi = (n) => n.valueType || "unknown", Zn = (n, e, t = !1) => {
  if (t || n == null) return !0;
  switch (e) {
    case "string":
      return typeof n == "string";
    case "number":
      return typeof n == "number" && !isNaN(n);
    case "boolean":
      return typeof n == "boolean";
    case "date":
      return n instanceof Date || typeof n == "string" || typeof n == "number";
    default:
      return !0;
  }
}, Vi = (n, e, t, r, s, i, o) => {
  const a = i.valueType === "json", c = (u, l, f) => {
    if (!Zn(f, l, a))
      throw new C(
        `Invalid value for operator '${u}' on attribute '${r}' in entity '${s}'. Expected ${l}, but received: ${typeof f}`,
        o
      );
  };
  switch (n) {
    case "in":
    case "$in":
      if (!Array.isArray(e))
        throw new C(
          `Operator '${n}' for attribute '${r}' in entity '${s}' must be an array, but received: ${typeof e}`,
          o
        );
      for (const u of e)
        c(n, t, u);
      break;
    case "$not":
    case "$ne":
    case "$gt":
    case "$lt":
    case "$gte":
    case "$lte":
      c(n, t, e);
      break;
    case "$like":
    case "$ilike":
      if (c(n, "string", e), n === "$ilike" && !i.isIndexed)
        throw new C(
          `Operator '${n}' can only be used with indexed attributes, but '${r}' in entity '${s}' is not indexed`,
          o
        );
      break;
    case "$isNull":
      c(n, "boolean", e);
      break;
    default:
      throw new C(
        `Unknown operator '${n}' for attribute '${r}' in entity '${s}'`,
        o
      );
  }
}, be = (n, e, t, r, s) => {
  const i = Wi(t), o = t.valueType === "json";
  if (typeof n == "object" && n !== null && !Array.isArray(n)) {
    if (o)
      return;
    const c = n;
    for (const [u, l] of Object.entries(c))
      Vi(
        u,
        l,
        i,
        e,
        r,
        t,
        `${s}.${u}`
      );
  } else if (!Zn(n, i, o))
    throw new C(
      `Invalid value for attribute '${e}' in entity '${r}'. Expected ${i}, but received: ${typeof n}`,
      s
    );
}, Qi = (n, e, t, r, s) => {
  const i = n.split(".");
  if (i.length < 2)
    throw new C(
      `Invalid dot notation path '${n}'. Must contain at least one dot.`,
      s
    );
  let o = t;
  for (let l = 0; l < i.length - 1; l++) {
    const f = i[l], d = r.entities[o];
    if (!d)
      throw new C(
        `Entity '${o}' does not exist in schema while traversing dot notation path '${n}'.`,
        s
      );
    const p = d.links[f];
    if (!p) {
      const y = Object.keys(d.links);
      throw new C(
        `Link '${f}' does not exist on entity '${o}' in dot notation path '${n}'. Available links: ${y.length > 0 ? y.join(", ") : "none"}`,
        s
      );
    }
    o = p.entityName;
  }
  const a = i[i.length - 1], c = r.entities[o];
  if (!c)
    throw new C(
      `Target entity '${o}' does not exist in schema for dot notation path '${n}'.`,
      s
    );
  if (a === "id") {
    if (typeof e == "string" && !me(e))
      throw new C(
        `Invalid value for id field in entity '${o}'. Expected a UUID, but received: ${e}`,
        s
      );
    be(
      e,
      n,
      new N("string", !1, !0),
      t,
      s
    );
    return;
  }
  const u = c.attrs[a];
  if (Object.keys(c.links).includes(a)) {
    if (typeof e == "string" && !me(e))
      throw new C(
        `Invalid value for link '${a}' in entity '${o}'. Expected a UUID, but received: ${e}`,
        s
      );
    be(
      e,
      n,
      new N("string", !1, !0),
      t,
      s
    );
    return;
  }
  if (!u) {
    const l = Object.keys(c.attrs);
    throw new C(
      `Attribute '${a}' does not exist on entity '${o}' in dot notation path '${n}'. Available attributes: ${l.length > 0 ? l.join(", ") + ", id" : "id"}`,
      s
    );
  }
  be(e, n, u, t, s);
}, Xn = (n, e, t, r) => {
  for (const [s, i] of Object.entries(n)) {
    if (s === "or" || s === "and") {
      if (Array.isArray(i))
        for (const u of i)
          typeof u == "object" && u !== null && Xn(
            u,
            e,
            t,
            `${r}.${s}[${u}]`
          );
      continue;
    }
    if (s === "id") {
      be(
        i,
        "id",
        new N("string", !1, !0),
        e,
        `${r}.id`
      );
      continue;
    }
    if (s.includes(".")) {
      Qi(
        s,
        i,
        e,
        t,
        `${r}.${s}`
      );
      continue;
    }
    const o = t.entities[e];
    if (!o) continue;
    const a = o.attrs[s], c = o.links[s];
    if (!a && !c) {
      const u = Object.keys(o.attrs), l = Object.keys(o.links);
      throw new C(
        `Attribute or link '${s}' does not exist on entity '${e}'. Available attributes: ${u.length > 0 ? u.join(", ") : "none"}. Available links: ${l.length > 0 ? l.join(", ") : "none"}`,
        `${r}.${s}`
      );
    }
    if (a)
      be(
        i,
        s,
        a,
        e,
        `${r}.${s}`
      );
    else if (c) {
      if (typeof i == "string" && !me(i))
        throw new C(
          `Invalid value for link '${s}' in entity '${e}'. Expected a UUID, but received: ${i}`,
          `${r}.${s}`
        );
      const u = new N("string", !1, !0);
      be(
        i,
        s,
        u,
        e,
        `${r}.${s}`
      );
    }
  }
}, Bi = (n, e, t, r, s = 0) => {
  for (const o of Object.keys(n))
    if (!on.includes(o))
      throw new C(
        `Invalid query parameter '${o}' in $ object. Valid parameters are: ${on.join(", ")}. Found: ${o}`,
        r
      );
  const i = [
    // 'limit', // only supported client side
    "offset",
    "before",
    "after",
    "first",
    "last"
  ];
  for (const o of i)
    if (n[o] !== void 0 && s > 0)
      throw new C(
        `'${o}' can only be used on top-level namespaces. It cannot be used in nested queries.`,
        r
      );
  if (n.where && t) {
    if (typeof n.where != "object" || n.where === null)
      throw new C(
        `'where' clause must be an object in entity '${e}', but received: ${typeof n.where}`,
        r ? `${r}.where` : void 0
      );
    Xn(
      n.where,
      e,
      t,
      r ? `${r}.where` : "where"
    );
  }
}, er = (n, e, t, r, s = 0) => {
  var i;
  if (!n || typeof n != "object")
    throw new C(
      `Query part for entity '${e}' must be an object, but received: ${typeof n}`,
      r
    );
  for (const o of Object.keys(n))
    if (o !== "$") {
      if (t && !(o in t.entities[e].links)) {
        const c = Object.keys(t.entities[e].links);
        throw new C(
          `Link '${o}' does not exist on entity '${e}'. Available links: ${c.length > 0 ? c.join(", ") : "none"}`,
          `${r}.${o}`
        );
      }
      const a = n[o];
      if (typeof a == "object" && a !== null) {
        const c = (i = t == null ? void 0 : t.entities[e].links[o]) == null ? void 0 : i.entityName;
        c && er(
          a,
          c,
          t,
          `${r}.${o}`,
          s + 1
        );
      }
    } else {
      const a = n[o];
      if (typeof a != "object" || a === null)
        throw new C(
          `Query parameter '$' must be an object in entity '${e}', but received: ${typeof a}`,
          `${r}.$`
        );
      Bi(
        a,
        e,
        t,
        `${r}.$`,
        s
      );
    }
}, an = (n, e) => {
  if (typeof n != "object" || n === null)
    throw new C(
      `Query must be an object, but received: ${typeof n}${n === null ? " (null)" : ""}`
    );
  if (Array.isArray(n))
    throw new C(
      `Query must be an object, but received: ${typeof n}`
    );
  const t = n;
  for (const r of Object.keys(t)) {
    if (Array.isArray(n[r]))
      throw new C(
        `Query keys must be strings, but found key of type: ${typeof r}`,
        r
      );
    if (typeof r != "string")
      throw new C(
        `Query keys must be strings, but found key of type: ${typeof r}`,
        r
      );
    if (r !== "$$ruleParams") {
      if (e && !e.entities[r]) {
        const s = Object.keys(e.entities);
        throw new C(
          `Entity '${r}' does not exist in schema. Available entities: ${s.length > 0 ? s.join(", ") : "none"}`,
          r
        );
      }
      er(
        t[r],
        r,
        e,
        r,
        0
        // Start at depth 0 for top-level entities
      );
    }
  }
}, cn = (n) => typeof n != "string" ? !1 : Qe(n) ? !0 : me(n);
class G extends Error {
  constructor(e) {
    super(e), this.name = "TransactionValidationError";
  }
}
const tr = (n) => n.length > 0 ? n.join(", ") : "none", Gi = (n, e) => new G(
  `Entity '${n}' does not exist in schema. Available entities: ${tr(e)}`
), tt = {
  string: (n) => typeof n == "string",
  number: (n) => typeof n == "number" && !isNaN(n),
  boolean: (n) => typeof n == "boolean",
  date: (n) => n instanceof Date || typeof n == "string" || typeof n == "number",
  json: () => !0
}, Hi = (n, e) => {
  var t;
  return n == null ? !0 : ((t = tt[e.valueType]) == null ? void 0 : t.call(tt, n)) ?? !1;
}, nr = (n, e) => {
  const t = e.entities[n];
  if (!t)
    throw Gi(n, Object.keys(e.entities));
  return t;
}, nt = (n, e, t) => {
  const r = nr(n, t);
  if (typeof e != "object" || e === null)
    throw new G(
      `Arguments for data operation on entity '${n}' must be an object, but received: ${typeof e}`
    );
  for (const [s, i] of Object.entries(e)) {
    if (s === "id") continue;
    const o = r.attrs[s];
    if (o && !Hi(i, o))
      throw new G(
        `Invalid value for attribute '${s}' in entity '${n}'. Expected ${o.valueType}, but received: ${typeof i}`
      );
  }
}, un = (n, e, t) => {
  const r = nr(n, t);
  if (typeof e != "object" || e === null)
    throw new G(
      `Arguments for link operation on entity '${n}' must be an object, but received: ${typeof e}`
    );
  for (const [s, i] of Object.entries(e)) {
    if (!r.links[s]) {
      const a = Object.keys(r.links);
      throw new G(
        `Link '${s}' does not exist on entity '${n}'. Available links: ${tr(a)}`
      );
    }
    if (i != null) {
      if (Array.isArray(i)) {
        for (const a of i)
          if (!cn(a))
            throw new G(
              `Invalid entity ID in link '${s}' for entity '${n}'. Expected a UUID or a lookup, but received: ${a}`
            );
      } else if (!cn(i))
        throw new G(
          `Invalid UUID in link '${s}' for entity '${n}'. Expected a UUID, but received: ${i}`
        );
    }
  }
}, Ji = {
  create: nt,
  update: nt,
  merge: nt,
  link: un,
  unlink: un,
  delete: () => {
  }
}, Yi = (n, e) => {
  if (!e) return;
  const [t, r, s, i] = n;
  if (!Array.isArray(s) && !me(s))
    throw new G(
      `Invalid id for entity '${r}'. Expected a UUID, but received: ${s}`
    );
  if (typeof r != "string")
    throw new G(
      `Entity name must be a string, but received: ${typeof r}`
    );
  const o = Ji[t];
  o && i !== void 0 && o(r, i, e);
}, Zi = (n, e) => {
  const t = Array.isArray(n) ? n : [n];
  for (const r of t) {
    if (!r || typeof r != "object")
      throw new G(
        `Transaction chunk must be an object, but received: ${typeof r}`
      );
    if (!Array.isArray(r.__ops))
      throw new G(
        `Transaction chunk must have __ops array, but received: ${typeof r.__ops}`
      );
    for (const s of r.__ops) {
      if (!Array.isArray(s))
        throw new G(
          `Transaction operation must be an array, but received: ${typeof s}`
        );
      Yi(s, e);
    }
  }
};
let rr = 0;
class ln {
  constructor(e) {
    h(this, "type", "ws");
    h(this, "conn");
    h(this, "id");
    h(this, "onopen");
    h(this, "onmessage");
    h(this, "onclose");
    h(this, "onerror");
    this.id = `${this.type}_${rr++}`, this.conn = new WebSocket(e), this.conn.onopen = (t) => {
      this.onopen && this.onopen({ target: this });
    }, this.conn.onmessage = (t) => {
      this.onmessage && this.onmessage({
        target: this,
        message: JSON.parse(t.data.toString())
      });
    }, this.conn.onclose = (t) => {
      this.onclose && this.onclose({ target: this });
    }, this.conn.onerror = (t) => {
      this.onerror && this.onerror({ target: this });
    };
  }
  close() {
    this.conn.close();
  }
  isOpen() {
    return this.conn.readyState === (WebSocket.OPEN ?? 1);
  }
  isConnecting() {
    return this.conn.readyState === (WebSocket.CONNECTING ?? 0);
  }
  send(e) {
    return this.conn.send(JSON.stringify(e));
  }
}
class Xi {
  constructor(e, t, r) {
    h(this, "type", "sse");
    h(this, "initParams", null);
    h(this, "sendQueue", []);
    h(this, "sendPromise");
    h(this, "closeFired", !1);
    h(this, "sseInitTimeout");
    h(this, "ES");
    h(this, "messageUrl");
    h(this, "conn");
    h(this, "url");
    h(this, "id");
    h(this, "onopen");
    h(this, "onmessage");
    h(this, "onclose");
    h(this, "onerror");
    this.id = `${this.type}_${rr++}`, this.url = t, this.messageUrl = r || this.url, this.ES = e, this.conn = new e(t), this.sseInitTimeout = setTimeout(() => {
      this.initParams || this.handleError();
    }, 1e4), this.conn.onmessage = (s) => {
      const i = JSON.parse(s.data);
      if (Array.isArray(i))
        for (const o of i)
          this.handleMessage(o);
      else
        this.handleMessage(i);
    }, this.conn.onerror = (s) => {
      this.handleError();
    };
  }
  handleMessage(e) {
    if (e.op === "sse-init") {
      this.initParams = {
        machineId: e["machine-id"],
        sessionId: e["session-id"],
        sseToken: e["sse-token"]
      }, this.onopen && this.onopen({ target: this }), clearTimeout(this.sseInitTimeout);
      return;
    }
    this.onmessage && this.onmessage({
      target: this,
      message: e
    });
  }
  // Runs the onerror and closes the connection
  handleError() {
    try {
      this.onerror && this.onerror({ target: this });
    } finally {
      this.handleClose();
    }
  }
  handleClose() {
    this.conn.close(), this.onclose && !this.closeFired && (this.closeFired = !0, this.onclose({ target: this }));
  }
  async postMessages(e) {
    var t, r, s;
    try {
      (await fetch(this.messageUrl, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          machine_id: (t = this.initParams) == null ? void 0 : t.machineId,
          session_id: (r = this.initParams) == null ? void 0 : r.sessionId,
          sse_token: (s = this.initParams) == null ? void 0 : s.sseToken,
          messages: e
        })
      })).ok || this.handleError();
    } catch {
      this.handleError();
    }
  }
  async flushQueue() {
    if (this.sendPromise || !this.sendQueue.length) return;
    const e = this.sendQueue;
    this.sendQueue = [];
    const t = this.postMessages(e);
    this.sendPromise = t, t.then(() => {
      this.sendPromise = null, this.flushQueue();
    });
  }
  send(e) {
    if (!this.isOpen() || !this.initParams)
      throw this.isConnecting() ? new Error(
        "Failed to execute 'send' on 'EventSource': Still in CONNECTING state."
      ) : this.conn.readyState === this.ES.CLOSED ? new Error("EventSource is already in CLOSING or CLOSED state.") : new Error("EventSource is in invalid state.");
    this.sendQueue.push(e), this.flushQueue();
  }
  isOpen() {
    return this.conn.readyState === this.ES.OPEN && this.initParams !== null;
  }
  isConnecting() {
    return this.conn.readyState === this.ES.CONNECTING || this.conn.readyState === this.ES.OPEN && this.initParams === null;
  }
  close() {
    this.handleClose();
  }
}
function eo(n, e) {
  const t = n.values;
  if (t) {
    const r = En(t.attrsStore, null);
    if (r) {
      for (const s of t.entities || [])
        s.store.useDateObjects = e, s.store = Cn(r, s.store);
      t.attrsStore = r;
    }
  }
  return n;
}
function to(n, e) {
  var t;
  if (e.values) {
    const r = [];
    for (const s of (t = e.values) == null ? void 0 : t.entities) {
      const i = In(s.store);
      r.push({ ...s, store: i });
    }
    return {
      ...e,
      values: { attrsStore: e.values.attrsStore.toJSON(), entities: r }
    };
  } else
    return e;
}
function no(n, e, t) {
  var i, o;
  const r = (i = e == null ? void 0 : e.state) == null ? void 0 : i.txId, s = (o = t == null ? void 0 : t.state) == null ? void 0 : o.txId;
  return r && (!s || r > s) ? e : s && (!r || s > r) ? t : e || t;
}
function rt(n, e, t) {
  return Rt(
    { store: e, attrsStore: t, pageInfo: null, aggregate: null },
    n.query
  ).data[n.table][0];
}
function fn(n, e, t, r) {
  var o;
  const s = (o = $(t, n.table, "id")) == null ? void 0 : o.id;
  if (!s)
    return -1;
  const i = _e(e.eav, [r, s, r]);
  return i ? i[3] : -1;
}
function dn(n, e, t) {
  for (const { action: r, triple: s } of t)
    switch (r) {
      case "added":
        jn(n, e, s);
        break;
      case "removed":
        Mn(n, e, s);
        break;
    }
}
function ro(n, e, t) {
  var s, i;
  const r = {};
  for (const { action: o, triple: a } of t) {
    const [c, u, l] = a, f = (i = (s = e.getAttr(u)) == null ? void 0 : s["forward-identity"]) == null ? void 0 : i[2];
    if (!f) continue;
    const d = r[c] ?? {};
    r[c] = d;
    const p = d[f] ?? {};
    switch (o) {
      case "added":
        p.newValue = l;
        break;
      case "removed":
        p.oldValue === void 0 && (p.oldValue = l);
        break;
    }
    d[f] = p;
  }
  for (const [o, a] of Object.entries(r))
    for (const [c, { oldValue: u, newValue: l }] of Object.entries(a))
      u === l && delete a[c];
  return r;
}
function Re(n, e) {
  return { [n.table]: e.map((t) => t.entity) };
}
function so(n, e) {
  var r;
  if (n.orderFieldType)
    return n.orderFieldType;
  const t = n.orderField === "serverCreatedAt" ? "number" : (r = $(e(), n.table, n.orderField)) == null ? void 0 : r["checked-data-type"];
  return n.orderFieldType = t, t;
}
function io(n, e, t) {
  const r = e;
  if (n.orderField === "serverCreatedAt") {
    t.sort(
      n.orderDirection === "asc" ? function(o, a) {
        return ve(
          o.entity.id,
          o.serverCreatedAt,
          a.entity.id,
          a.serverCreatedAt,
          r
        );
      } : function(o, a) {
        return ve(
          a.entity.id,
          a.serverCreatedAt,
          o.entity.id,
          o.serverCreatedAt,
          r
        );
      }
    );
    return;
  }
  const s = n.orderField;
  t.sort(
    n.orderDirection === "asc" ? function(o, a) {
      return ve(
        o.entity.id,
        o.entity[s],
        a.entity.id,
        a.entity[s],
        r
      );
    } : function(o, a) {
      return ve(
        a.entity.id,
        a.entity[s],
        o.entity.id,
        o.entity[s],
        r
      );
    }
  );
}
var oo = /* @__PURE__ */ ((n) => (n.InitialSyncBatch = "InitialSyncBatch", n.InitialSyncComplete = "InitialSyncComplete", n.LoadFromStorage = "LoadFromStorage", n.SyncTransaction = "SyncTransaction", n.Error = "Error", n))(oo || {});
class ao {
  constructor(e, t, r, s, i, o) {
    h(this, "trySend");
    h(this, "subs");
    // Using any for the SyncCallback because we'd need Reactor to be typed
    h(this, "callbacks", {});
    h(this, "config");
    h(this, "idToHash", {});
    h(this, "log");
    h(this, "createStore");
    h(this, "getAttrs");
    this.trySend = e, this.config = r, this.log = s, this.createStore = i, this.getAttrs = o, this.subs = new wt({
      persister: t,
      merge: no,
      serialize: to,
      parse: (a, c) => eo(c, this.config.useDateObjects),
      objectSize: (a) => {
        var c;
        return ((c = a.values) == null ? void 0 : c.entities.length) || 0;
      },
      logger: s,
      gc: {
        maxAgeMs: 1e3 * 60 * 60 * 24 * 7 * 52,
        // 1 year
        maxEntries: 1e3,
        // Size of each sub is the number of entity
        maxSize: 1e6
        // 1 million entities
      }
    });
  }
  beforeUnload() {
    this.subs.flush();
  }
  subscribe(e, t) {
    const r = R(e);
    return this.callbacks[r] = this.callbacks[r] || [], this.callbacks[r].push(t), this.initSubscription(e, r, t), (s) => {
      this.unsubscribe(r, t, s == null ? void 0 : s.keepSubscription);
    };
  }
  unsubscribe(e, t, r) {
    const s = (this.callbacks[e] || []).filter((i) => i !== t);
    if (this.callbacks[e] = s, !s.length) {
      delete this.callbacks[e];
      const i = this.subs.currentValue[e];
      i != null && i.state && this.clearSubscriptionData(
        i.state.subscriptionId,
        !!r
      ), r || this.subs.updateInPlace((o) => {
        delete o[e];
      });
    }
  }
  sendStart(e) {
    this.trySend(M(), {
      op: "start-sync",
      q: e
    });
  }
  sendResync(e, t, r) {
    this.idToHash[t.subscriptionId] = e.hash, this.trySend(t.subscriptionId, {
      op: "resync-table",
      "subscription-id": t.subscriptionId,
      "tx-id": r,
      token: t.token
    });
  }
  sendRemove(e, t) {
    this.trySend(M(), {
      op: "remove-sync",
      "subscription-id": e.subscriptionId,
      "keep-subscription": t
    });
  }
  async initSubscription(e, t, r) {
    var u, l, f, d;
    await this.subs.waitForKeyToLoad(t);
    const s = this.subs.currentValue[t];
    if (s && s.state && s.state.txId) {
      this.sendResync(s, s.state, s.state.txId), (u = s.values) != null && u.entities && r && r({
        type: "LoadFromStorage",
        data: Re(s, (l = s.values) == null ? void 0 : l.entities)
      });
      return;
    }
    const i = Object.keys(e)[0], o = ((d = (f = e[i]) == null ? void 0 : f.$) == null ? void 0 : d.order) || { serverCreatedAt: "asc" }, [a, c] = Object.entries(o)[0];
    this.subs.updateInPlace((p) => {
      p[t] = {
        query: e,
        hash: t,
        table: i,
        orderDirection: c,
        orderField: a,
        createdAt: Date.now(),
        updatedAt: Date.now()
      };
    }), this.sendStart(e);
  }
  async flushPending() {
    for (const e of Object.keys(this.callbacks)) {
      await this.subs.waitForKeyToLoad(e);
      const t = this.subs.currentValue[e];
      t ? await this.initSubscription(t.query, t.hash) : this.log.error("Missing sub for hash in flushPending", e);
    }
  }
  onStartSyncOk(e) {
    const t = e["subscription-id"], r = e.q, s = R(r);
    this.idToHash[t] = s, this.subs.updateInPlace((i) => {
      const o = i[s];
      if (!o)
        return this.log.error(
          "Missing sub for hash",
          s,
          "subscription-id",
          t,
          "query",
          r
        ), i;
      o.state = {
        subscriptionId: t,
        token: e.token
      };
    });
  }
  notifyCbs(e, t) {
    for (const r of this.callbacks[e] || [])
      r(t);
  }
  onSyncLoadBatch(e) {
    const t = e["subscription-id"], r = e["join-rows"], s = this.idToHash[t];
    if (!s) {
      this.log.error("Missing hash for subscription", e);
      return;
    }
    const i = [], o = this.subs.currentValue[s];
    if (!o) {
      this.log.error("Missing sub for hash", s, e);
      return;
    }
    const a = o.values ?? {
      entities: [],
      attrsStore: this.getAttrs()
    };
    o.values = a;
    const c = a.entities;
    for (const u of r) {
      const l = this.createStore(u), f = rt(o, l, a.attrsStore);
      c.push({
        store: l,
        entity: f,
        serverCreatedAt: fn(
          o,
          l,
          a.attrsStore,
          f.id
        )
      }), i.push(f);
    }
    this.subs.updateInPlace((u) => {
      u[s] = o, u[s].updatedAt = Date.now();
    }), o.values && this.notifyCbs(s, {
      type: "InitialSyncBatch",
      data: Re(o, o.values.entities),
      batch: i
    });
  }
  onSyncInitFinish(e) {
    var i;
    const t = e["subscription-id"], r = this.idToHash[t];
    if (!r) {
      this.log.error("Missing hash for subscription", e);
      return;
    }
    this.subs.updateInPlace((o) => {
      const a = o[r];
      if (!a) {
        this.log.error("Missing sub for hash", r, e);
        return;
      }
      const c = a.state;
      if (!c)
        return this.log.error("Sub never set init, missing result", a, e), o;
      c.txId = e["tx-id"], a.updatedAt = Date.now();
    });
    const s = this.subs.currentValue[r];
    s && this.notifyCbs(r, {
      type: "InitialSyncComplete",
      data: Re(s, ((i = s.values) == null ? void 0 : i.entities) || [])
    });
  }
  onSyncUpdateTriples(e) {
    var o;
    const t = e["subscription-id"], r = this.idToHash[t];
    if (!r) {
      this.log.error("Missing hash for subscription", e);
      return;
    }
    const s = this.subs.currentValue[r];
    if (!s) {
      this.log.error("Missing sub for hash", r, e);
      return;
    }
    const i = s.state;
    if (!i) {
      this.log.error("Missing state for sub", s, e);
      return;
    }
    for (const a of e.txes) {
      if (i.txId && i.txId >= a["tx-id"])
        continue;
      i.txId = a["tx-id"];
      const c = [], u = {};
      for (const _ of a.changes) {
        const w = u[_.triple[0]] ?? [];
        u[_.triple[0]] = w, w.push(_);
      }
      const l = s.values ?? {
        entities: [],
        attrsStore: this.getAttrs()
      }, f = l.entities;
      s.values = l;
      const d = [];
      e: for (const [_, w] of Object.entries(u))
        for (let b = 0; b < f.length; b++) {
          const g = f[b];
          if (Nr(g.store, _)) {
            dn(g.store, l.attrsStore, w);
            const k = rt(s, g.store, l.attrsStore), T = ro(
              g.store,
              l.attrsStore,
              w
            )[_];
            k ? (d.push({
              oldEntity: g.entity,
              newEntity: k,
              changedFields: T || {}
            }), g.entity = k) : c.push(b), delete u[_];
            continue e;
          }
        }
      const p = [];
      for (const [_, w] of Object.entries(u)) {
        const b = this.createStore([]);
        dn(b, l.attrsStore, w);
        const g = rt(s, b, l.attrsStore);
        if (!g) {
          this.log.error("No entity found after applying change", {
            sub: s,
            changes: w,
            store: b
          });
          continue;
        }
        f.push({
          store: b,
          entity: g,
          serverCreatedAt: fn(
            s,
            b,
            l.attrsStore,
            g.id
          )
        }), p.push(g);
      }
      const y = [];
      for (const _ of c.sort().reverse())
        y.push(f[_].entity), f.splice(_, 1);
      const m = so(s, this.getAttrs);
      io(s, m, f), this.notifyCbs(r, {
        type: "SyncTransaction",
        data: Re(s, (o = s.values) == null ? void 0 : o.entities),
        added: p,
        removed: y,
        updated: d
      });
    }
    this.subs.updateInPlace((a) => {
      a[r] = s, a[r].updatedAt = Date.now();
    });
  }
  clearSubscriptionData(e, t) {
    const r = this.idToHash[e];
    if (r) {
      delete this.idToHash[e];
      const s = this.subs.currentValue[r];
      if (s.state && this.sendRemove(s.state, t), t ? this.subs.unloadKey(r) : this.subs.updateInPlace((i) => {
        delete i[r];
      }), s)
        return s;
    }
  }
  onStartSyncError(e) {
    const t = R(e["original-event"].q), r = {
      message: e.message || "Uh-oh, something went wrong. Ping Joe & Stopa.",
      status: e.status,
      type: e.type,
      hint: e.hint
    }, s = Object.keys(e["original-event"].q)[0];
    this.notifyCbs(t, {
      type: "Error",
      data: { [s]: [] },
      error: r
    });
  }
  onResyncError(e) {
    const t = e["original-event"]["subscription-id"], r = this.clearSubscriptionData(t, !1);
    r && this.initSubscription(r.query, r.hash);
  }
}
function co({
  WStream: n,
  opts: e,
  startStream: t,
  appendStream: r,
  registerStream: s
}) {
  const i = e.clientId;
  let o = null, a = null;
  const c = M();
  let u = !1, l = !1;
  const f = [], d = [], p = [];
  let y = !1, m = 0, _ = 0;
  const w = [], b = new TextEncoder();
  function g() {
    l = !0;
    for (const A of f)
      A();
  }
  function k(A) {
    return f.push(A), () => {
      const I = f.indexOf(A);
      I !== -1 && f.splice(I, 1);
    };
  }
  function T(A) {
    return p.push(A), () => {
      const I = p.indexOf(A);
      I !== -1 && p.splice(I, 1);
    };
  }
  e.waitUntil && e.waitUntil(
    new Promise((A) => {
      p.push(A);
    })
  );
  function S() {
    for (const A of p)
      try {
        A();
      } catch {
      }
  }
  function v(A) {
    return d.push(A), () => {
      const I = d.indexOf(A);
      I !== -1 && d.splice(I, 1);
    };
  }
  function E(A) {
    o = A;
    for (const I of d)
      I(o);
  }
  function D() {
    y = !0;
  }
  function ue(A) {
    let I = m, L = 0, q = 0;
    for (const { byteLen: J } of w) {
      const se = I + J;
      if (se > A)
        break;
      I = se, L++, q += J;
    }
    L > 0 && (m += q, _ -= q, w.splice(0, L));
  }
  function ne(A, I) {
    g(), A.error(I), S();
  }
  async function re() {
    const A = await t({
      clientId: i,
      reconnectToken: c,
      ruleParams: e.ruleParams
    });
    switch (A.type) {
      case "ok": {
        const { streamId: I, offset: L } = A;
        o = I, ue(L), w.length && r({
          streamId: I,
          chunks: w.map((q) => q.chunk),
          offset: m
        }), y = !1;
        break;
      }
      case "disconnect": {
        D();
        break;
      }
      case "error": {
        a && ne(a, A.error);
        break;
      }
    }
  }
  function H() {
    D(), re();
  }
  function U({ offset: A, done: I }) {
    ue(A), I && (u = !0, S());
  }
  function ae(A) {
    return u ? (ne(A, new V("Stream has been closed.")), null) : o || (ne(A, new V("Stream has not been initialized.")), null);
  }
  async function Pe(A) {
    a = A;
    let I = !0, L = 0;
    for (; I; ) {
      let q = Date.now() + Math.min(15e3, 500 * (L - 1));
      I = !1;
      const J = await t({
        clientId: e.clientId,
        reconnectToken: c,
        ruleParams: e.ruleParams
      });
      switch (J.type) {
        case "ok": {
          const { streamId: se, offset: Ze } = J;
          if (Ze !== 0) {
            const we = new V("Write stream is corrupted");
            ne(A, we);
            return;
          }
          E(se), s(se, {
            onDisconnect: D,
            onFlush: U,
            onConnectionReconnect: re,
            onAppendFailed: H
          }), y = !1;
          return;
        }
        case "disconnect": {
          I = !0, D(), L++, await new Promise((se) => {
            setTimeout(se, q - Date.now());
          });
          break;
        }
        case "error": {
          ne(A, J.error);
          return;
        }
      }
    }
  }
  class je extends n {
    constructor(I, L) {
      super(I, L);
    }
    async streamId() {
      return o || new Promise((I, L) => {
        const q = [], J = () => {
          for (const we of q)
            we();
        }, se = (we) => {
          I(we), J();
        }, Ze = () => {
          L(new V("Stream is closed.")), J();
        };
        q.push(v(se)), q.push(k(Ze));
      });
    }
  }
  return {
    stream: new je({
      // TODO(dww): accept a storage so that write streams can survive across
      //            browser restarts
      async start(A) {
        try {
          await Pe(A);
        } catch (I) {
          ne(A, I);
        }
      },
      write(A, I) {
        const L = ae(I);
        if (L) {
          const q = b.encode(A).length;
          w.push({ chunk: A, byteLen: q });
          const J = m + _;
          _ += q, y || r({ streamId: L, chunks: [A], offset: J });
        }
      },
      close() {
        o ? r({
          streamId: o,
          chunks: [],
          offset: m + _,
          isDone: !0
        }) : S(), g();
      },
      abort(A) {
        o ? r({
          streamId: o,
          chunks: [],
          offset: m + _,
          isDone: !0,
          abortReason: A
        }) : S(), g();
      }
    }),
    addCompleteCb: T,
    closed() {
      return l;
    }
  };
}
class uo {
  constructor() {
    h(this, "items", []);
    h(this, "resolvers", []);
    h(this, "isClosed", !1);
  }
  push(e) {
    if (this.isClosed) return;
    const t = this.resolvers.shift();
    t ? t({ value: e, done: !1 }) : this.items.push(e);
  }
  close() {
    for (this.isClosed = !0; this.resolvers.length > 0; )
      this.resolvers.shift()({ value: void 0, done: !0 });
  }
  async *[Symbol.asyncIterator]() {
    for (; ; )
      if (this.items.length > 0)
        yield this.items.shift();
      else {
        if (this.isClosed)
          return;
        {
          const { value: e, done: t } = await new Promise((r) => {
            this.resolvers.push(r);
          });
          if (t || !e)
            return;
          yield e;
        }
      }
  }
}
function lo({
  RStream: n,
  opts: e,
  startStream: t,
  cancelStream: r
}) {
  let s = e.byteOffset || 0, i = !1;
  const o = new TextDecoder("utf-8"), a = new TextEncoder();
  let c, u = !1;
  const l = [];
  function f() {
    u = !0;
    for (const b of l)
      b();
  }
  function d(b) {
    return l.push(b), () => {
      const g = l.indexOf(b);
      g !== -1 && l.splice(g, 1);
    };
  }
  function p(b, g) {
    b.error(g), f();
  }
  let y = 0;
  async function m(b, g) {
    c = M();
    const k = { ...b || {}, eventId: c };
    for await (const T of t(k)) {
      if (i)
        return;
      if (T.type === "reconnect")
        return { retry: !0 };
      if (T.type === "error") {
        p(g, T.error);
        return;
      }
      if (T.offset > s) {
        p(g, new V("Stream is corrupted.")), i = !0;
        return;
      }
      let S = s - T.offset;
      if (T.files && T.files.length) {
        const v = new AbortController();
        let E = fetch(T.files[0].url, {
          signal: v.signal
        });
        for (let D = 0; D < T.files.length; D++) {
          const ue = T.files[D + 1], re = await E;
          if (ue && (E = fetch(ue.url, { signal: v.signal })), !re.ok) {
            if (y++, y > 10) {
              p(g, new V("Unable to process stream."));
              return;
            }
            return { retry: !0 };
          }
          if (re.body)
            for await (const H of re.body) {
              if (i) {
                v.abort();
                return;
              }
              let U = H;
              if (S > 0 && (U = H.subarray(S), S -= H.length - U.length), !U.length)
                continue;
              s += U.length;
              const ae = o.decode(U);
              g.enqueue(ae);
            }
          else {
            const H = await re.arrayBuffer();
            let U = H;
            if (i) {
              v.abort();
              return;
            }
            if (S > 0 && (U = new Uint8Array(H).subarray(S), S -= H.byteLength - U.length), !U.byteLength)
              continue;
            s += U.byteLength;
            const ae = o.decode(U);
            g.enqueue(ae);
          }
        }
      }
      if (y = 0, T.content) {
        let v = T.content, E = a.encode(T.content);
        if (S > 0) {
          const D = E.subarray(S);
          if (S -= E.length - D.length, !D.length)
            continue;
          E = D, v = o.decode(D);
        }
        s += E.length, g.enqueue(v);
      }
    }
  }
  async function _(b) {
    let g = !0, k = 0;
    for (; g; ) {
      g = !1;
      let T = Date.now() + Math.min(15e3, 500 * (k - 1));
      const S = await m(
        { ...e, offset: s },
        b
      );
      S != null && S.retry && (g = !0, k++, T < Date.now() - 3e5 && (k = 0), await new Promise((v) => {
        setTimeout(v, T - Date.now());
      }));
    }
    !i && !u && (b.close(), f());
  }
  return {
    stream: new n({
      start(b) {
        _(b);
      },
      cancel(b) {
        i = !0, c && r({ eventId: c }), f();
      }
    }),
    addCloseCb: d,
    closed() {
      return u;
    }
  };
}
class fo {
  constructor({
    WStream: e,
    RStream: t,
    trySend: r,
    log: s
  }) {
    h(this, "trySend");
    h(this, "WStream");
    h(this, "RStream");
    h(this, "writeStreams", {});
    h(this, "startWriteStreamCbs", {});
    h(this, "readStreamIterators", {});
    h(this, "log");
    h(this, "activeStreams", /* @__PURE__ */ new Set());
    this.WStream = e, this.RStream = t, this.trySend = r, this.log = s;
  }
  createWriteStream(e) {
    const { stream: t, addCompleteCb: r } = co({
      WStream: this.WStream,
      startStream: this.startWriteStream.bind(this),
      appendStream: this.appendStream.bind(this),
      registerStream: this.registerWriteStream.bind(this),
      opts: e
    });
    return this.activeStreams.add(t), r(() => {
      this.activeStreams.delete(t);
    }), t;
  }
  createReadStream(e) {
    const { stream: t, addCloseCb: r } = lo({
      RStream: this.RStream,
      opts: e,
      startStream: this.startReadStream.bind(this),
      cancelStream: this.cancelReadStream.bind(this)
    });
    return this.activeStreams.add(t), r(() => {
      this.activeStreams.delete(t);
    }), t;
  }
  startWriteStream(e) {
    const t = M();
    let r = null;
    const s = new Promise((o) => {
      r = o;
    });
    this.startWriteStreamCbs[t] = r;
    const i = {
      op: "start-stream",
      "client-id": e.clientId,
      "reconnect-token": e.reconnectToken
    };
    return e.ruleParams && (i["rule-params"] = e.ruleParams), this.trySend(t, i), s;
  }
  registerWriteStream(e, t) {
    this.writeStreams[e] = t;
  }
  appendStream({
    streamId: e,
    chunks: t,
    isDone: r,
    offset: s,
    abortReason: i
  }) {
    const o = {
      op: "append-stream",
      "stream-id": e,
      chunks: t,
      offset: s,
      done: !!r
    };
    i && (o["abort-reason"] = i), this.trySend(M(), o);
  }
  onAppendFailed(e) {
    const t = this.writeStreams[e["stream-id"]];
    t && t.onAppendFailed();
  }
  onStartStreamOk(e) {
    const t = this.startWriteStreamCbs[e["client-event-id"]];
    if (!t) {
      this.log.info("No stream for start-stream-ok", e);
      return;
    }
    t({ type: "ok", streamId: e["stream-id"], offset: e.offset }), delete this.startWriteStreamCbs[e["client-event-id"]];
  }
  onStreamFlushed(e) {
    const t = e["stream-id"], r = this.writeStreams[t];
    if (!r) {
      this.log.info("No stream cbs for stream-flushed", e);
      return;
    }
    r.onFlush({ offset: e.offset, done: e.done }), e.done && delete this.writeStreams[t];
  }
  startReadStream({
    eventId: e,
    clientId: t,
    streamId: r,
    offset: s,
    ruleParams: i
  }) {
    const o = { op: "subscribe-stream" };
    if (!r && !t)
      throw new Error(
        "Must provide one of streamId or clientId to subscribe to the stream."
      );
    r && (o["stream-id"] = r), t && (o["client-id"] = t), s && (o.offset = s), i && (o["rule-params"] = i);
    const a = new uo();
    return this.readStreamIterators[e] = a, this.trySend(e, o), a;
  }
  cancelReadStream({ eventId: e }) {
    const t = {
      op: "unsubscribe-stream",
      "subscribe-event-id": e
    };
    this.trySend(M(), t), delete this.readStreamIterators[e];
  }
  onStreamAppend(e) {
    var s;
    const t = e["client-event-id"], r = this.readStreamIterators[t];
    if (!r) {
      this.log.info("No iterator for read stream", e);
      return;
    }
    if (e.error) {
      e.retry ? r.push({ type: "reconnect" }) : r.push({
        type: "error",
        error: new V(e.error)
      }), r.close(), delete this.readStreamIterators[t];
      return;
    }
    ((s = e.files) != null && s.length || e.content) && r.push({
      type: "append",
      offset: e.offset,
      files: e.files,
      content: e.content
    }), e.done && (r.close(), delete this.readStreamIterators[t]);
  }
  onConnectionStatusChange(e) {
    for (const t of Object.values(this.startWriteStreamCbs))
      t({ type: "disconnect" });
    if (this.startWriteStreamCbs = {}, e !== ee.AUTHENTICATED)
      for (const { onDisconnect: t } of Object.values(this.writeStreams))
        t();
    else {
      for (const { onConnectionReconnect: t } of Object.values(
        this.writeStreams
      ))
        t();
      for (const t of Object.values(this.readStreamIterators))
        t.push({ type: "reconnect" }), t.close();
      this.readStreamIterators = {};
    }
  }
  onRecieveError(e) {
    const t = e["original-event"];
    switch (t.op) {
      case "append-stream": {
        const r = t["stream-id"], s = this.writeStreams[r];
        s == null || s.onAppendFailed();
        break;
      }
      case "start-stream": {
        const r = e["client-event-id"], s = this.startWriteStreamCbs[r];
        s && (s({
          type: "error",
          error: new V(e.message || "Unknown error", e.hint)
        }), delete this.startWriteStreamCbs[r]);
        break;
      }
      case "subscribe-stream": {
        const r = e["client-event-id"], s = this.readStreamIterators[r];
        s && (s.push({
          type: "error",
          error: new V(e.message || "Unknown error", e.hint)
        }), s.close(), delete this.readStreamIterators[r]);
        break;
      }
    }
  }
  hasActiveStreams() {
    return this.activeStreams.size > 0;
  }
}
const ee = {
  CONNECTING: "connecting",
  OPENED: "opened",
  AUTHENTICATED: "authenticated",
  CLOSED: "closed",
  ERRORED: "errored"
}, ho = 3e4, po = 3e4, yo = 200, bo = 1e3 * 60, mo = {
  apiURI: "https://api.instantdb.com",
  websocketURI: "wss://api.instantdb.com/runtime/session"
}, st = "_instant_oauth_redirect", Te = "currentUser";
function go({
  transportType: n,
  appId: e,
  apiURI: t,
  wsURI: r,
  EventSourceImpl: s
}) {
  if (!s)
    return new ln(`${r}?app_id=${e}`);
  switch (n) {
    case "ws":
      return new ln(`${r}?app_id=${e}`);
    case "sse":
      return new Xi(
        s,
        `${t}/runtime/sse?app_id=${e}`
      );
    default:
      throw new Error("Unknown transport type " + n);
  }
}
function _o() {
  return typeof window < "u" || typeof chrome < "u";
}
const hn = {
  "set-presence": !0,
  "set-presence-ok": !0,
  "refresh-presence": !0,
  "patch-presence": !0
};
function wo(n, e) {
  var r;
  const t = typeof n == "string" ? JSON.parse(n) : n;
  if ((r = t == null ? void 0 : t.result) != null && r.store) {
    const s = En(
      t.result.attrsStore,
      t.result.store
    );
    if (s) {
      const i = t.result.store;
      t.result.store = Cn(s, {
        ...i,
        useDateObjects: e
      }), t.result.attrsStore = s;
    }
  }
  return t;
}
function To(n, e) {
  const { result: t, ...r } = e, s = (
    /** @type {import('./reactorTypes.ts').QuerySubInStorage} */
    r
  );
  if (t) {
    const i = {
      ...t,
      store: In(t.store),
      attrsStore: t.attrsStore.toJSON()
    };
    s.result = i;
  }
  return s;
}
function So(n, e) {
  switch (n) {
    case "pendingMutations":
      return new Map(typeof e == "string" ? JSON.parse(e) : e);
    default:
      return e;
  }
}
function Ao(n, e) {
  switch (n) {
    case "pendingMutations":
      return [...e.entries()];
    default:
      return e;
  }
}
function vo(n, e, t) {
  const r = e == null ? void 0 : e.result, s = t == null ? void 0 : t.result;
  return r && !s && t && (t.result = r), t || e;
}
function it(n) {
  return [...n].sort((e, t) => {
    const [r, s] = e, [i, o] = t, a = s.order || 0, c = o.order || 0;
    return a == c ? r < i ? -1 : r > i ? 1 : 0 : a - c;
  });
}
class ko {
  constructor(e, t = Qn, r = Bn, s, i) {
    /** @type {s.AttrsStore | undefined} */
    h(this, "attrs");
    h(this, "_isOnline", !0);
    h(this, "_isShutdown", !1);
    h(this, "status", ee.CONNECTING);
    /** @type {PersistedObject<string, QuerySub, QuerySubInStorage>} */
    h(this, "querySubs");
    /** @type {PersistedObject} */
    h(this, "kv");
    /** @type {SyncTable} */
    h(this, "_syncTable");
    /** @type {InstantStream} */
    h(this, "_instantStream");
    /** @type {Record<string, Array<{ q: any, cb: (data: any) => any }>>} */
    h(this, "queryCbs", {});
    /** @type {Record<string, Array<{ q: any, eventId: string, dfd: Deferred }>>} */
    h(this, "queryOnceDfds", {});
    h(this, "authCbs", []);
    h(this, "attrsCbs", []);
    h(this, "mutationErrorCbs", []);
    h(this, "connectionStatusCbs", []);
    h(this, "config");
    h(this, "mutationDeferredStore", /* @__PURE__ */ new Map());
    h(this, "_reconnectTimeoutId", null);
    h(this, "_reconnectTimeoutMs", 0);
    /** @type {Connection} */
    h(this, "_transport");
    /** @type {TransportType} */
    h(this, "_transportType", "ws");
    /** @type {EventSourceConstructor} */
    h(this, "_EventSource");
    /** @type {boolean | null} */
    h(this, "_wsOk", null);
    h(this, "_localIdPromises", {});
    h(this, "_errorMessage", null);
    /** @type {Promise<null | {error: {message: string}}> | null}**/
    h(this, "_oauthCallbackResponse", null);
    /** @type {null | import('./utils/linkIndex.ts').LinkIndex}} */
    h(this, "_linkIndex", null);
    /** @type BroadcastChannel | undefined */
    h(this, "_broadcastChannel");
    /** @type {Record<string, {roomType: string; isConnected: boolean; error: any}>} */
    h(this, "_rooms", {});
    /** @type {Record<string, boolean>} */
    h(this, "_roomsPendingLeave", {});
    h(this, "_presence", {});
    h(this, "_broadcastQueue", []);
    h(this, "_broadcastSubs", {});
    /** @type {{isLoading: boolean; error: any | undefined, user: any | undefined}} */
    h(this, "_currentUserCached", { isLoading: !0, error: void 0, user: void 0 });
    h(this, "_beforeUnloadCbs", []);
    h(this, "_dataForQueryCache", {});
    /** @type {Logger} */
    h(this, "_log");
    h(this, "_pendingTxCleanupTimeout");
    h(this, "_pendingMutationCleanupThreshold");
    h(this, "_inFlightMutationEventIds", /* @__PURE__ */ new Set());
    h(this, "_onMergeKv", (e, t, r) => {
      switch (e) {
        case "pendingMutations": {
          const s = (t == null ? void 0 : t.entries()) ?? [], i = (r == null ? void 0 : r.entries()) ?? [], o = new Map([...s, ...i]);
          return (t ? this._rewriteMutationsSorted(this.attrs, t) : []).forEach(([c, u]) => {
            var l;
            !((l = r == null ? void 0 : r.pendingMutations) != null && l.has(c)) && !u["tx-id"] && this._sendMutation(c, u);
          }), o;
        }
        default:
          return r || t;
      }
    });
    // ---------------------------
    // Queries
    h(this, "getPreviousResult", (e) => {
      var r;
      const t = R(e);
      return (r = this.dataForQuery(t)) == null ? void 0 : r.data;
    });
    /** Re-run instaql and call all callbacks with new data */
    h(this, "notifyOne", (e) => {
      var i;
      const t = this.queryCbs[e] ?? [], r = (i = this._dataForQueryCache[e]) == null ? void 0 : i.data, s = this.dataForQuery(e);
      s != null && s.data && (this._dataForQueryCache[e] = s, !Ne(s.data, r) && t.forEach((o) => o.cb(s.data)));
    });
    h(this, "notifyOneQueryOnce", (e) => {
      var s;
      const t = this.queryOnceDfds[e] ?? [], r = (s = this.dataForQuery(e)) == null ? void 0 : s.data;
      t.forEach((i) => {
        this._completeQueryOnce(i.q, e, i.dfd), i.dfd.resolve(r);
      });
    });
    h(this, "notifyQueryError", (e, t) => {
      (this.queryCbs[e] || []).forEach((s) => s.cb({ error: t }));
    });
    /** Applies transactions locally and sends transact message to server */
    h(this, "pushTx", (e) => {
      this.config.disableValidation || Zi(e, this.config.schema);
      try {
        const t = Ai(
          {
            attrsStore: this.optimisticAttrs(),
            schema: this.config.schema,
            stores: Object.values(this.querySubs.currentValue).map(
              (r) => {
                var s;
                return (s = r == null ? void 0 : r.result) == null ? void 0 : s.store;
              }
            ),
            useDateObjects: this.config.useDateObjects
          },
          e
        );
        return this.pushOps(t);
      } catch (t) {
        return this.pushOps([], t);
      }
    });
    /**
     * @param {*} txSteps
     * @param {*} [error]
     * @returns
     */
    h(this, "pushOps", (e, t) => {
      const r = M(), s = [...this._pendingMutations().values()], i = Math.max(0, ...s.map((c) => c.order || 0)) + 1, o = {
        op: "transact",
        "tx-steps": e,
        created: Date.now(),
        error: t,
        order: i
      };
      this._updatePendingMutations((c) => {
        c.set(r, o);
      });
      const a = new rn();
      return this.mutationDeferredStore.set(r, a), this._sendMutation(r, o), this.notifyAll(), a.promise;
    });
    h(this, "_transportOnOpen", (e) => {
      const t = e.target;
      if (this._transport !== t) {
        this._log.info(
          "[socket][open]",
          t.id,
          "skip; this is no longer the current transport"
        );
        return;
      }
      this._log.info("[socket][open]", this._transport.id), this._setStatus(ee.OPENED), this.getCurrentUser().then((r) => {
        var s;
        this._trySend(M(), {
          op: "init",
          "app-id": this.config.appId,
          "refresh-token": (s = r.user) == null ? void 0 : s.refresh_token,
          versions: this.versions,
          // If an admin token is provided for an app, we will
          // skip all permission checks. This is an advanced feature,
          // to let users write internal tools
          // This option is not exposed in `Config`, as it's
          // not ready for prime time
          "__admin-token": this.config.__adminToken
        });
      }).catch((r) => {
        this._log.error("[socket][error]", t.id, r);
      });
    });
    h(this, "_transportOnMessage", (e) => {
      const t = e.target, r = e.message;
      if (this._transport !== t) {
        this._log.info(
          "[socket][message]",
          t.id,
          r,
          "skip; this is no longer the current transport"
        );
        return;
      }
      if (!this._wsOk && t.type === "ws" && (this._wsOk = !0), this._transportType = "ws", Array.isArray(e.message))
        for (const s of e.message)
          this._handleReceive(t.id, s);
      else
        this._handleReceive(t.id, e.message);
    });
    h(this, "_transportOnError", (e) => {
      const t = e.target;
      if (this._transport !== t) {
        this._log.info(
          "[socket][error]",
          t.id,
          "skip; this is no longer the current transport"
        );
        return;
      }
      this._log.error("[socket][error]", t.id, e);
    });
    h(this, "_scheduleReconnect", () => {
      !this._wsOk && this._transportType !== "sse" && (this._transportType = "sse", this._reconnectTimeoutMs = 0), setTimeout(() => {
        if (this._reconnectTimeoutMs = Math.min(
          this._reconnectTimeoutMs + 1e3,
          1e4
        ), !this._isOnline) {
          this._log.info(
            "[socket][close]",
            this._transport.id,
            "we are offline, no need to start socket"
          );
          return;
        }
        this._startSocket();
      }, this._reconnectTimeoutMs);
    });
    h(this, "_transportOnClose", (e) => {
      const t = e.target;
      if (this._transport !== t) {
        this._log.info(
          "[socket][close]",
          t.id,
          "skip; this is no longer the current transport"
        );
        return;
      }
      this._setStatus(ee.CLOSED);
      for (const r of Object.values(this._rooms))
        r.isConnected = !1;
      if (this._isShutdown) {
        this._log.info(
          "[socket][close]",
          t.id,
          "Reactor has been shut down and will not reconnect"
        );
        return;
      }
      this._log.info(
        "[socket][close]",
        t.id,
        "schedule reconnect, ms =",
        this._reconnectTimeoutMs
      ), this._scheduleReconnect();
    });
    if (this._EventSource = i, this.config = { ...mo, ...e }, this.queryCacheLimit = this.config.queryCacheLimit ?? 10, this._pendingTxCleanupTimeout = this.config.pendingTxCleanupTimeout ?? po, this._pendingMutationCleanupThreshold = this.config.pendingMutationCleanupThreshold ?? yo, this._log = zi(
      e.verbose || xt || Gn,
      () => this._reactorStats()
    ), this.versions = { ...s || {}, "@instantdb/core": Yn }, this.config.schema && (this._linkIndex = Tt(this.config.schema)), !!_o()) {
      if (!e.appId)
        throw new Error("Instant must be initialized with an appId.");
      if (!me(e.appId))
        throw new Error(
          `Instant must be initialized with a valid appId. \`${e.appId}\` is not a valid uuid.`
        );
      typeof BroadcastChannel == "function" && (this._broadcastChannel = new BroadcastChannel("@instantdb"), this._broadcastChannel.addEventListener("message", async (o) => {
        var a;
        try {
          if (((a = o.data) == null ? void 0 : a.type) === "auth") {
            const c = await this.getCurrentUser();
            await this.updateUser(c.user).catch((u) => {
              this._log.error("[error] update user", u);
            });
          }
        } catch (c) {
          this._log.error("[error] handle broadcast channel", c);
        }
      })), this._initStorage(t), this._syncTable = new ao(
        this._trySendAuthed.bind(this),
        new t(this.config.appId, "syncSubs"),
        {
          useDateObjects: this.config.useDateObjects
        },
        this._log,
        (o) => ye(
          this.ensureAttrs(),
          o,
          this.config.enableCardinalityInference,
          this.config.useDateObjects
        ),
        () => this.ensureAttrs()
      ), this._instantStream = new fo({
        WStream: this.config.WritableStream || WritableStream,
        RStream: this.config.ReadableStream || ReadableStream,
        trySend: this._trySendAuthed.bind(this),
        log: this._log
      }), this._oauthCallbackResponse = this._oauthLoginInit(), this.getCurrentUser().then((o) => {
        this.syncUserToEndpoint(o.user);
      }), setInterval(async () => {
        const o = await this.getCurrentUser();
        this.syncUserToEndpoint(o.user);
      }, bo), r.getIsOnline().then((o) => {
        this._isOnline = o, this._startSocket(), r.listen((a) => {
          a !== this._isOnline && (this._log.info("[network] online =", a), this._isOnline = a, this._isOnline ? this._startSocket() : (this._log.info(
            "Changing status from",
            this.status,
            "to",
            ee.CLOSED
          ), this._setStatus(ee.CLOSED)));
        });
      }), typeof addEventListener < "u" && (this._beforeUnload = this._beforeUnload.bind(this), addEventListener("beforeunload", this._beforeUnload));
    }
  }
  ensureAttrs() {
    if (!this.attrs)
      throw new Error("attrs have not loaded.");
    return this.attrs;
  }
  updateSchema(e) {
    this.config = {
      ...this.config,
      schema: e,
      cardinalityInference: !!e
    }, this._linkIndex = e ? Tt(this.config.schema) : null;
  }
  _reactorStats() {
    return {
      inFlightMutationCount: this._inFlightMutationEventIds.size,
      storedMutationCount: this._pendingMutations().size,
      transportType: this._transportType
    };
  }
  _onQuerySubLoaded(e) {
    this.kv.waitForKeyToLoad("pendingMutations").then(() => this.notifyOne(e));
  }
  _initStorage(e) {
    this.querySubs = new wt({
      persister: new e(this.config.appId, "querySubs"),
      merge: vo,
      serialize: To,
      parse: (t, r) => wo(r, this.config.useDateObjects),
      // objectSize
      objectSize: (t) => {
        var r, s, i;
        return ((i = (s = (r = t == null ? void 0 : t.result) == null ? void 0 : r.store) == null ? void 0 : s.triples) == null ? void 0 : i.length) ?? 0;
      },
      logger: this._log,
      preloadEntryCount: 10,
      gc: {
        maxAgeMs: 1e3 * 60 * 60 * 24 * 7 * 52,
        // 1 year
        maxEntries: 1e3,
        // Size of each query is the number of triples
        maxSize: 1e6
        // 1 million triples
      }
    }), this.querySubs.onKeyLoaded = (t) => this._onQuerySubLoaded(t), this.kv = new wt({
      persister: new e(this.config.appId, "kv"),
      merge: this._onMergeKv,
      serialize: Ao,
      parse: So,
      objectSize: () => 0,
      logger: this._log,
      saveThrottleMs: 100,
      idleCallbackMaxWaitMs: 100,
      // Don't GC the kv store
      gc: null
    }), this.kv.onKeyLoaded = (t) => {
      t === "pendingMutations" && this.notifyAll();
    }, this.kv.waitForKeyToLoad("pendingMutations"), this.kv.waitForKeyToLoad(Te), this._beforeUnloadCbs.push(() => {
      this.kv.flush(), this.querySubs.flush();
    });
  }
  _beforeUnload() {
    for (const e of this._beforeUnloadCbs)
      e();
    this._syncTable.beforeUnload();
  }
  /**
   * @param {'enqueued' | 'pending' | 'synced' | 'timeout' |  'error' } status
   * @param {string} eventId
   * @param {{message?: string, type?: string, status?: number, hint?: unknown}} [errorMsg]
   */
  _finishTransaction(e, t, r) {
    const s = this.mutationDeferredStore.get(t);
    this.mutationDeferredStore.delete(t);
    const i = e !== "error" && e !== "timeout";
    if (!s && !i && console.error("Mutation failed", { status: e, eventId: t, ...r }), !!s)
      if (i)
        s.resolve({ status: e, eventId: t });
      else if (r != null && r.type) {
        const { status: o, ...a } = r;
        s.reject(
          new He({
            // @ts-expect-error body.type is not constant typed
            body: a,
            status: o ?? 0
          })
        );
      } else
        s.reject(
          new V(
            (r == null ? void 0 : r.message) || "Unknown error",
            r == null ? void 0 : r.hint
          )
        );
  }
  _setStatus(e, t) {
    this.status = e, this._errorMessage = t, this.notifyConnectionStatusSubs(e), this._instantStream.onConnectionStatusChange(e);
  }
  _flushEnqueuedRoomData(e) {
    var s, i;
    const t = (i = (s = this._presence[e]) == null ? void 0 : s.result) == null ? void 0 : i.user, r = this._broadcastQueue[e];
    if (this._broadcastQueue[e] = [], t && this._trySetPresence(e, t), r)
      for (const o of r) {
        const { topic: a, roomType: c, data: u } = o;
        this._tryBroadcast(e, c, a, u);
      }
  }
  /**
   * Does the same thing as add-query-ok
   * but called as a result of receiving query info from ssr
   * @param {any} q
   * @param {{ triples: any; pageInfo: any; }} result
   * @param {boolean} enableCardinalityInference
   */
  _addQueryData(e, t, r) {
    if (!this.attrs)
      throw new Error("Attrs in reactor have not been set");
    const s = R(e), i = this.ensureAttrs(), o = ye(
      this.attrs,
      t.triples,
      r,
      this.config.useDateObjects
    );
    this.querySubs.updateInPlace((a) => {
      a[s] = {
        result: {
          store: o,
          attrsStore: i,
          pageInfo: t.pageInfo,
          processedTxId: void 0,
          isExternal: !0
        },
        q: e
      };
    }), this._cleanupPendingMutationsQueries(), this.notifyOne(s), this.notifyOneQueryOnce(s), this._cleanupPendingMutationsTimeout();
  }
  _handleReceive(e, t) {
    var s, i, o, a, c, u, l;
    const r = !!this.config.schema && ("cardinalityInference" in this.config ? !!this.config.cardinalityInference : !0);
    switch (hn[t.op] || this._log.info("[receive]", e, t.op, t), t.op) {
      case "init-ok": {
        this._setStatus(ee.AUTHENTICATED), this._reconnectTimeoutMs = 0, this._setAttrs(t.attrs), this._flushPendingMessages(), this._sessionId = t["session-id"];
        for (const p of Object.keys(this._rooms)) {
          const y = (i = (s = this._presence[p]) == null ? void 0 : s.result) == null ? void 0 : i.user, m = (o = this._rooms[p]) == null ? void 0 : o.roomType;
          this._tryJoinRoom(m, p, y);
        }
        break;
      }
      case "add-query-exists": {
        this.notifyOneQueryOnce(R(t.q));
        break;
      }
      case "add-query-ok": {
        const { q: p, result: y } = t, m = R(p);
        if (!this._hasQueryListeners() && !this.querySubs.currentValue[m])
          break;
        const _ = (c = (a = y == null ? void 0 : y[0]) == null ? void 0 : a.data) == null ? void 0 : c["page-info"], w = (l = (u = y == null ? void 0 : y[0]) == null ? void 0 : u.data) == null ? void 0 : l.aggregate, b = sn(y), g = this.ensureAttrs(), k = ye(
          g,
          b,
          r,
          this.config.useDateObjects
        );
        this.querySubs.updateInPlace((T) => {
          if (!T[m]) {
            this._log.info("Missing value in querySubs", { hash: m, q: p });
            return;
          }
          T[m].result = {
            store: k,
            attrsStore: g,
            pageInfo: _,
            aggregate: w,
            processedTxId: t["processed-tx-id"]
          };
        }), this._cleanupPendingMutationsQueries(), this.notifyOne(m), this.notifyOneQueryOnce(m), this._cleanupPendingMutationsTimeout();
        break;
      }
      case "start-sync-ok": {
        this._syncTable.onStartSyncOk(t);
        break;
      }
      case "sync-load-batch": {
        this._syncTable.onSyncLoadBatch(t);
        break;
      }
      case "sync-init-finish": {
        this._syncTable.onSyncInitFinish(t);
        break;
      }
      case "sync-update-triples": {
        this._syncTable.onSyncUpdateTriples(t);
        break;
      }
      case "start-stream-ok": {
        this._instantStream.onStartStreamOk(t);
        break;
      }
      case "stream-flushed": {
        this._instantStream.onStreamFlushed(t);
        break;
      }
      case "append-failed": {
        this._instantStream.onAppendFailed(t);
        break;
      }
      case "stream-append": {
        this._instantStream.onStreamAppend(t);
        break;
      }
      case "refresh-ok": {
        const { computations: p, attrs: y } = t, m = t["processed-tx-id"];
        y && this._setAttrs(y), this._cleanupPendingMutationsTimeout();
        const _ = this._rewriteMutations(
          this.ensureAttrs(),
          this._pendingMutations(),
          m
        );
        _ !== this._pendingMutations() && this.kv.updateInPlace((g) => {
          g.pendingMutations = _;
        });
        const w = it(_.entries()), b = p.map((g) => {
          var U, ae, Pe, je;
          const k = g["instaql-query"], T = g["instaql-result"], S = R(k), v = sn(T), E = this.ensureAttrs(), D = ye(
            E,
            v,
            r,
            this.config.useDateObjects
          ), { store: ue, attrsStore: ne } = this._applyOptimisticUpdates(
            D,
            E,
            w,
            m
          ), re = (ae = (U = T == null ? void 0 : T[0]) == null ? void 0 : U.data) == null ? void 0 : ae["page-info"], H = (je = (Pe = T == null ? void 0 : T[0]) == null ? void 0 : Pe.data) == null ? void 0 : je.aggregate;
          return {
            q: k,
            hash: S,
            store: ue,
            attrsStore: ne,
            pageInfo: re,
            aggregate: H
          };
        });
        b.forEach(
          ({ hash: g, q: k, store: T, attrsStore: S, pageInfo: v, aggregate: E }) => {
            this.querySubs.updateInPlace((D) => {
              if (!D[g]) {
                this._log.error("Missing value in querySubs", { hash: g, q: k });
                return;
              }
              D[g].result = {
                store: T,
                attrsStore: S,
                pageInfo: v,
                aggregate: E,
                processedTxId: m
              };
            });
          }
        ), this._cleanupPendingMutationsQueries(), b.forEach(({ hash: g }) => {
          this.notifyOne(g);
        });
        break;
      }
      case "transact-ok": {
        const { "client-event-id": p, "tx-id": y } = t;
        this._inFlightMutationEventIds.delete(p);
        const _ = this._rewriteMutations(
          this.ensureAttrs(),
          this._pendingMutations()
        ).get(p);
        if (!_)
          break;
        this._updatePendingMutations((b) => {
          b.set(p, {
            ...b.get(p),
            "tx-id": y,
            confirmed: Date.now()
          });
        });
        const w = [];
        for (const b of _["tx-steps"])
          if (b[0] === "add-attr") {
            const g = b[1];
            w.push(g);
          }
        if (w.length) {
          const b = Object.values(this.ensureAttrs().attrs);
          this._setAttrs([...b, ...w]);
        }
        this._finishTransaction("synced", p), this._cleanupPendingMutationsTimeout();
        break;
      }
      case "patch-presence": {
        const p = t["room-id"];
        this._trySetRoomConnected(p, !0), this._patchPresencePeers(p, t.edits), this._notifyPresenceSubs(p);
        break;
      }
      case "refresh-presence": {
        const p = t["room-id"];
        this._trySetRoomConnected(p, !0), this._setPresencePeers(p, t.data), this._notifyPresenceSubs(p);
        break;
      }
      case "server-broadcast": {
        const p = t["room-id"], y = t.topic;
        this._trySetRoomConnected(p, !0), this._notifyBroadcastSubs(p, y, t);
        break;
      }
      case "join-room-ok": {
        const p = t["room-id"];
        if (!this._rooms[p]) {
          this._roomsPendingLeave[p] && (this._tryLeaveRoom(p), delete this._roomsPendingLeave[p]);
          break;
        }
        this._trySetRoomConnected(p, !0), this._flushEnqueuedRoomData(p);
        break;
      }
      case "leave-room-ok": {
        const p = t["room-id"];
        this._trySetRoomConnected(p, !1);
        break;
      }
      case "join-room-error":
        const f = t["room-id"], d = this._rooms[f];
        d && (d.error = t.error), this._notifyPresenceSubs(f);
        break;
      case "error":
        this._handleReceiveError(t);
        break;
      default:
        this._log.info("Unknown op", t.op, t);
        break;
    }
  }
  createWriteStream(e) {
    return this._instantStream.createWriteStream(e);
  }
  createReadStream(e) {
    return this._instantStream.createReadStream(e);
  }
  _pendingMutations() {
    return this.kv.currentValue.pendingMutations ?? /* @__PURE__ */ new Map();
  }
  _updatePendingMutations(e) {
    this.kv.updateInPlace((t) => {
      const r = t.pendingMutations ?? /* @__PURE__ */ new Map();
      t.pendingMutations = r, e(r);
    });
  }
  /**
   * @param {'timeout' | 'error'} status
   * @param {string} eventId
   * @param {{message?: string, type?: string, status?: number, hint?: unknown}} errorMsg
   */
  _handleMutationError(e, t, r) {
    const s = this._pendingMutations().get(t);
    if (s && (e !== "timeout" || !s["tx-id"])) {
      this._updatePendingMutations((o) => (o.delete(t), o)), this._inFlightMutationEventIds.delete(t);
      const i = {
        message: r.message,
        hint: r.hint
      };
      this.notifyAll(), this.notifyAttrsSubs(), this.notifyMutationErrorSubs(i), this._finishTransaction(e, t, r);
    }
  }
  _handleReceiveError(e) {
    var a, c, u, l, f, d;
    console.log("error", e);
    const t = e["client-event-id"];
    this._inFlightMutationEventIds.delete(t);
    const r = this._pendingMutations().get(t), s = {
      message: e.message || "Uh-oh, something went wrong. Ping Joe & Stopa."
    };
    if (e.hint && (s.hint = e.hint), r) {
      this._handleMutationError("error", t, e);
      return;
    }
    if ((a = e["original-event"]) != null && a.hasOwnProperty("q") && ((c = e["original-event"]) == null ? void 0 : c.op) === "add-query") {
      const p = (u = e["original-event"]) == null ? void 0 : u.q, y = R(p);
      this.notifyQueryError(R(p), s), this.notifyQueryOnceError(p, y, t, s);
      return;
    }
    if (((l = e["original-event"]) == null ? void 0 : l.op) === "init") {
      if (e.type === "record-not-found" && ((f = e.hint) == null ? void 0 : f["record-type"]) === "app-user") {
        this.changeCurrentUser(null);
        return;
      }
      this._setStatus(ee.ERRORED, s), this.notifyAll();
      return;
    }
    switch ((d = e["original-event"]) == null ? void 0 : d.op) {
      case "resync-table": {
        this._syncTable.onResyncError(e);
        return;
      }
      case "start-sync": {
        this._syncTable.onStartSyncError(e);
        return;
      }
      case "start-stream":
      case "append-stream":
      case "subscribe-stream":
      case "unsubscribe-stream": {
        this._instantStream.onRecieveError(e);
        return;
      }
    }
    const o = { ...e };
    delete o.message, delete o.hint, console.error(e.message, o), e.hint && console.error(
      `This error comes with some debugging information. Here it is: 
`,
      e.hint
    );
  }
  notifyQueryOnceError(e, t, r, s) {
    var o;
    const i = (o = this.queryOnceDfds[t]) == null ? void 0 : o.find((a) => a.eventId === r);
    i && (i.dfd.reject(s), this._completeQueryOnce(e, t, i.dfd));
  }
  _setAttrs(e) {
    this.attrs = new ce(
      e.reduce((t, r) => (t[r.id] = r, t), {}),
      this._linkIndex
    ), this.notifyAttrsSubs();
  }
  _startQuerySub(e, t) {
    const r = M();
    return this.querySubs.updateInPlace((s) => {
      s[t] = s[t] || { q: e, result: null, eventId: r }, s[t].lastAccessed = Date.now();
    }), this._trySendAuthed(r, { op: "add-query", q: e }), r;
  }
  subscribeTable(e, t) {
    return this._syncTable.subscribe(e, t);
  }
  /**
   *  When a user subscribes to a query the following side effects occur:
   *
   *  - We update querySubs to include the new query
   *  - We update queryCbs to include the new cb
   *  - If we already have a result for the query we call cb immediately
   *  - We send the server an `add-query` message
   *
   *  Returns an unsubscribe function
   */
  subscribeQuery(e, t, r) {
    this.config.disableValidation || an(e, this.config.schema), r && "ruleParams" in r && (e = { $$ruleParams: r.ruleParams, ...e });
    const s = R(e), i = this.getPreviousResult(e);
    return i && t(i), this.queryCbs[s] = this.queryCbs[s] ?? [], this.queryCbs[s].push({ q: e, cb: t }), this._startQuerySub(e, s), () => {
      this._unsubQuery(e, s, t);
    };
  }
  queryOnce(e, t) {
    this.config.disableValidation || an(e, this.config.schema), t && "ruleParams" in t && (e = { $$ruleParams: t.ruleParams, ...e });
    const r = new rn();
    if (!this._isOnline)
      return r.reject(
        new Error("We can't run `queryOnce`, because the device is offline.")
      ), r.promise;
    if (!this.querySubs)
      return r.reject(
        new Error(
          "We can't run `queryOnce` on the backend. Use adminAPI.query instead: https://www.instantdb.com/docs/backend#query"
        )
      ), r.promise;
    const s = R(e), i = this._startQuerySub(e, s);
    return this.queryOnceDfds[s] = this.queryOnceDfds[s] ?? [], this.queryOnceDfds[s].push({ q: e, dfd: r, eventId: i }), setTimeout(
      () => r.reject(new Error("Query timed out")),
      ho
    ), r.promise;
  }
  _completeQueryOnce(e, t, r) {
    this.queryOnceDfds[t] && (this.queryOnceDfds[t] = this.queryOnceDfds[t].filter(
      (s) => s.dfd !== r
    ), this._cleanupQuery(e, t));
  }
  _unsubQuery(e, t, r) {
    this.queryCbs[t] && (this.queryCbs[t] = this.queryCbs[t].filter((s) => s.cb !== r), this._cleanupQuery(e, t));
  }
  _hasQueryListeners(e) {
    var t, r;
    return !!((t = this.queryCbs[e]) != null && t.length || (r = this.queryOnceDfds[e]) != null && r.length);
  }
  _cleanupQuery(e, t) {
    this._hasQueryListeners(t) || (delete this.queryCbs[t], delete this.queryOnceDfds[t], delete this._dataForQueryCache[t], this.querySubs.unloadKey(t), this._trySendAuthed(M(), { op: "remove-query", q: e }));
  }
  // When we `pushTx`, it's possible that we don't yet have `this.attrs`
  // This means that `tx-steps` in `pendingMutations` will include `add-attr`
  // commands for attrs that already exist.
  //
  // This will also affect `add-triple` and `retract-triple` which
  // reference attr-ids that do not match the server.
  //
  // We fix this by rewriting `tx-steps` in each `pendingMutation`.
  // We remove `add-attr` commands for attrs that already exist.
  // We update `add-triple` and `retract-triple` commands to use the
  // server attr-ids.
  /**
   *
   * @param {s.AttrsStore} attrs
   * @param {any} muts
   * @param {number} [processedTxId]
   */
  _rewriteMutations(e, t, r) {
    if (!e) return t;
    if (!t) return /* @__PURE__ */ new Map();
    const s = (l) => {
      const [f, d, p] = l["forward-identity"];
      return $(e, d, p);
    }, i = (l) => {
      const [f, d, p] = l["forward-identity"];
      return he(e, d, p);
    }, o = { attrIdMap: {}, refSwapAttrIds: /* @__PURE__ */ new Set() };
    let a = !1;
    const c = (l, f) => {
      const d = [];
      for (const p of l) {
        const [y] = p;
        if (y === "add-attr") {
          const [_, w] = p, b = s(w);
          if (b && w.id !== b.id) {
            o.attrIdMap[w.id] = b.id, a = !0;
            continue;
          }
          if (w["value-type"] === "ref") {
            const g = i(w);
            if (g) {
              o.attrIdMap[w.id] = g.id, o.refSwapAttrIds.add(w.id), a = !0;
              continue;
            }
          }
        }
        if (r && f && r >= f && y === "add-attr" || y === "update-attr" || y === "delete-attr") {
          a = !0;
          continue;
        }
        const m = a ? Zs(o, p) : p;
        d.push(m);
      }
      return a ? d : l;
    }, u = /* @__PURE__ */ new Map();
    for (const [l, f] of t.entries())
      u.set(l, {
        ...f,
        "tx-steps": c(f["tx-steps"], f["tx-id"])
      });
    return a ? u : t;
  }
  _rewriteMutationsSorted(e, t) {
    return it(this._rewriteMutations(e, t).entries());
  }
  // ---------------------------
  // Transact
  /**
   * @returns {s.AttrsStore}
   */
  optimisticAttrs() {
    var i, o;
    const e = [...this._pendingMutations().values()].flatMap((a) => a["tx-steps"]), t = new Set(
      e.filter(([a, c]) => a === "delete-attr").map(([a, c]) => c)
    ), r = [];
    for (const [a, c] of e)
      if (a === "add-attr")
        r.push(c);
      else if (a === "update-attr" && c.id && ((i = this.attrs) != null && i.getAttr(c.id))) {
        const u = { ...this.attrs.getAttr(c.id), ...c };
        r.push(u);
      }
    if (!t.size && !r.length)
      return this.attrs || new ce({}, this._linkIndex);
    const s = { ...((o = this.attrs) == null ? void 0 : o.attrs) || {} };
    for (const a of r)
      s[a.id] = a;
    for (const a of t)
      delete s[a];
    return new ce(s, this._linkIndex);
  }
  /** Runs instaql on a query and a store */
  dataForQuery(e, t = !0) {
    const r = this._errorMessage;
    if (r)
      return { error: r };
    if (!this.querySubs || !this.kv.currentValue.pendingMutations) return;
    const s = this.querySubs.version(), i = this.querySubs.currentValue, o = this.kv.version(), a = this._pendingMutations(), { q: c, result: u } = i[e] || {};
    if (!u) return;
    const l = this._dataForQueryCache[e];
    if (l && s === l.querySubVersion && o === l.pendingMutationsVersion)
      return l;
    let f = u.store, d = u.attrsStore;
    const { pageInfo: p, aggregate: y, processedTxId: m } = u, _ = this._rewriteMutationsSorted(
      d,
      a
    );
    if (t) {
      const b = this._applyOptimisticUpdates(
        f,
        d,
        _,
        m
      );
      f = b.store, d = b.attrsStore;
    }
    return { data: Rt(
      { store: f, attrsStore: d, pageInfo: p, aggregate: y },
      c
    ), querySubVersion: s, pendingMutationsVersion: o };
  }
  _applyOptimisticUpdates(e, t, r, s) {
    for (const [i, o] of r)
      if (!o["tx-id"] || s && o["tx-id"] > s) {
        const a = Zr(e, t, o["tx-steps"]);
        e = a.store, t = a.attrsStore;
      }
    return { store: e, attrsStore: t };
  }
  /** Re-compute all subscriptions */
  notifyAll() {
    Object.keys(this.queryCbs).forEach((e) => {
      this.querySubs.waitForKeyToLoad(e).then(() => this.notifyOne(e)).catch(() => this.notifyOne(e));
    });
  }
  loadedNotifyAll() {
    this.kv.waitForKeyToLoad("pendingMutations").then(() => this.notifyAll()).catch(() => this.notifyAll());
  }
  shutdown() {
    var e;
    this._log.info("[shutdown]", this.config.appId), this._isShutdown = !0, (e = this._transport) == null || e.close();
  }
  /**
   * Sends mutation to server and schedules a timeout to cancel it if
   * we don't hear back in time.
   * Note: If we're offline we don't schedule a timeout, we'll schedule it
   * later once we're back online and send the mutation again
   *
   */
  _sendMutation(e, t) {
    if (t.error) {
      this._handleMutationError("error", e, {
        message: t.error.message
      });
      return;
    }
    if (this.status !== ee.AUTHENTICATED) {
      this._finishTransaction("enqueued", e);
      return;
    }
    const r = Math.max(
      6e3,
      Math.min(
        this._inFlightMutationEventIds.size + 1,
        // Defensive code in case we don't clean up in flight mutation event ids
        this._pendingMutations().size + 1
      ) * 6e3
    );
    this._isOnline ? (this._trySend(e, t), setTimeout(() => {
      this._isOnline && this._handleMutationError("timeout", e, {
        message: "transaction timed out"
      });
    }, r)) : this._finishTransaction("enqueued", e);
  }
  // ---------------------------
  // Websocket
  /** Send messages we accumulated while we were connecting */
  _flushPendingMessages() {
    Object.keys(this.queryCbs).map((i) => this.querySubs.currentValue[i]).filter((i) => i).forEach(({ eventId: i, q: o }) => {
      this._trySendAuthed(i, { op: "add-query", q: o });
    }), Object.values(this.queryOnceDfds).flat().forEach(({ eventId: i, q: o }) => {
      this._trySendAuthed(i, { op: "add-query", q: o });
    });
    const r = this._rewriteMutations(
      this.ensureAttrs(),
      this._pendingMutations()
    );
    r !== this._pendingMutations() && this.kv.updateInPlace((i) => {
      i.pendingMutations = r;
    }), it(r.entries()).forEach(([i, o]) => {
      o["tx-id"] || this._sendMutation(i, o);
    }), this._syncTable.flushPending();
  }
  /**
   * Clean up pendingMutations that all queries have seen
   */
  _cleanupPendingMutationsQueries() {
    let e = Number.MAX_SAFE_INTEGER;
    for (const { result: t } of Object.values(this.querySubs.currentValue))
      t != null && t.processedTxId && (e = Math.min(e, t == null ? void 0 : t.processedTxId));
    this._updatePendingMutations((t) => {
      for (const [r, s] of Array.from(t.entries()))
        s["tx-id"] && s["tx-id"] <= e && t.delete(r);
    });
  }
  /**
   * After mutations is confirmed by server, we give each query 30 sec
   * to update its results. If that doesn't happen, we assume query is
   * unaffected by this mutation and it’s safe to delete it from local queue
   */
  _cleanupPendingMutationsTimeout() {
    if (this._pendingMutations().size < this._pendingMutationCleanupThreshold)
      return;
    const e = Date.now();
    this._updatePendingMutations((t) => {
      for (const [r, s] of Array.from(t.entries()))
        s.confirmed && s.confirmed + this._pendingTxCleanupTimeout < e && t.delete(r);
    });
  }
  _trySendAuthed(...e) {
    this.status === ee.AUTHENTICATED && this._trySend(...e);
  }
  _trySend(e, t, r) {
    if (this._transport.isOpen()) {
      switch (hn[t.op] || this._log.info("[send]", this._transport.id, t.op, {
        "client-event-id": e,
        ...t
      }), t.op) {
        case "transact": {
          this._inFlightMutationEventIds.add(e);
          break;
        }
        case "init":
          this._inFlightMutationEventIds.clear();
      }
      this._transport.send({ "client-event-id": e, ...t });
    }
  }
  _startSocket() {
    if (this._wsOk = null, this._isShutdown) {
      this._log.info(
        "[socket][start]",
        this.config.appId,
        "Reactor has been shut down and will not start a new socket"
      );
      return;
    }
    if (this._transport && this._transport.isConnecting()) {
      this._log.info(
        "[socket][start]",
        this._transport.id,
        "maintained as current transport, we were still in a connecting state"
      );
      return;
    }
    const e = this._transport;
    this._transport = go({
      transportType: this._transportType,
      appId: this.config.appId,
      apiURI: this.config.apiURI,
      wsURI: this.config.websocketURI,
      EventSourceImpl: this._EventSource
    }), this._transport.onopen = this._transportOnOpen, this._transport.onmessage = this._transportOnMessage, this._transport.onclose = this._transportOnClose, this._transport.onerror = this._transportOnError, this._log.info("[socket][start]", this._transport.id), e != null && e.isOpen() && (this._log.info(
      "[socket][start]",
      this._transport.id,
      "close previous transport id = ",
      e.id
    ), e.close());
  }
  /**
   * Given a key, returns a stable local id, unique to this device and app.
   *
   * This can be useful if you want to create guest ids for example.
   *
   * Note: If the user deletes their local storage, this id will change.
   *
   */
  async getLocalId(e) {
    const t = `localToken_${e}`;
    if (this.kv.currentValue[t])
      return this.kv.currentValue[t];
    const r = await this.kv.waitForKeyToLoad(t);
    if (r)
      return r;
    const s = M();
    return this.kv.updateInPlace((i) => {
      i[t] || (i[t] = s);
    }), await this.kv.waitForKeyToLoad(t);
  }
  // ----
  // Auth
  _replaceUrlAfterOAuth() {
    if (typeof URL > "u")
      return;
    const e = new URL(window.location.href);
    if (e.searchParams.get(st)) {
      const t = e.toString();
      e.searchParams.delete(st), e.searchParams.delete("code"), e.searchParams.delete("error");
      const r = e.pathname + (e.searchParams.size ? "?" + e.searchParams : "") + e.hash;
      if (history.replaceState(history.state, "", r), // @ts-ignore (waiting for ts support)
      typeof navigation == "object" && // @ts-ignore (waiting for ts support)
      typeof navigation.addEventListener == "function" && // @ts-ignore (waiting for ts support)
      typeof navigation.removeEventListener == "function") {
        let s = !1;
        const i = (o) => {
          var a;
          s || (s = !0, navigation.removeEventListener("navigate", i), !o.userInitiated && o.navigationType === "replace" && ((a = o.destination) == null ? void 0 : a.url) === t && history.replaceState(history.state, "", r));
        };
        navigation.addEventListener("navigate", i);
      }
    }
  }
  /**
   *
   * @returns Promise<null | {error: {message: string}}>
   */
  async _oauthLoginInit() {
    var s, i, o, a;
    if (typeof window > "u" || typeof window.location > "u" || typeof URLSearchParams > "u")
      return null;
    const e = new URLSearchParams(window.location.search);
    if (!e.get(st))
      return null;
    const t = e.get("error");
    if (t)
      return this._replaceUrlAfterOAuth(), { error: { message: t } };
    const r = e.get("code");
    if (!r)
      return null;
    this._replaceUrlAfterOAuth();
    try {
      const c = await this._getCurrentUser(), u = (c == null ? void 0 : c.type) === "guest", { user: l } = await tn({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        code: r,
        refreshToken: u ? c.refresh_token : void 0
      });
      return this.setCurrentUser(l), null;
    } catch (c) {
      return ((s = c == null ? void 0 : c.body) == null ? void 0 : s.type) === "record-not-found" && ((o = (i = c == null ? void 0 : c.body) == null ? void 0 : i.hint) == null ? void 0 : o["record-type"]) === "app-oauth-code" && await this._hasCurrentUser() ? null : { error: { message: ((a = c == null ? void 0 : c.body) == null ? void 0 : a.message) || "Error logging in." } };
    }
  }
  async _waitForOAuthCallbackResponse() {
    return await this._oauthCallbackResponse;
  }
  __subscribeMutationErrors(e) {
    return this.mutationErrorCbs.push(e), () => {
      this.mutationErrorCbs = this.mutationErrorCbs.filter((t) => t !== e);
    };
  }
  subscribeAuth(e) {
    this.authCbs.push(e);
    const t = this._currentUserCached;
    t.isLoading || e(this._currentUserCached);
    let r = !1;
    return this.getCurrentUser().then((s) => {
      r || Ne(s, t) || e(s);
    }), () => {
      r = !0, this.authCbs = this.authCbs.filter((s) => s !== e);
    };
  }
  async getAuth() {
    const { user: e, error: t } = await this.getCurrentUser();
    if (t)
      throw new V("Could not get current user: " + t.message);
    return e;
  }
  subscribeConnectionStatus(e) {
    return this.connectionStatusCbs.push(e), () => {
      this.connectionStatusCbs = this.connectionStatusCbs.filter(
        (t) => t !== e
      );
    };
  }
  subscribeAttrs(e) {
    return this.attrsCbs.push(e), this.attrs && e(this.attrs.attrs), () => {
      this.attrsCbs = this.attrsCbs.filter((t) => t !== e);
    };
  }
  notifyAuthSubs(e) {
    this.authCbs.forEach((t) => t(e));
  }
  notifyMutationErrorSubs(e) {
    this.mutationErrorCbs.forEach((t) => t(e));
  }
  notifyAttrsSubs() {
    if (!this.attrs) return;
    const e = this.optimisticAttrs();
    this.attrsCbs.forEach((t) => t(e.attrs));
  }
  notifyConnectionStatusSubs(e) {
    this.connectionStatusCbs.forEach((t) => t(e));
  }
  async setCurrentUser(e) {
    this.kv.updateInPlace((t) => {
      t[Te] = e;
    }), await this.kv.waitForKeyToLoad(Te);
  }
  getCurrentUserCached() {
    return this._currentUserCached;
  }
  async _getCurrentUser() {
    const e = await this.kv.waitForKeyToLoad(Te);
    return typeof e == "string" ? JSON.parse(e) : e;
  }
  async getCurrentUser() {
    const e = await this._waitForOAuthCallbackResponse();
    if (e != null && e.error) {
      const t = { error: e.error, user: void 0 };
      return this._currentUserCached = { isLoading: !1, ...t }, t;
    }
    try {
      const r = { user: await this._getCurrentUser(), error: void 0 };
      return this._currentUserCached = {
        isLoading: !1,
        ...r
      }, r;
    } catch (t) {
      return {
        user: void 0,
        isLoading: !1,
        error: { message: (t == null ? void 0 : t.message) || "Error loading user" }
      };
    }
  }
  async _hasCurrentUser() {
    const e = await this.kv.waitForKeyToLoad(Te);
    return typeof e == "string" ? JSON.parse(e) != null : e != null;
  }
  async changeCurrentUser(e) {
    var r;
    const { user: t } = await this.getCurrentUser();
    if (!Ne(t, e)) {
      await this.setCurrentUser(e), await this.updateUser(e);
      try {
        (r = this._broadcastChannel) == null || r.postMessage({ type: "auth" });
      } catch (s) {
        console.error("Error posting message to broadcast channel", s);
      }
    }
  }
  async syncUserToEndpoint(e) {
    if (this.config.firstPartyPath)
      try {
        await fetch(this.config.firstPartyPath + "/", {
          method: "POST",
          body: JSON.stringify({
            type: "sync-user",
            appId: this.config.appId,
            user: e
          }),
          headers: {
            "Content-Type": "application/json"
          }
        });
      } catch (t) {
        this._log.error("Error syncing user with external endpoint", t);
      }
  }
  async updateUser(e) {
    try {
      await this.syncUserToEndpoint(e);
    } catch (r) {
      this._log.error("Error syncing user with external endpoint", r);
    }
    const t = { error: void 0, user: e };
    this._currentUserCached = { isLoading: !1, ...t }, this._dataForQueryCache = {}, this.querySubs.updateInPlace((r) => {
      Object.keys(r).forEach((s) => {
        delete r[s].result;
      });
    }), this._reconnectTimeoutMs = 0, this._transport.close(), this._oauthCallbackResponse = null, this.notifyAuthSubs(t);
  }
  sendMagicCode({ email: e }) {
    return Mi({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      email: e
    });
  }
  async signInWithMagicCode({ email: e, code: t }) {
    var o;
    const r = await this.getCurrentUser(), s = ((o = r == null ? void 0 : r.user) == null ? void 0 : o.type) === "guest", i = await Pi({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      email: e,
      code: t,
      refreshToken: s ? r.user.refresh_token : void 0
    });
    return await this.changeCurrentUser(i.user), i;
  }
  async signInWithCustomToken(e) {
    const t = await ji({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      refreshToken: e
    });
    return await this.changeCurrentUser(t.user), t;
  }
  async signInAsGuest() {
    const e = await Di({
      apiURI: this.config.apiURI,
      appId: this.config.appId
    });
    return await this.changeCurrentUser(e.user), e;
  }
  potentiallyInvalidateToken(e, t) {
    var i;
    const r = (i = e == null ? void 0 : e.user) == null ? void 0 : i.refresh_token;
    if (!r)
      return;
    if (t.invalidateToken === !1) {
      this._log.info("[auth-invalidate] skipped invalidateToken");
      return;
    }
    Ri({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      refreshToken: r
    }).then(() => {
      this._log.info("[auth-invalidate] completed invalidateToken");
    }).catch((o) => {
    });
  }
  async signOut(e) {
    const t = await this.getCurrentUser();
    this.potentiallyInvalidateToken(t, e), await this.changeCurrentUser(null);
  }
  /**
   * Creates an OAuth authorization URL.
   *
   * @param {Object} params - The parameters to create the authorization URL.
   * @param {string} params.clientName - The name of the client requesting authorization.
   * @param {string} params.redirectURL - The URL to redirect users to after authorization.
   * @returns {string} The created authorization URL.
   */
  createAuthorizationURL({ clientName: e, redirectURL: t }) {
    const { apiURI: r, appId: s } = this.config;
    return `${r}/runtime/oauth/start?app_id=${s}&client_name=${e}&redirect_uri=${t}`;
  }
  /**
   * @param {Object} params
   * @param {string} params.code - The code received from the OAuth service.
   * @param {string} [params.codeVerifier] - The code verifier used to generate the code challenge.
   */
  async exchangeCodeForToken({ code: e, codeVerifier: t }) {
    var o;
    const r = await this.getCurrentUser(), s = ((o = r == null ? void 0 : r.user) == null ? void 0 : o.type) === "guest", i = await tn({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      code: e,
      codeVerifier: t,
      refreshToken: s ? r.user.refresh_token : void 0
    });
    return await this.changeCurrentUser(i.user), i;
  }
  issuerURI() {
    const { apiURI: e, appId: t } = this.config;
    return `${e}/runtime/${t}`;
  }
  /**
   * @param {Object} params
   * @param {string} params.clientName - The name of the client requesting authorization.
   * @param {string} params.idToken - The id_token from the external service
   * @param {string | null | undefined} [params.nonce] - The nonce used when requesting the id_token from the external service
   */
  async signInWithIdToken({ idToken: e, clientName: t, nonce: r }) {
    var a;
    const s = await this.getCurrentUser(), i = (a = s == null ? void 0 : s.user) == null ? void 0 : a.refresh_token, o = await $i({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      idToken: e,
      clientName: t,
      nonce: r,
      refreshToken: i
    });
    return await this.changeCurrentUser(o.user), o;
  }
  // --------
  // Rooms
  /**
   * @param {string} roomType
   * @param {string} roomId
   * @param {any | null | undefined} [initialPresence] -- initial presence data to send when joining the room
   * @returns () => void
   */
  joinRoom(e, t, r) {
    let s = !1;
    this._rooms[t] || (s = !0, this._rooms[t] = {
      roomType: e,
      isConnected: !1,
      error: void 0
    }), this._presence[t] = this._presence[t] || {};
    const i = this._presence[t].result;
    return r && !i && (this._presence[t].result = this._presence[t].result || {}, this._presence[t].result.user = r, this._notifyPresenceSubs(t)), s && this._tryJoinRoom(e, t, r), () => {
      this._cleanupRoom(t);
    };
  }
  _cleanupRoom(e) {
    var t, r, s;
    if (!((r = (t = this._presence[e]) == null ? void 0 : t.handlers) != null && r.length) && !Object.keys(this._broadcastSubs[e] ?? {}).length) {
      const i = (s = this._rooms[e]) == null ? void 0 : s.isConnected;
      delete this._rooms[e], delete this._presence[e], delete this._broadcastSubs[e], i ? this._tryLeaveRoom(e) : this._roomsPendingLeave[e] = !0;
    }
  }
  // --------
  // Presence
  // TODO: look into typing again
  getPresence(e, t, r = {}) {
    const s = this._rooms[t], i = this._presence[t];
    return !s || !i || !i.result ? null : {
      ...qi(i.result, r, this._sessionId),
      isLoading: !s.isConnected,
      error: s.error
    };
  }
  // TODO: look into typing again
  publishPresence(e, t, r) {
    const s = this._rooms[t], i = this._presence[t];
    if (!s || !i)
      return;
    i.result = i.result || {};
    const o = {
      ...i.result.user,
      ...r
    };
    i.result.user = o, s.isConnected && (this._trySetPresence(t, o), this._notifyPresenceSubs(t));
  }
  _trySetPresence(e, t) {
    this._trySendAuthed(M(), {
      op: "set-presence",
      "room-id": e,
      data: t
    });
  }
  _tryJoinRoom(e, t, r) {
    this._trySendAuthed(M(), {
      op: "join-room",
      "room-type": e,
      "room-id": t,
      data: r
    }), delete this._roomsPendingLeave[t];
  }
  _tryLeaveRoom(e) {
    this._trySendAuthed(M(), { op: "leave-room", "room-id": e });
  }
  _trySetRoomConnected(e, t) {
    const r = this._rooms[e];
    r && (r.isConnected = t);
  }
  // TODO: look into typing again
  subscribePresence(e, t, r, s) {
    const i = this.joinRoom(
      e,
      t,
      // Oct 28, 2025
      // Note: initialData is deprecated.
      // Keeping here for backwards compatibility
      r.initialPresence || r.initialData
    ), o = { ...r, roomId: t, cb: s, prev: null };
    return this._presence[t] = this._presence[t] || {}, this._presence[t].handlers = this._presence[t].handlers || [], this._presence[t].handlers.push(o), this._notifyPresenceSub(t, o), () => {
      var a, c;
      this._presence[t].handlers = ((c = (a = this._presence[t]) == null ? void 0 : a.handlers) == null ? void 0 : c.filter((u) => u !== o)) ?? [], i();
    };
  }
  _notifyPresenceSubs(e) {
    var t, r;
    (r = (t = this._presence[e]) == null ? void 0 : t.handlers) == null || r.forEach((s) => {
      this._notifyPresenceSub(e, s);
    });
  }
  _notifyPresenceSub(e, t) {
    const r = this.getPresence("", e, t);
    r && (t.prev && !Ki(r, t.prev) || (t.prev = r, t.cb(r)));
  }
  _patchPresencePeers(e, t) {
    var o, a, c;
    const r = ((a = (o = this._presence[e]) == null ? void 0 : o.result) == null ? void 0 : a.peers) || {};
    let s = Object.fromEntries(
      Object.entries(r).map(([u, l]) => [u, { data: l }])
    );
    (c = this._presence[e]) == null || c.result;
    const i = We(s, (u) => {
      for (let [l, f, d] of t)
        switch (f) {
          case "+":
            wr(u, l, d);
            break;
          case "r":
            Kt(u, l, d);
            break;
          case "-":
            kn(u, l);
            break;
        }
      delete u[this._sessionId];
    });
    this._setPresencePeers(e, i);
  }
  _setPresencePeers(e, t) {
    const r = { ...t };
    delete r[this._sessionId];
    const s = Object.fromEntries(
      Object.entries(r).map(([i, o]) => [i, o.data])
    );
    this._presence = We(this._presence, (i) => {
      Kt(i, [e, "result", "peers"], s);
    });
  }
  // --------
  // Broadcast
  publishTopic({ roomType: e, roomId: t, topic: r, data: s }) {
    const i = this._rooms[t];
    if (i) {
      if (!i.isConnected) {
        this._broadcastQueue[t] = this._broadcastQueue[t] ?? [], this._broadcastQueue[t].push({ topic: r, roomType: e, data: s });
        return;
      }
      this._tryBroadcast(t, e, r, s);
    }
  }
  _tryBroadcast(e, t, r, s) {
    this._trySendAuthed(M(), {
      op: "client-broadcast",
      "room-id": e,
      roomType: t,
      topic: r,
      data: s
    });
  }
  subscribeTopic(e, t, r, s) {
    const i = this.joinRoom(e, t);
    return this._broadcastSubs[t] = this._broadcastSubs[t] || {}, this._broadcastSubs[t][r] = this._broadcastSubs[t][r] || [], this._broadcastSubs[t][r].push(s), this._presence[t] = this._presence[t] || {}, () => {
      this._broadcastSubs[t][r] = this._broadcastSubs[t][r].filter((o) => o !== s), this._broadcastSubs[t][r].length || delete this._broadcastSubs[t][r], i();
    };
  }
  _notifyBroadcastSubs(e, t, r) {
    var s, i, o;
    (o = (i = (s = this._broadcastSubs) == null ? void 0 : s[e]) == null ? void 0 : i[t]) == null || o.forEach((a) => {
      var l, f, d, p, y, m;
      const c = (l = r.data) == null ? void 0 : l.data, u = r.data["peer-id"] === this._sessionId ? (d = (f = this._presence[e]) == null ? void 0 : f.result) == null ? void 0 : d.user : (m = (y = (p = this._presence[e]) == null ? void 0 : p.result) == null ? void 0 : y.peers) == null ? void 0 : m[r.data["peer-id"]];
      return a(c, u);
    });
  }
  // --------
  // Storage
  async uploadFile(e, t, r) {
    var o;
    const s = await this.getCurrentUser(), i = (o = s == null ? void 0 : s.user) == null ? void 0 : o.refresh_token;
    return xi({
      ...r,
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      path: e,
      file: t,
      refreshToken: i
    });
  }
  async deleteFile(e) {
    var i;
    const t = await this.getCurrentUser(), r = (i = t == null ? void 0 : t.user) == null ? void 0 : i.refresh_token;
    return await Ui({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      path: e,
      refreshToken: r
    });
  }
  // Deprecated Storage API (Jan 2025)
  // ---------------------------------
  async upload(e, t) {
    var c;
    const r = await this.getCurrentUser(), s = (c = r == null ? void 0 : r.user) == null ? void 0 : c.refresh_token, i = e || t.name, o = await Li({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      fileName: i,
      refreshToken: s
    });
    return await Fi(o, t);
  }
  async getDownloadUrl(e) {
    var i;
    const t = await this.getCurrentUser(), r = (i = t == null ? void 0 : t.user) == null ? void 0 : i.refresh_token;
    return await Ni({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      path: e,
      refreshToken: r
    });
  }
}
function Oo(n, e) {
  return new Ye(
    sr(n, e),
    // (XXX): LinksDef<any> stems from TypeScript’s inability to reconcile the
    // type EntitiesWithLinks<EntitiesWithoutLinks, Links> with
    // EntitiesWithoutLinks. TypeScript is strict about ensuring that types are
    // correctly aligned and does not allow for substituting a type that might
    // be broader or have additional properties.
    e,
    void 0
  );
}
function Io(n) {
  return new Je(n, {});
}
function Co() {
  return new N("string", !0, !1);
}
function Eo() {
  return new N("number", !0, !1);
}
function Mo() {
  return new N("boolean", !0, !1);
}
function Po() {
  return new N("date", !0, !1);
}
function jo() {
  return new N("json", !0, !1);
}
function Do() {
  return new N("json", !0, !1);
}
function sr(n, e) {
  var s, i, o, a;
  const t = { fwd: {}, rev: {} };
  for (const c of Object.values(e))
    (s = t.fwd)[i = c.forward.on] || (s[i] = {}), (o = t.rev)[a = c.reverse.on] || (o[a] = {}), t.fwd[c.forward.on][c.forward.label] = {
      entityName: c.reverse.on,
      cardinality: c.forward.has
    }, t.rev[c.reverse.on][c.reverse.label] = {
      entityName: c.forward.on,
      cardinality: c.reverse.has
    };
  return Object.fromEntries(
    Object.entries(n).map(([c, u]) => [
      c,
      new Je(u.attrs, {
        ...t.fwd[c],
        ...t.rev[c]
      })
    ])
  );
}
function $o({
  entities: n,
  links: e,
  rooms: t
}) {
  const r = e ?? {}, s = t ?? {};
  return new Ye(
    sr(n, r),
    // (XXX): LinksDef<any> stems from TypeScript's inability to reconcile the
    // type EntitiesWithLinks<EntitiesWithoutLinks, Links> with
    // EntitiesWithoutLinks. TypeScript is strict about ensuring that types are
    // correctly aligned and does not allow for substituting a type that might
    // be broader or have additional properties.
    r,
    s
  );
}
const P = {
  // constructs
  graph: Oo,
  schema: $o,
  entity: Io,
  // value types
  string: Co,
  number: Eo,
  boolean: Mo,
  date: Po,
  json: jo,
  any: Do
};
let xe;
function Ro(n, e) {
  xe == null || xe.dispose();
  const t = qo(e), r = Lo(e, a), s = Uo(xo(n));
  function i(l) {
    var f;
    l.source === s.element.contentWindow && ((f = l.data) == null ? void 0 : f.type) === "close" && t.isVisible() && a();
  }
  function o(l) {
    const f = l.shiftKey && l.ctrlKey && l.key === "0", d = l.key === "Escape" || l.key === "Esc";
    (f || d && t.isVisible()) && a();
  }
  function a() {
    t.isVisible() ? t.element.style.display = "none" : (t.element.style.display = "block", t.element.contains(s.element) || t.element.appendChild(s.element));
  }
  function c() {
    t.element.remove(), r.element.remove(), removeEventListener("keydown", o), removeEventListener("message", i);
  }
  function u() {
    document.body.appendChild(t.element), document.body.appendChild(r.element), addEventListener("keydown", o), addEventListener("message", i), xe = {
      dispose: c
    };
  }
  return u();
}
function xo(n) {
  return `${xt || Hn ? "http://localhost:3000" : "https://instantdb.com"}/_devtool?appId=${n}`;
}
function Uo(n) {
  const e = document.createElement("iframe");
  return e.src = n, e.className = "instant-devtool-iframe", Object.assign(e.style, {
    width: "100%",
    height: "100%",
    backgroundColor: "white",
    border: "none"
  }), { element: e };
}
function Lo(n, e) {
  const t = `
    <svg width="32" height="32" viewBox="0 0 512 512" fill="none" xmlns="http://www.w3.org/2000/svg">
      <rect width="512" height="512" fill="black"/>
      <rect x="97.0973" y="91.3297" width="140" height="330" fill="white"/>
    </svg>
  `, r = document.createElement("button");
  return r.innerHTML = t, r.className = "instant-devtool-toggler", Object.assign(r.style, {
    // pos
    position: "fixed",
    ...Fo(n.position),
    height: "32px",
    width: "32px",
    // layout
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    zIndex: "9010",
    // look
    padding: "0",
    margin: "0",
    border: "none",
    cursor: "pointer"
  }), r.addEventListener("click", e), { element: r };
}
function Fo(n) {
  switch (n) {
    case "bottom-left":
      return { bottom: "24px", left: "24px" };
    case "bottom-right":
      return { bottom: "24px", right: "24px" };
    case "top-right":
      return { top: "24px", right: "24px" };
    case "top-left":
      return { top: "24px", left: "24px" };
  }
}
function No(n) {
  switch (n) {
    case "bottom-left":
      return { bottom: "24px", right: "24px", left: "60px", top: "72px" };
    case "bottom-right":
      return { bottom: "24px", left: "24px", right: "60px", top: "72px" };
    case "top-right":
      return { top: "24px", left: "24px", right: "60px", bottom: "72px" };
    case "top-left":
      return { top: "24px", right: "24px", left: "60px", bottom: "72px" };
  }
}
function qo(n) {
  const e = document.createElement("div");
  Object.assign(e.style, {
    position: "fixed",
    ...No(n.position),
    display: "block",
    borderRadius: "4px",
    border: "1px #ccc solid",
    boxShadow: "0px 0px 8px #00000044",
    backgroundColor: "#eee",
    zIndex: "999990"
  }), e.style.display = "none", e.className = "instant-devtool-container";
  function t() {
    return e.style.display !== "none";
  }
  return { element: e, isVisible: t };
}
function Ko(n, e) {
  return e && e.refresh_token ? new Response(JSON.stringify({ ok: !0 }), {
    headers: {
      "Content-Type": "application/json",
      // 7 day expiry
      "Set-Cookie": `instant_user_${n.appId}=${encodeURIComponent(JSON.stringify(e))}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=604800`
    }
  }) : new Response(JSON.stringify({ ok: !0 }), {
    headers: {
      "Content-Type": "application/json",
      // remove the cookie (some browsers)
      "Set-Cookie": `instant_user_${n.appId}=; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=-1`
    }
  });
}
function Ue(n, e) {
  return new Response(JSON.stringify({ ok: !1, error: e }), {
    status: n,
    headers: { "Content-Type": "application/json" }
  });
}
const ia = (n) => ({
  POST: async (e) => {
    let t;
    try {
      t = await e.json();
    } catch {
      return Ue(400, "Invalid JSON body");
    }
    if (!t.type)
      return Ue(400, 'Missing "type" field');
    if (t.appId !== n.appId)
      return Ue(403, "App ID mismatch");
    switch (t.type) {
      case "sync-user":
        return Ko(n, t.user ?? null);
      default:
        return Ue(400, `Unknown type: ${t.type}`);
    }
  }
}), oa = (n) => {
  var i, o, a, c, u, l;
  const e = {};
  for (const [f, d] of Object.entries(n.entities)) {
    const p = d, y = {};
    for (const [m, _] of Object.entries(p.attrs)) {
      const w = _;
      let b;
      switch (w.valueType) {
        case "string":
          b = P.string();
          break;
        case "number":
          b = P.number();
          break;
        case "boolean":
          b = P.boolean();
          break;
        case "date":
          b = P.date();
          break;
        case "json":
          b = P.json();
          break;
        default:
          b = P.json();
      }
      w.required || (b = b.optional()), (i = w.config) != null && i.indexed && (b = b.indexed()), (o = w.config) != null && o.unique && (b = b.unique()), y[m] = b;
    }
    e[f] = P.entity(y);
  }
  const t = n.links || {}, r = {};
  if (n.rooms)
    for (const [f, d] of Object.entries(n.rooms)) {
      const p = d, y = {};
      for (const [_, w] of Object.entries(
        p.presence.attrs
      )) {
        const b = w;
        let g;
        switch (b.valueType) {
          case "string":
            g = P.string();
            break;
          case "number":
            g = P.number();
            break;
          case "boolean":
            g = P.boolean();
            break;
          case "date":
            g = P.date();
            break;
          case "json":
            g = P.json();
            break;
          default:
            g = P.json();
        }
        b.required || (g = g.optional()), (a = b.config) != null && a.indexed && (g = g.indexed()), (c = b.config) != null && c.unique && (g = g.unique()), y[_] = g;
      }
      const m = {};
      if (p.topics)
        for (const [_, w] of Object.entries(p.topics)) {
          const b = w, g = {};
          for (const [k, T] of Object.entries(b.attrs)) {
            const S = T;
            let v;
            switch (S.valueType) {
              case "string":
                v = P.string();
                break;
              case "number":
                v = P.number();
                break;
              case "boolean":
                v = P.boolean();
                break;
              case "date":
                v = P.date();
                break;
              case "json":
                v = P.json();
                break;
              default:
                v = P.json();
            }
            S.required || (v = v.optional()), (u = S.config) != null && u.indexed && (v = v.indexed()), (l = S.config) != null && l.unique && (v = v.unique()), g[k] = v;
          }
          m[_] = P.entity(g);
        }
      r[f] = {
        presence: P.entity(y),
        topics: m
      };
    }
  return P.schema({
    entities: e,
    links: t,
    rooms: r
  });
}, zo = typeof window > "u" || "Deno" in globalThis;
class aa {
  constructor(e) {
    h(this, "params");
    h(this, "db");
    // stores all of the query promises so that ssr can read them
    // and send the relevant results alongside the html that resulted in the query resolving
    h(this, "resultMap", /* @__PURE__ */ new Map());
    h(this, "queryResolvedCallbacks", []);
    h(this, "subscribe", (e) => {
      this.queryResolvedCallbacks.push(e);
    });
    // Runs on the client when ssr gets html script tags
    h(this, "addQueryResult", (e, t) => {
      this.resultMap.set(e, {
        type: t.type,
        status: "success",
        data: t,
        promise: null,
        error: null
      }), zo || (this.db._reactor.attrs || this.db._reactor._setAttrs(t.attrs), this.db._reactor._addQueryData(
        t.query,
        t,
        !!this.db._reactor.config.schema
      ));
    });
    // creates an entry in the results map
    // and returns the same thing added to the map
    h(this, "query", (e, t) => {
      const { hash: r, query: s } = this.hashQuery(e, t);
      if (this.db._reactor.status === "authenticated") {
        const a = this.db.queryOnce(e, t);
        let c = {
          status: "pending",
          type: "session",
          data: void 0,
          error: void 0,
          promise: a
        };
        return a.then((u) => {
          c.status = "success", c.data = u, c.promise = null;
        }), a.catch((u) => {
          c.status = "error", c.error = u, c.promise = null;
        }), this.resultMap.set(r, c), c;
      }
      const i = this.getTriplesAndAttrsForQuery(s);
      let o = {
        status: "pending",
        type: "http",
        data: void 0,
        error: void 0,
        promise: i
      };
      return i.then((a) => {
        o.status = "success", o.data = a, o.promise = null;
      }), i.catch((a) => {
        o.status = "error", o.error = a, o.promise = null;
      }), i.then((a) => {
        this.queryResolvedCallbacks.forEach((c) => {
          c({
            queryHash: r,
            query: s,
            attrs: a.attrs,
            triples: a.triples,
            pageInfo: a.pageInfo
          });
        });
      }), this.resultMap.set(r, o), o;
    });
    h(this, "getExistingResultForQuery", (e, t) => {
      const { hash: r } = this.hashQuery(e, t);
      return this.resultMap.get(r);
    });
    // creates a query result from a set of triples, query, and attrs
    // can be run server side or client side
    h(this, "completeIsomorphic", (e, t, r, s) => {
      var l, f, d, p, y, m, _, w;
      const i = {};
      r.forEach((b) => {
        i[b.id] = b;
      });
      const o = !!((d = (f = (l = this.db) == null ? void 0 : l._reactor) == null ? void 0 : f.config) != null && d.schema) && ("cardinalityInference" in ((y = (p = this.db) == null ? void 0 : p._reactor) == null ? void 0 : y.config) ? !!((_ = (m = this.db) == null ? void 0 : m._reactor.config) != null && _.cardinalityInference) : !0), a = new ce(
        r.reduce((b, g) => (b[g.id] = g, b), {}),
        Tt((w = this.db) == null ? void 0 : w._reactor.config.schema)
      ), c = ye(
        a,
        t,
        o,
        this.params.db._reactor.config.useDateObjects || !1
      );
      return Rt(
        {
          store: c,
          attrsStore: a,
          pageInfo: s,
          aggregate: void 0
        },
        e
      );
    });
    h(this, "hashQuery", (e, t) => {
      e && t && "ruleParams" in t && (e = { $$ruleParams: t.ruleParams, ...e });
      const r = e ? Jo(e) : null;
      return { hash: R(r), query: r };
    });
    // Run by the server to get triples and attrs
    h(this, "getTriplesAndAttrsForQuery", async (e) => {
      var t, r, s, i, o, a;
      try {
        const c = await fetch(
          `${this.db._reactor.config.apiURI}/runtime/framework/query`,
          {
            method: "POST",
            headers: {
              "app-id": this.params.db._reactor.config.appId,
              "Content-Type": "application/json",
              Authorization: this.params.token ? `Bearer ${this.params.token}` : void 0
            },
            body: JSON.stringify({
              query: e
            })
          }
        );
        if (!c.ok)
          throw new Error("Error getting triples from server");
        const u = await c.json(), l = u == null ? void 0 : u.attrs;
        if (!l)
          throw new Error("No attrs");
        const f = (s = (r = (t = u.result) == null ? void 0 : t[0].data) == null ? void 0 : r["datalog-result"]) == null ? void 0 : s["join-rows"][0], d = (a = (o = (i = u.result) == null ? void 0 : i[0]) == null ? void 0 : o.data) == null ? void 0 : a["page-info"];
        return {
          attrs: l,
          triples: f,
          type: "http",
          queryHash: this.hashQuery(e).hash,
          query: e,
          pageInfo: d
        };
      } catch (c) {
        const u = new Error(
          "Error getting triples from framework client"
        );
        throw u.cause = c, u;
      }
    });
    this.params = e, this.db = e.db, this.resultMap = /* @__PURE__ */ new Map();
  }
}
const Wo = {
  apiURI: "https://api.instantdb.com",
  websocketURI: "wss://api.instantdb.com/runtime/session"
};
function Vo() {
  return globalThis.__instantDbSchemaHashStore = globalThis.__instantDbSchemaHashStore ?? /* @__PURE__ */ new WeakMap(), globalThis.__instantDbSchemaHashStore;
}
function Qo() {
  return globalThis.__instantDbStore = globalThis.__instantDbStore ?? {}, globalThis.__instantDbStore;
}
function St(n) {
  const e = n.__adminToken;
  return n.appId + "_" + (n.websocketURI || "default_ws_uri") + "_" + (n.apiURI || "default_api_uri") + "_" + (e || "client_only") + "_" + n.useDateObjects;
}
const At = Qo(), pn = Vo();
class Bo {
  constructor(e) {
    /**
     * Sends a magic code to the user's email address.
     *
     * Once you send the magic code, see {@link auth.signInWithMagicCode} to let the
     * user verify.
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *  db.auth.sendMagicCode({email: "example@gmail.com"})
     *    .catch((err) => console.error(err.body?.message))
     */
    h(this, "sendMagicCode", (e) => this.db.sendMagicCode(e));
    /**
     * Verify a magic code that was sent to the user's email address.
     *
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *  db.auth.signInWithMagicCode({email: "example@gmail.com", code: "123456"})
     *       .catch((err) => console.error(err.body?.message))
     */
    h(this, "signInWithMagicCode", (e) => this.db.signInWithMagicCode(e));
    /**
     * Sign in a user with a refresh token
     *
     * @see https://instantdb.com/docs/backend#frontend-auth-sign-in-with-token
     *
     * @example
     *   // Get the token from your backend
     *   const token = await fetch('/signin', ...);
     *   //Sign in
     *   db.auth.signInWithToken(token);
     */
    h(this, "signInWithToken", (e) => this.db.signInWithCustomToken(e));
    /**
     * Sign in as guest, creating a new user without email
     *
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *   db.auth.signInAsGuest();
     */
    h(this, "signInAsGuest", () => this.db.signInAsGuest());
    /**
     * Create an authorization url to sign in with an external provider.
     *
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *   // Get the authorization url from your backend
     *   const url = db.auth.createAuthorizationUrl({
     *     clientName: "google",
     *     redirectURL: window.location.href,
     *   });
     *
     *   // Put it in a sign in link
     *   <a href={url}>Log in with Google</a>
     */
    h(this, "createAuthorizationURL", (e) => this.db.createAuthorizationURL(e));
    /**
     * Sign in with the id_token from an external provider like Google
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *   db.auth
     *  .signInWithIdToken({
     *    // Token from external service
     *    idToken: id_token,
     *    // The name you gave the client when you registered it with Instant
     *    clientName: "google",
     *    // The nonce, if any, that you used when you initiated the auth flow
     *    // with the external service.
     *    nonce: your_nonce
     *  })
     *  .catch((err) => console.error(err.body?.message));
     *
     */
    h(this, "signInWithIdToken", (e) => this.db.signInWithIdToken(e));
    /**
     * Sign in with the id_token from an external provider like Google
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *   db.auth
     *  .exchangeOAuthCode({
     *    // code received in redirect from OAuth callback
     *    code: code
     *    // The PKCE code_verifier, if any, that you used when you
     *    // initiated the auth flow
     *    codeVerifier: your_code_verifier
     *  })
     *  .catch((err) => console.error(err.body?.message));
     *
     */
    h(this, "exchangeOAuthCode", (e) => this.db.exchangeCodeForToken(e));
    /**
     * OpenID Discovery path for use with tools like
     * expo-auth-session that use auto-discovery of
     * OAuth parameters.
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *   const discovery = useAutoDiscovery(
     *     db.auth.issuerURI()
     *   );
     */
    h(this, "issuerURI", () => this.db.issuerURI());
    /**
     * Sign out the current user
     */
    h(this, "signOut", (e = { invalidateToken: !0 }) => this.db.signOut(e));
    this.db = e;
  }
}
class Go {
  constructor(e) {
    /**
     * Uploads file at the provided path.
     *
     * @see https://instantdb.com/docs/storage
     * @example
     *   const [file] = e.target.files; // result of file input
     *   const data = await db.storage.uploadFile('photos/demo.png', file);
     */
    h(this, "uploadFile", (e, t, r = {}) => this.db.uploadFile(e, t, r));
    /**
     * Deletes a file by path name.
     *
     * @see https://instantdb.com/docs/storage
     * @example
     *   await db.storage.delete('photos/demo.png');
     */
    h(this, "delete", (e) => this.db.deleteFile(e));
    // Deprecated Storage API (Jan 2025)
    // ---------------------------------
    /**
     * @deprecated. Use `db.storage.uploadFile` instead
     * remove in the future.
     */
    h(this, "upload", (e, t) => this.db.upload(e, t));
    /**
     * @deprecated Use `db.storage.uploadFile` instead
     */
    h(this, "put", this.upload);
    /**
     * @deprecated. getDownloadUrl will be removed in the future.
     * Use `useQuery` instead to query and fetch for valid urls
     *
     * db.useQuery({
     *   $files: {
     *     $: {
     *       where: {
     *         path: "moop.png"
     *       }
     *     }
     *   }
     * })
     */
    h(this, "getDownloadUrl", (e) => this.db.getDownloadUrl(e));
    this.db = e;
  }
}
class Ho {
  constructor(e) {
    /**
     * Creates a new ReadableStream for the given clientId.
     *
     * @example
     *   const stream = db.streams.createReadStream({clientId: clientId})
     *   for await (const chunk of stream) {
     *     console.log(chunk);
     *   }
     */
    h(this, "createReadStream", (e) => this.db.createReadStream(e));
    /**
     * Creates a new WritableStream for the given clientId.
     *
     * @example
     *   const writeStream = db.streams.createWriteStream({clientId: clientId})
     *   const writer = writeStream.getWriter();
     *   writer.write('Hello world');
     *   writer.close();
     */
    h(this, "createWriteStream", (e) => this.db.createWriteStream(e));
    this.db = e;
  }
}
function Jo(n) {
  return JSON.parse(JSON.stringify(n));
}
class Yo {
  constructor(e) {
    h(this, "_reactor");
    h(this, "auth");
    h(this, "storage");
    h(this, "streams");
    h(this, "tx", Kn());
    this._reactor = e, this.auth = new Bo(this._reactor), this.storage = new Go(this._reactor), this.streams = new Ho(this._reactor);
  }
  /**
   * Use this to write data! You can create, update, delete, and link objects
   *
   * @see https://instantdb.com/docs/instaml
   *
   * @example
   *   // Create a new object in the `goals` namespace
   *   const goalId = id();
   *   db.transact(db.tx.goals[goalId].update({title: "Get fit"}))
   *
   *   // Update the title
   *   db.transact(db.tx.goals[goalId].update({title: "Get super fit"}))
   *
   *   // Delete it
   *   db.transact(db.tx.goals[goalId].delete())
   *
   *   // Or create an association:
   *   todoId = id();
   *   db.transact([
   *    db.tx.todos[todoId].update({ title: 'Go on a run' }),
   *    db.tx.goals[goalId].link({todos: todoId}),
   *  ])
   */
  transact(e) {
    return this._reactor.pushTx(e);
  }
  getLocalId(e) {
    return this._reactor.getLocalId(e);
  }
  /**
   * Use this to query your data!
   *
   * @see https://instantdb.com/docs/instaql
   *
   * @example
   *  // listen to all goals
   *  db.subscribeQuery({ goals: {} }, (resp) => {
   *    console.log(resp.data.goals)
   *  })
   *
   *  // goals where the title is "Get Fit"
   *  db.subscribeQuery(
   *    { goals: { $: { where: { title: "Get Fit" } } } },
   *    (resp) => {
   *      console.log(resp.data.goals)
   *    }
   *  )
   *
   *  // all goals, _alongside_ their todos
   *  db.subscribeQuery({ goals: { todos: {} } }, (resp) => {
   *    console.log(resp.data.goals)
   *  });
   */
  subscribeQuery(e, t, r) {
    return this._reactor.subscribeQuery(e, t, r);
  }
  /**
   * Listen for the logged in state. This is useful
   * for deciding when to show a login screen.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const unsub = db.subscribeAuth((auth) => {
   *     if (auth.user) {
   *     console.log('logged in as', auth.user.email)
   *    } else {
   *      console.log('logged out')
   *    }
   *  })
   */
  subscribeAuth(e) {
    return this._reactor.subscribeAuth(e);
  }
  /**
   * One time query for the logged in state. This is useful
   * for scenarios where you want to know the current auth
   * state without subscribing to changes.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const user = await db.getAuth();
   *   console.log('logged in as', user.email)
   */
  getAuth() {
    return this._reactor.getAuth();
  }
  /**
   * Listen for connection status changes to Instant. This is useful
   * for building things like connectivity indicators
   *
   * @see https://www.instantdb.com/docs/patterns#connection-status
   * @example
   *   const unsub = db.subscribeConnectionStatus((status) => {
   *     const connectionState =
   *       status === 'connecting' || status === 'opened'
   *         ? 'authenticating'
   *       : status === 'authenticated'
   *         ? 'connected'
   *       : status === 'closed'
   *         ? 'closed'
   *       : status === 'errored'
   *         ? 'errored'
   *       : 'unexpected state';
   *
   *     console.log('Connection status:', connectionState);
   *   });
   */
  subscribeConnectionStatus(e) {
    return this._reactor.subscribeConnectionStatus(e);
  }
  /**
   * Join a room to publish and subscribe to topics and presence.
   *
   * @see https://instantdb.com/docs/presence-and-topics
   * @example
   * // init
   * const db = init();
   * const room = db.joinRoom(roomType, roomId);
   * // usage
   * const unsubscribeTopic = room.subscribeTopic("foo", console.log);
   * const unsubscribePresence = room.subscribePresence({}, console.log);
   * room.publishTopic("hello", { message: "hello world!" });
   * room.publishPresence({ name: "joe" });
   * // later
   * unsubscribePresence();
   * unsubscribeTopic();
   * room.leaveRoom();
   */
  joinRoom(e = "_defaultRoomType", t = "_defaultRoomId", r) {
    return {
      leaveRoom: this._reactor.joinRoom(
        e,
        t,
        r == null ? void 0 : r.initialPresence
      ),
      subscribeTopic: (i, o) => this._reactor.subscribeTopic(e, t, i, o),
      subscribePresence: (i, o) => this._reactor.subscribePresence(e, t, i, o),
      publishTopic: (i, o) => this._reactor.publishTopic({ roomType: e, roomId: t, topic: i, data: o }),
      publishPresence: (i) => this._reactor.publishPresence(e, t, i),
      getPresence: (i) => this._reactor.getPresence(e, t, i)
    };
  }
  shutdown() {
    delete At[St(this._reactor.config)], this._reactor.shutdown();
  }
  /**
   * Use this for one-off queries.
   * Returns local data if available, otherwise fetches from the server.
   * Because we want to avoid stale data, this method will throw an error
   * if the user is offline or there is no active connection to the server.
   *
   * @see https://instantdb.com/docs/instaql
   *
   * @example
   *
   *  const resp = await db.queryOnce({ goals: {} });
   *  console.log(resp.data.goals)
   */
  queryOnce(e, t) {
    return this._reactor.queryOnce(e, t);
  }
  /**
   * @deprecated This is an experimental function that is not yet ready for production use.
   * Use this function to sync an entire namespace.
   * It has many limitations that will be removed in the future:
   * 1. Must be used with an admin token
   * 2. Does not support permissions
   * 3. Does not support where clauses
   * 4. Does not support links
   * It also does not support multiple top-level namespaces. For example,
   *  {posts: {}, users: {}} is invalid. Only `posts` or `users` is allowed, but not both.
   */
  _syncTableExperimental(e, t) {
    return this._reactor.subscribeTable(e, t);
  }
}
function yn(n) {
  if (!n)
    return "0";
  const e = pn.get(n);
  if (e)
    return e;
  const t = R(n);
  return pn.set(n, t), t;
}
function Zo(n, e) {
  return yn(n._reactor.config.schema) !== yn(e);
}
function Xo(n, e, t, r, s) {
  var u;
  const i = {
    ...n,
    appId: (u = n.appId) == null ? void 0 : u.trim(),
    useDateObjects: n.useDateObjects ?? !1
  }, o = At[St(i)];
  if (o)
    return Zo(o, i.schema) && o._reactor.updateSchema(i.schema), o;
  const a = new ko(
    {
      ...Wo,
      ...i,
      cardinalityInference: !!i.schema
    },
    e || Qn,
    t || Bn,
    { ...r || {}, "@instantdb/core": Yn },
    s
  ), c = new Yo(a);
  return At[St(i)] = c, ea(i.appId, i.devtool), c;
}
function ea(n, e) {
  if (typeof window > "u" || typeof window.location > "u" || typeof document > "u" || typeof e == "boolean" && !e)
    return;
  const t = {
    position: "bottom-right",
    allowedHosts: ["localhost"],
    ...typeof e == "object" ? e : {}
  };
  t.allowedHosts.includes(window.location.hostname) && Ro(n, t);
}
const ca = Xo, ua = Vn;
export {
  Bo as Auth,
  aa as FrameworkClient,
  Qn as IndexedDBStorage,
  He as InstantAPIError,
  Yo as InstantCoreDatabase,
  V as InstantError,
  fo as InstantStream,
  C as QueryValidationError,
  ko as Reactor,
  Xi as SSEConnection,
  Go as Storage,
  ua as StorageInterface,
  Vn as StoreInterface,
  Ho as Streams,
  oo as SyncTableCallbackEventType,
  G as TransactionValidationError,
  Bn as WindowNetworkListener,
  Jo as coerceQuery,
  Be as coerceToDate,
  ia as createInstantRouteHandler,
  Ys as getOps,
  P as i,
  M as id,
  Xo as init,
  ca as init_experimental,
  Hs as lookup,
  oa as parseSchemaFromJSON,
  ra as setInstantWarningsEnabled,
  sa as tx,
  Kn as txInit,
  an as validateQuery,
  Zi as validateTransactions,
  Yn as version,
  R as weakHash
};
